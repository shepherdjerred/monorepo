# Copy to .xcode.env.local (untracked) if Xcode cannot find Node in build phases.
# Example for mise-managed Node:
# export NODE_BINARY=$(mise where node)/bin/node

# Generic fallback:
export NODE_BINARY=$(command -v node)
