/**
 * The shops in the game: sign creation, clicks, the container lock and its guards, owner notices,
 * the NPC shop dialogs and the {@code /shop} command. Main thread only; storage is reached through
 * futures completed back onto the main thread.
 */
@NullMarked
package com.shepherdjerred.thestorm.shops.adapter.paper;

import org.jspecify.annotations.NullMarked;
