---
id: log-2026-05-30-scout-template-testing-db
type: log
status: complete
board: false
---

# Scout Template Testing DB

## Summary

Answered what Scout for LoL's committed test template database is for.

The template lives at `packages/scout-for-lol/packages/backend/src/testing/template.db`. It is generated by `packages/scout-for-lol/packages/backend/scripts/generate-test-template-db.ts` via `bun run generate:test-template`, which is also run by the backend `db:generate` script.

The generated SQLite database contains the migrated schema plus seeded `Season` rows. Integration tests call `createTestDatabase(...)`, which copies this template into a per-test temporary `test.db` and constructs a Prisma client against that copy. This avoids running `prisma db push` or migrations for every test suite, making test setup much faster and avoiding prior Bun/Prisma setup instability.

## Session Log — 2026-05-30

### Done

- Loaded the relevant Scout/LoL and Prisma skills.
- Checked local recall for prior Scout template DB context.
- Inspected `packages/scout-for-lol/packages/backend/scripts/generate-test-template-db.ts`.
- Inspected `packages/scout-for-lol/packages/backend/src/testing/test-database.ts`.
- Inspected the backend package scripts and `.gitignore` exception for `src/testing/template.db`.
- Answered that the template DB is useful for Scout's current integration-test shape because it makes per-suite isolated database setup fast and avoids the brittle `prisma db push` path.

### Remaining

- None.

### Caveats

- No commands were run that regenerate or validate the database; this was a code-and-doc inspection answer only.

## Session Log — 2026-05-31

### Done

- Regenerated `packages/scout-for-lol/packages/backend/src/testing/template.db`; the checked-in DB was stale by byte comparison before regeneration.
- Updated `packages/scout-for-lol/packages/backend/scripts/generate-test-template-db.ts` so it can write to `SCOUT_TEST_TEMPLATE_DB_PATH` for non-mutating freshness checks.
- Added `packages/scout-for-lol/packages/backend/scripts/check-test-template-db.ts` and the backend `check:test-template` script.
- Added a blocking Buildkite quality gate, `scout-test-template-check`, so CI compares the committed template against a freshly generated temp DB.
- Added a lefthook `scout-test-template-check` for Scout migrations, template generator/check scripts, `template.db`, and season seed data changes.
- Updated Buildkite pipeline tests for the new quality gate.

### Remaining

- None.

### Caveats

- `bun run db:generate` initially failed through the mise shim because this worktree's mise config is not trusted; verification used the installed Bun binary directly with an explicit `PATH`.
- `scripts/ci/package.json` has `typecheck: tsc --noEmit` but does not declare `typescript`; `bun run typecheck` failed with `tsc: command not found`, so the CI package was typechecked with `bunx tsc --noEmit` after installing its declared Bun types.
- Scout's ESLint config ignores backend `scripts/*.ts`; the focused lint command completed with ignored-file warnings only.

## Session Log — 2026-05-31 Stale Behavior

### Done

- Explained that stale `template.db` makes integration tests copy and run against an old SQLite schema or old seed baseline.
- Clarified that missing tables/columns usually fail loudly, while stale constraints/indexes/defaults/seed data can create false positives or false negatives.
- Clarified that the new `check:test-template` path turns staleness into an explicit CI/pre-commit failure before tests rely on the old DB.

### Remaining

- None.

### Caveats

- No new verification commands were needed for this explanation; it is based on the implementation inspected earlier in this session.

## Session Log — 2026-05-31 PR Follow-up

### Done

- Opened PR #1000 for the Scout template freshness guard.
- Addressed Greptile's P2 stale-message feedback by documenting libsql/SQLite lockfile updates as a possible byte-drift cause.
- Addressed Greptile's P1 subprocess-exit feedback by removing the unsafe `result.exitCode.toString()` call.
- Addressed Greptile's P1 cleanup feedback by moving the stale-template `process.exit(1)` path after temp-directory cleanup.
- Re-ran focused verification after each code fix and pushed the resulting commits.

### Remaining

- Continue monitoring the latest PR head until Buildkite is green, the PR remains mergeable, and no P3-or-higher review comments remain unresolved.

### Caveats

- Buildkite soft failures are intentionally not treated as blocking for this PR-readiness loop.

## Session Log — 2026-05-31 CI Follow-up

### Done

- Investigated Buildkite build 3131 after the `scout-test-template-check` step failed.
- Updated `packages/scout-for-lol/packages/backend/scripts/check-test-template-db.ts` to compare a deterministic logical snapshot of schema and table contents instead of raw SQLite file bytes.
- Preserved stale-template failure behavior for real migration or seed drift while avoiding platform-specific SQLite page-layout false positives.
- Verified the updated checker with `bun run check:test-template`, backend `bun run typecheck`, and Prettier.

### Remaining

- Push the CI fix and continue monitoring the new PR head until Buildkite is green, the PR remains mergeable, and no P3-or-higher review comments remain unresolved.

### Caveats

- Direct Buildkite log access was unavailable in this environment due to the local escalation usage limit, so the failure was inferred from the failed Scout template status and reproduced through the equivalent local command path.
