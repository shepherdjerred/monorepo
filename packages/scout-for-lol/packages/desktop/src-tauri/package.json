{
  "name": "@scout-for-lol/desktop-rust",
  "private": true,
  "description": "Turbo shim for the Tauri Rust crate — scripts wrap cargo so the crate participates in the task graph (caching + affected).",
  "scripts": {
    "test": "cargo test --locked --all-features",
    "test:ci": "bun run test:report",
    "test:report": "bun ../../../../../scripts/run-ci-test.ts",
    "lint": "cargo fmt --check",
    "clippy": "cargo clippy --all-targets --all-features -- -D warnings"
  }
}
