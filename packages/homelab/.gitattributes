# src/cdk8s/generated/ is .gitignored and regenerated by `bun run scripts/setup.ts`,
# so it has no committed presence here. If we ever start tracking a subset
# (e.g. a stub), mark those paths linguist-generated=true here.
