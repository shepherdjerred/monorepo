// @ts-nocheck
// generated by cdk8s
import { ApiObject, ApiObjectMetadata, GroupVersionKind } from "cdk8s";
import { Construct } from "constructs";

/**
 *
 *
 * @schema DeletingPolicy
 */
export class DeletingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "DeletingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "DeletingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "DeletingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: DeletingPolicyProps): any {
    return {
      ...DeletingPolicy.GVK,
      ...toJson_DeletingPolicyProps(props),
    };
  }

  /**
   * Defines a "DeletingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(scope: Construct, id: string, props: DeletingPolicyProps) {
    super(scope, id, {
      ...DeletingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...DeletingPolicy.GVK,
      ...toJson_DeletingPolicyProps(resolved),
    };
  }
}

/**
 * @schema DeletingPolicy
 */
export interface DeletingPolicyProps {
  /**
   * @schema DeletingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
   *
   * @schema DeletingPolicy#spec
   */
  readonly spec: DeletingPolicySpec;
}

/**
 * Converts an object of type 'DeletingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyProps(
  obj: DeletingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_DeletingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
 *
 * @schema DeletingPolicySpec
 */
export interface DeletingPolicySpec {
  /**
   * Conditions is a list of conditions that must be met for a resource to be deleted.
   * Conditions filter resources that have already been matched by the match constraints,
   * namespaceSelector, and objectSelector. An empty list of conditions matches all resources.
   * There are a maximum of 64 conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY condition evaluates to FALSE, the policy is skipped.
   * 2. If ALL conditions evaluate to TRUE, the policy is executed.
   *
   * @schema DeletingPolicySpec#conditions
   */
  readonly conditions?: DeletingPolicySpecConditions[];

  /**
   * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
   *
   * @schema DeletingPolicySpec#deletionPropagationPolicy
   */
  readonly deletionPropagationPolicy?: DeletingPolicySpecDeletionPropagationPolicy;

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema DeletingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: DeletingPolicySpecMatchConstraints;

  /**
   * The schedule in Cron format
   * Required.
   *
   * @schema DeletingPolicySpec#schedule
   */
  readonly schedule: string;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema DeletingPolicySpec#variables
   */
  readonly variables?: DeletingPolicySpecVariables[];
}

/**
 * Converts an object of type 'DeletingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpec(
  obj: DeletingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    conditions: obj.conditions?.map((y) =>
      toJson_DeletingPolicySpecConditions(y),
    ),
    deletionPropagationPolicy: obj.deletionPropagationPolicy,
    matchConstraints: toJson_DeletingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    schedule: obj.schedule,
    variables: obj.variables?.map((y) => toJson_DeletingPolicySpecVariables(y)),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema DeletingPolicySpecConditions
 */
export interface DeletingPolicySpecConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema DeletingPolicySpecConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema DeletingPolicySpecConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'DeletingPolicySpecConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecConditions(
  obj: DeletingPolicySpecConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
 *
 * @schema DeletingPolicySpecDeletionPropagationPolicy
 */
export enum DeletingPolicySpecDeletionPropagationPolicy {
  /** Foreground */
  FOREGROUND = "Foreground",
  /** Background */
  BACKGROUND = "Background",
  /** Orphan */
  ORPHAN = "Orphan",
}

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema DeletingPolicySpecMatchConstraints
 */
export interface DeletingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema DeletingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: DeletingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema DeletingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema DeletingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: DeletingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema DeletingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: DeletingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema DeletingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: DeletingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraints(
  obj: DeletingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_DeletingPolicySpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_DeletingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector: toJson_DeletingPolicySpecMatchConstraintsObjectSelector(
      obj.objectSelector,
    ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_DeletingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema DeletingPolicySpecVariables
 */
export interface DeletingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema DeletingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema DeletingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'DeletingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecVariables(
  obj: DeletingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface DeletingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema DeletingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraintsExcludeResourceRules(
  obj: DeletingPolicySpecMatchConstraintsExcludeResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema DeletingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface DeletingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema DeletingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema DeletingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraintsNamespaceSelector(
  obj: DeletingPolicySpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema DeletingPolicySpecMatchConstraintsObjectSelector
 */
export interface DeletingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema DeletingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema DeletingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraintsObjectSelector(
  obj: DeletingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema DeletingPolicySpecMatchConstraintsResourceRules
 */
export interface DeletingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema DeletingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema DeletingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema DeletingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraintsResourceRules(
  obj: DeletingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | DeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | DeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema DeletingPolicyV1Alpha1
 */
export class DeletingPolicyV1Alpha1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "DeletingPolicyV1Alpha1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1alpha1",
    kind: "DeletingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "DeletingPolicyV1Alpha1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: DeletingPolicyV1Alpha1Props): any {
    return {
      ...DeletingPolicyV1Alpha1.GVK,
      ...toJson_DeletingPolicyV1Alpha1Props(props),
    };
  }

  /**
   * Defines a "DeletingPolicyV1Alpha1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: DeletingPolicyV1Alpha1Props,
  ) {
    super(scope, id, {
      ...DeletingPolicyV1Alpha1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...DeletingPolicyV1Alpha1.GVK,
      ...toJson_DeletingPolicyV1Alpha1Props(resolved),
    };
  }
}

/**
 * @schema DeletingPolicyV1Alpha1
 */
export interface DeletingPolicyV1Alpha1Props {
  /**
   * @schema DeletingPolicyV1Alpha1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
   *
   * @schema DeletingPolicyV1Alpha1#spec
   */
  readonly spec: DeletingPolicyV1Alpha1Spec;
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1Props(
  obj: DeletingPolicyV1Alpha1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_DeletingPolicyV1Alpha1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
 *
 * @schema DeletingPolicyV1Alpha1Spec
 */
export interface DeletingPolicyV1Alpha1Spec {
  /**
   * Conditions is a list of conditions that must be met for a resource to be deleted.
   * Conditions filter resources that have already been matched by the match constraints,
   * namespaceSelector, and objectSelector. An empty list of conditions matches all resources.
   * There are a maximum of 64 conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY condition evaluates to FALSE, the policy is skipped.
   * 2. If ALL conditions evaluate to TRUE, the policy is executed.
   *
   * @schema DeletingPolicyV1Alpha1Spec#conditions
   */
  readonly conditions?: DeletingPolicyV1Alpha1SpecConditions[];

  /**
   * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
   *
   * @schema DeletingPolicyV1Alpha1Spec#deletionPropagationPolicy
   */
  readonly deletionPropagationPolicy?: DeletingPolicyV1Alpha1SpecDeletionPropagationPolicy;

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1Spec#matchConstraints
   */
  readonly matchConstraints?: DeletingPolicyV1Alpha1SpecMatchConstraints;

  /**
   * The schedule in Cron format
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1Spec#schedule
   */
  readonly schedule: string;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema DeletingPolicyV1Alpha1Spec#variables
   */
  readonly variables?: DeletingPolicyV1Alpha1SpecVariables[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1Spec(
  obj: DeletingPolicyV1Alpha1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    conditions: obj.conditions?.map((y) =>
      toJson_DeletingPolicyV1Alpha1SpecConditions(y),
    ),
    deletionPropagationPolicy: obj.deletionPropagationPolicy,
    matchConstraints: toJson_DeletingPolicyV1Alpha1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    schedule: obj.schedule,
    variables: obj.variables?.map((y) =>
      toJson_DeletingPolicyV1Alpha1SpecVariables(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema DeletingPolicyV1Alpha1SpecConditions
 */
export interface DeletingPolicyV1Alpha1SpecConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecConditions(
  obj: DeletingPolicyV1Alpha1SpecConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
 *
 * @schema DeletingPolicyV1Alpha1SpecDeletionPropagationPolicy
 */
export enum DeletingPolicyV1Alpha1SpecDeletionPropagationPolicy {
  /** Foreground */
  FOREGROUND = "Foreground",
  /** Background */
  BACKGROUND = "Background",
  /** Orphan */
  ORPHAN = "Orphan",
}

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraints
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraints(
  obj: DeletingPolicyV1Alpha1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema DeletingPolicyV1Alpha1SpecVariables
 */
export interface DeletingPolicyV1Alpha1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema DeletingPolicyV1Alpha1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema DeletingPolicyV1Alpha1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecVariables(
  obj: DeletingPolicyV1Alpha1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | DeletingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
  obj: DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
  obj: DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules(
  obj: DeletingPolicyV1Alpha1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | DeletingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | DeletingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema DeletingPolicyV1Beta1
 */
export class DeletingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "DeletingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "DeletingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "DeletingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: DeletingPolicyV1Beta1Props): any {
    return {
      ...DeletingPolicyV1Beta1.GVK,
      ...toJson_DeletingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "DeletingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: DeletingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...DeletingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...DeletingPolicyV1Beta1.GVK,
      ...toJson_DeletingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema DeletingPolicyV1Beta1
 */
export interface DeletingPolicyV1Beta1Props {
  /**
   * @schema DeletingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
   *
   * @schema DeletingPolicyV1Beta1#spec
   */
  readonly spec: DeletingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1Props(
  obj: DeletingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_DeletingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
 *
 * @schema DeletingPolicyV1Beta1Spec
 */
export interface DeletingPolicyV1Beta1Spec {
  /**
   * Conditions is a list of conditions that must be met for a resource to be deleted.
   * Conditions filter resources that have already been matched by the match constraints,
   * namespaceSelector, and objectSelector. An empty list of conditions matches all resources.
   * There are a maximum of 64 conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY condition evaluates to FALSE, the policy is skipped.
   * 2. If ALL conditions evaluate to TRUE, the policy is executed.
   *
   * @schema DeletingPolicyV1Beta1Spec#conditions
   */
  readonly conditions?: DeletingPolicyV1Beta1SpecConditions[];

  /**
   * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
   *
   * @schema DeletingPolicyV1Beta1Spec#deletionPropagationPolicy
   */
  readonly deletionPropagationPolicy?: DeletingPolicyV1Beta1SpecDeletionPropagationPolicy;

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: DeletingPolicyV1Beta1SpecMatchConstraints;

  /**
   * The schedule in Cron format
   * Required.
   *
   * @schema DeletingPolicyV1Beta1Spec#schedule
   */
  readonly schedule: string;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema DeletingPolicyV1Beta1Spec#variables
   */
  readonly variables?: DeletingPolicyV1Beta1SpecVariables[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1Spec(
  obj: DeletingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    conditions: obj.conditions?.map((y) =>
      toJson_DeletingPolicyV1Beta1SpecConditions(y),
    ),
    deletionPropagationPolicy: obj.deletionPropagationPolicy,
    matchConstraints: toJson_DeletingPolicyV1Beta1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    schedule: obj.schedule,
    variables: obj.variables?.map((y) =>
      toJson_DeletingPolicyV1Beta1SpecVariables(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema DeletingPolicyV1Beta1SpecConditions
 */
export interface DeletingPolicyV1Beta1SpecConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecConditions(
  obj: DeletingPolicyV1Beta1SpecConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
 *
 * @schema DeletingPolicyV1Beta1SpecDeletionPropagationPolicy
 */
export enum DeletingPolicyV1Beta1SpecDeletionPropagationPolicy {
  /** Foreground */
  FOREGROUND = "Foreground",
  /** Background */
  BACKGROUND = "Background",
  /** Orphan */
  ORPHAN = "Orphan",
}

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema DeletingPolicyV1Beta1SpecMatchConstraints
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema DeletingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema DeletingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema DeletingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraints(
  obj: DeletingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema DeletingPolicyV1Beta1SpecVariables
 */
export interface DeletingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema DeletingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema DeletingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecVariables(
  obj: DeletingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | DeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj: DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj: DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj: DeletingPolicyV1Beta1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | DeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | DeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema GeneratingPolicy
 */
export class GeneratingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "GeneratingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "GeneratingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "GeneratingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: GeneratingPolicyProps): any {
    return {
      ...GeneratingPolicy.GVK,
      ...toJson_GeneratingPolicyProps(props),
    };
  }

  /**
   * Defines a "GeneratingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: GeneratingPolicyProps,
  ) {
    super(scope, id, {
      ...GeneratingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...GeneratingPolicy.GVK,
      ...toJson_GeneratingPolicyProps(resolved),
    };
  }
}

/**
 * @schema GeneratingPolicy
 */
export interface GeneratingPolicyProps {
  /**
   * @schema GeneratingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
   *
   * @schema GeneratingPolicy#spec
   */
  readonly spec: GeneratingPolicySpec;
}

/**
 * Converts an object of type 'GeneratingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyProps(
  obj: GeneratingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_GeneratingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
 *
 * @schema GeneratingPolicySpec
 */
export interface GeneratingPolicySpec {
  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema GeneratingPolicySpec#evaluation
   */
  readonly evaluation?: GeneratingPolicySpecEvaluation;

  /**
   * Generation defines a set of CEL expressions that will be evaluated to generate resources.
   * Required.
   *
   * @schema GeneratingPolicySpec#generate
   */
  readonly generate: GeneratingPolicySpecGenerate[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema GeneratingPolicySpec#matchConditions
   */
  readonly matchConditions?: GeneratingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources will trigger this policy.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema GeneratingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: GeneratingPolicySpecMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema GeneratingPolicySpec#variables
   */
  readonly variables?: GeneratingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema GeneratingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: GeneratingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'GeneratingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpec(
  obj: GeneratingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    evaluation: toJson_GeneratingPolicySpecEvaluation(obj.evaluation),
    generate: obj.generate?.map((y) => toJson_GeneratingPolicySpecGenerate(y)),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_GeneratingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_GeneratingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    variables: obj.variables?.map((y) =>
      toJson_GeneratingPolicySpecVariables(y),
    ),
    webhookConfiguration: toJson_GeneratingPolicySpecWebhookConfiguration(
      obj.webhookConfiguration,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema GeneratingPolicySpecEvaluation
 */
export interface GeneratingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema GeneratingPolicySpecEvaluation#admission
   */
  readonly admission?: GeneratingPolicySpecEvaluationAdmission;

  /**
   * GenerateExisting defines the configuration for generating resources for existing triggeres.
   *
   * @schema GeneratingPolicySpecEvaluation#generateExisting
   */
  readonly generateExisting?: GeneratingPolicySpecEvaluationGenerateExisting;

  /**
   * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
   *
   * @schema GeneratingPolicySpecEvaluation#orphanDownstreamOnPolicyDelete
   */
  readonly orphanDownstreamOnPolicyDelete?: GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete;

  /**
   * Synchronization defines the configuration for the synchronization of generated resources.
   *
   * @schema GeneratingPolicySpecEvaluation#synchronize
   */
  readonly synchronize?: GeneratingPolicySpecEvaluationSynchronize;
}

/**
 * Converts an object of type 'GeneratingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecEvaluation(
  obj: GeneratingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_GeneratingPolicySpecEvaluationAdmission(obj.admission),
    generateExisting: toJson_GeneratingPolicySpecEvaluationGenerateExisting(
      obj.generateExisting,
    ),
    orphanDownstreamOnPolicyDelete:
      toJson_GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete(
        obj.orphanDownstreamOnPolicyDelete,
      ),
    synchronize: toJson_GeneratingPolicySpecEvaluationSynchronize(
      obj.synchronize,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Generation defines the configuration for the generation of resources.
 *
 * @schema GeneratingPolicySpecGenerate
 */
export interface GeneratingPolicySpecGenerate {
  /**
   * Expression is a CEL expression that takes a list of resources to be generated.
   *
   * @schema GeneratingPolicySpecGenerate#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'GeneratingPolicySpecGenerate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecGenerate(
  obj: GeneratingPolicySpecGenerate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema GeneratingPolicySpecMatchConditions
 */
export interface GeneratingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConditions(
  obj: GeneratingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources will trigger this policy.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema GeneratingPolicySpecMatchConstraints
 */
export interface GeneratingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema GeneratingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: GeneratingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema GeneratingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema GeneratingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: GeneratingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema GeneratingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: GeneratingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema GeneratingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: GeneratingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraints(
  obj: GeneratingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_GeneratingPolicySpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_GeneratingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector: toJson_GeneratingPolicySpecMatchConstraintsObjectSelector(
      obj.objectSelector,
    ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_GeneratingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema GeneratingPolicySpecVariables
 */
export interface GeneratingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema GeneratingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema GeneratingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'GeneratingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecVariables(
  obj: GeneratingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema GeneratingPolicySpecWebhookConfiguration
 */
export interface GeneratingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema GeneratingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'GeneratingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecWebhookConfiguration(
  obj: GeneratingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema GeneratingPolicySpecEvaluationAdmission
 */
export interface GeneratingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema GeneratingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecEvaluationAdmission(
  obj: GeneratingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GenerateExisting defines the configuration for generating resources for existing triggeres.
 *
 * @schema GeneratingPolicySpecEvaluationGenerateExisting
 */
export interface GeneratingPolicySpecEvaluationGenerateExisting {
  /**
   * Enabled controls whether to trigger the policy for existing resources
   * If is set to "true" the policy will be triggered and applied to existing matched resources.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicySpecEvaluationGenerateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicySpecEvaluationGenerateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecEvaluationGenerateExisting(
  obj: GeneratingPolicySpecEvaluationGenerateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
 *
 * @schema GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete
 */
export interface GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete {
  /**
   * Enabled controls whether generated resources should be deleted when the policy that generated
   * them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete(
  obj: GeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Synchronization defines the configuration for the synchronization of generated resources.
 *
 * @schema GeneratingPolicySpecEvaluationSynchronize
 */
export interface GeneratingPolicySpecEvaluationSynchronize {
  /**
   * Enabled controls if generated resources should be kept in-sync with their source resource.
   * If Synchronize is set to "true" changes to generated resources will be overwritten with resource
   * data from Data or the resource specified in the Clone declaration.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicySpecEvaluationSynchronize#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicySpecEvaluationSynchronize' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecEvaluationSynchronize(
  obj: GeneratingPolicySpecEvaluationSynchronize | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface GeneratingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema GeneratingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraintsExcludeResourceRules(
  obj: GeneratingPolicySpecMatchConstraintsExcludeResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface GeneratingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraintsNamespaceSelector(
  obj: GeneratingPolicySpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema GeneratingPolicySpecMatchConstraintsObjectSelector
 */
export interface GeneratingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema GeneratingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema GeneratingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraintsObjectSelector(
  obj: GeneratingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema GeneratingPolicySpecMatchConstraintsResourceRules
 */
export interface GeneratingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema GeneratingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema GeneratingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema GeneratingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraintsResourceRules(
  obj: GeneratingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | GeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | GeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema GeneratingPolicyV1Alpha1
 */
export class GeneratingPolicyV1Alpha1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "GeneratingPolicyV1Alpha1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1alpha1",
    kind: "GeneratingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "GeneratingPolicyV1Alpha1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: GeneratingPolicyV1Alpha1Props): any {
    return {
      ...GeneratingPolicyV1Alpha1.GVK,
      ...toJson_GeneratingPolicyV1Alpha1Props(props),
    };
  }

  /**
   * Defines a "GeneratingPolicyV1Alpha1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: GeneratingPolicyV1Alpha1Props,
  ) {
    super(scope, id, {
      ...GeneratingPolicyV1Alpha1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...GeneratingPolicyV1Alpha1.GVK,
      ...toJson_GeneratingPolicyV1Alpha1Props(resolved),
    };
  }
}

/**
 * @schema GeneratingPolicyV1Alpha1
 */
export interface GeneratingPolicyV1Alpha1Props {
  /**
   * @schema GeneratingPolicyV1Alpha1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
   *
   * @schema GeneratingPolicyV1Alpha1#spec
   */
  readonly spec: GeneratingPolicyV1Alpha1Spec;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1Props(
  obj: GeneratingPolicyV1Alpha1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_GeneratingPolicyV1Alpha1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
 *
 * @schema GeneratingPolicyV1Alpha1Spec
 */
export interface GeneratingPolicyV1Alpha1Spec {
  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema GeneratingPolicyV1Alpha1Spec#evaluation
   */
  readonly evaluation?: GeneratingPolicyV1Alpha1SpecEvaluation;

  /**
   * Generation defines a set of CEL expressions that will be evaluated to generate resources.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1Spec#generate
   */
  readonly generate: GeneratingPolicyV1Alpha1SpecGenerate[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema GeneratingPolicyV1Alpha1Spec#matchConditions
   */
  readonly matchConditions?: GeneratingPolicyV1Alpha1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources will trigger this policy.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1Spec#matchConstraints
   */
  readonly matchConstraints?: GeneratingPolicyV1Alpha1SpecMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema GeneratingPolicyV1Alpha1Spec#variables
   */
  readonly variables?: GeneratingPolicyV1Alpha1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema GeneratingPolicyV1Alpha1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: GeneratingPolicyV1Alpha1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1Spec(
  obj: GeneratingPolicyV1Alpha1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    evaluation: toJson_GeneratingPolicyV1Alpha1SpecEvaluation(obj.evaluation),
    generate: obj.generate?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecGenerate(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_GeneratingPolicyV1Alpha1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    variables: obj.variables?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_GeneratingPolicyV1Alpha1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema GeneratingPolicyV1Alpha1SpecEvaluation
 */
export interface GeneratingPolicyV1Alpha1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema GeneratingPolicyV1Alpha1SpecEvaluation#admission
   */
  readonly admission?: GeneratingPolicyV1Alpha1SpecEvaluationAdmission;

  /**
   * GenerateExisting defines the configuration for generating resources for existing triggeres.
   *
   * @schema GeneratingPolicyV1Alpha1SpecEvaluation#generateExisting
   */
  readonly generateExisting?: GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting;

  /**
   * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
   *
   * @schema GeneratingPolicyV1Alpha1SpecEvaluation#orphanDownstreamOnPolicyDelete
   */
  readonly orphanDownstreamOnPolicyDelete?: GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete;

  /**
   * Synchronization defines the configuration for the synchronization of generated resources.
   *
   * @schema GeneratingPolicyV1Alpha1SpecEvaluation#synchronize
   */
  readonly synchronize?: GeneratingPolicyV1Alpha1SpecEvaluationSynchronize;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecEvaluation(
  obj: GeneratingPolicyV1Alpha1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_GeneratingPolicyV1Alpha1SpecEvaluationAdmission(
      obj.admission,
    ),
    generateExisting:
      toJson_GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting(
        obj.generateExisting,
      ),
    orphanDownstreamOnPolicyDelete:
      toJson_GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete(
        obj.orphanDownstreamOnPolicyDelete,
      ),
    synchronize: toJson_GeneratingPolicyV1Alpha1SpecEvaluationSynchronize(
      obj.synchronize,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Generation defines the configuration for the generation of resources.
 *
 * @schema GeneratingPolicyV1Alpha1SpecGenerate
 */
export interface GeneratingPolicyV1Alpha1SpecGenerate {
  /**
   * Expression is a CEL expression that takes a list of resources to be generated.
   *
   * @schema GeneratingPolicyV1Alpha1SpecGenerate#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecGenerate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecGenerate(
  obj: GeneratingPolicyV1Alpha1SpecGenerate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema GeneratingPolicyV1Alpha1SpecMatchConditions
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConditions(
  obj: GeneratingPolicyV1Alpha1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources will trigger this policy.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraints
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraints(
  obj: GeneratingPolicyV1Alpha1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema GeneratingPolicyV1Alpha1SpecVariables
 */
export interface GeneratingPolicyV1Alpha1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema GeneratingPolicyV1Alpha1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema GeneratingPolicyV1Alpha1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecVariables(
  obj: GeneratingPolicyV1Alpha1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema GeneratingPolicyV1Alpha1SpecWebhookConfiguration
 */
export interface GeneratingPolicyV1Alpha1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema GeneratingPolicyV1Alpha1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecWebhookConfiguration(
  obj: GeneratingPolicyV1Alpha1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema GeneratingPolicyV1Alpha1SpecEvaluationAdmission
 */
export interface GeneratingPolicyV1Alpha1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema GeneratingPolicyV1Alpha1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecEvaluationAdmission(
  obj: GeneratingPolicyV1Alpha1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GenerateExisting defines the configuration for generating resources for existing triggeres.
 *
 * @schema GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting
 */
export interface GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting {
  /**
   * Enabled controls whether to trigger the policy for existing resources
   * If is set to "true" the policy will be triggered and applied to existing matched resources.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting(
  obj: GeneratingPolicyV1Alpha1SpecEvaluationGenerateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
 *
 * @schema GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete
 */
export interface GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete {
  /**
   * Enabled controls whether generated resources should be deleted when the policy that generated
   * them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete(
  obj:
    | GeneratingPolicyV1Alpha1SpecEvaluationOrphanDownstreamOnPolicyDelete
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Synchronization defines the configuration for the synchronization of generated resources.
 *
 * @schema GeneratingPolicyV1Alpha1SpecEvaluationSynchronize
 */
export interface GeneratingPolicyV1Alpha1SpecEvaluationSynchronize {
  /**
   * Enabled controls if generated resources should be kept in-sync with their source resource.
   * If Synchronize is set to "true" changes to generated resources will be overwritten with resource
   * data from Data or the resource specified in the Clone declaration.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicyV1Alpha1SpecEvaluationSynchronize#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecEvaluationSynchronize' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecEvaluationSynchronize(
  obj: GeneratingPolicyV1Alpha1SpecEvaluationSynchronize | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | GeneratingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
  obj:
    | GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
  obj: GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules(
  obj: GeneratingPolicyV1Alpha1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | GeneratingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | GeneratingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema GeneratingPolicyV1Beta1
 */
export class GeneratingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "GeneratingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "GeneratingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "GeneratingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: GeneratingPolicyV1Beta1Props): any {
    return {
      ...GeneratingPolicyV1Beta1.GVK,
      ...toJson_GeneratingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "GeneratingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: GeneratingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...GeneratingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...GeneratingPolicyV1Beta1.GVK,
      ...toJson_GeneratingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema GeneratingPolicyV1Beta1
 */
export interface GeneratingPolicyV1Beta1Props {
  /**
   * @schema GeneratingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
   *
   * @schema GeneratingPolicyV1Beta1#spec
   */
  readonly spec: GeneratingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1Props(
  obj: GeneratingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_GeneratingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
 *
 * @schema GeneratingPolicyV1Beta1Spec
 */
export interface GeneratingPolicyV1Beta1Spec {
  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema GeneratingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: GeneratingPolicyV1Beta1SpecEvaluation;

  /**
   * Generation defines a set of CEL expressions that will be evaluated to generate resources.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1Spec#generate
   */
  readonly generate: GeneratingPolicyV1Beta1SpecGenerate[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema GeneratingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: GeneratingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources will trigger this policy.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: GeneratingPolicyV1Beta1SpecMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema GeneratingPolicyV1Beta1Spec#variables
   */
  readonly variables?: GeneratingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema GeneratingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: GeneratingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1Spec(
  obj: GeneratingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    evaluation: toJson_GeneratingPolicyV1Beta1SpecEvaluation(obj.evaluation),
    generate: obj.generate?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecGenerate(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_GeneratingPolicyV1Beta1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    variables: obj.variables?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_GeneratingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema GeneratingPolicyV1Beta1SpecEvaluation
 */
export interface GeneratingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema GeneratingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: GeneratingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * GenerateExisting defines the configuration for generating resources for existing triggeres.
   *
   * @schema GeneratingPolicyV1Beta1SpecEvaluation#generateExisting
   */
  readonly generateExisting?: GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting;

  /**
   * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
   *
   * @schema GeneratingPolicyV1Beta1SpecEvaluation#orphanDownstreamOnPolicyDelete
   */
  readonly orphanDownstreamOnPolicyDelete?: GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete;

  /**
   * Synchronization defines the configuration for the synchronization of generated resources.
   *
   * @schema GeneratingPolicyV1Beta1SpecEvaluation#synchronize
   */
  readonly synchronize?: GeneratingPolicyV1Beta1SpecEvaluationSynchronize;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecEvaluation(
  obj: GeneratingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_GeneratingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    generateExisting:
      toJson_GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting(
        obj.generateExisting,
      ),
    orphanDownstreamOnPolicyDelete:
      toJson_GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete(
        obj.orphanDownstreamOnPolicyDelete,
      ),
    synchronize: toJson_GeneratingPolicyV1Beta1SpecEvaluationSynchronize(
      obj.synchronize,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Generation defines the configuration for the generation of resources.
 *
 * @schema GeneratingPolicyV1Beta1SpecGenerate
 */
export interface GeneratingPolicyV1Beta1SpecGenerate {
  /**
   * Expression is a CEL expression that takes a list of resources to be generated.
   *
   * @schema GeneratingPolicyV1Beta1SpecGenerate#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecGenerate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecGenerate(
  obj: GeneratingPolicyV1Beta1SpecGenerate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema GeneratingPolicyV1Beta1SpecMatchConditions
 */
export interface GeneratingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConditions(
  obj: GeneratingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources will trigger this policy.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraints
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraints(
  obj: GeneratingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema GeneratingPolicyV1Beta1SpecVariables
 */
export interface GeneratingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema GeneratingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema GeneratingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecVariables(
  obj: GeneratingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema GeneratingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface GeneratingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema GeneratingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecWebhookConfiguration(
  obj: GeneratingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema GeneratingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface GeneratingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema GeneratingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecEvaluationAdmission(
  obj: GeneratingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GenerateExisting defines the configuration for generating resources for existing triggeres.
 *
 * @schema GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting
 */
export interface GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting {
  /**
   * Enabled controls whether to trigger the policy for existing resources
   * If is set to "true" the policy will be triggered and applied to existing matched resources.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting(
  obj: GeneratingPolicyV1Beta1SpecEvaluationGenerateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
 *
 * @schema GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete
 */
export interface GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete {
  /**
   * Enabled controls whether generated resources should be deleted when the policy that generated
   * them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete(
  obj:
    | GeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Synchronization defines the configuration for the synchronization of generated resources.
 *
 * @schema GeneratingPolicyV1Beta1SpecEvaluationSynchronize
 */
export interface GeneratingPolicyV1Beta1SpecEvaluationSynchronize {
  /**
   * Enabled controls if generated resources should be kept in-sync with their source resource.
   * If Synchronize is set to "true" changes to generated resources will be overwritten with resource
   * data from Data or the resource specified in the Clone declaration.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema GeneratingPolicyV1Beta1SpecEvaluationSynchronize#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecEvaluationSynchronize' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecEvaluationSynchronize(
  obj: GeneratingPolicyV1Beta1SpecEvaluationSynchronize | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | GeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj: GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj: GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj: GeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | GeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | GeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema ImageValidatingPolicy
 */
export class ImageValidatingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "ImageValidatingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "ImageValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "ImageValidatingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: ImageValidatingPolicyProps): any {
    return {
      ...ImageValidatingPolicy.GVK,
      ...toJson_ImageValidatingPolicyProps(props),
    };
  }

  /**
   * Defines a "ImageValidatingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: ImageValidatingPolicyProps,
  ) {
    super(scope, id, {
      ...ImageValidatingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...ImageValidatingPolicy.GVK,
      ...toJson_ImageValidatingPolicyProps(resolved),
    };
  }
}

/**
 * @schema ImageValidatingPolicy
 */
export interface ImageValidatingPolicyProps {
  /**
   * @schema ImageValidatingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
   *
   * @schema ImageValidatingPolicy#spec
   */
  readonly spec: ImageValidatingPolicySpec;
}

/**
 * Converts an object of type 'ImageValidatingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyProps(
  obj: ImageValidatingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_ImageValidatingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
 *
 * @schema ImageValidatingPolicySpec
 */
export interface ImageValidatingPolicySpec {
  /**
   * Attestations provides a list of image metadata to verify
   *
   * @schema ImageValidatingPolicySpec#attestations
   */
  readonly attestations?: ImageValidatingPolicySpecAttestations[];

  /**
   * Attestors provides a list of trusted authorities.
   *
   * @schema ImageValidatingPolicySpec#attestors
   */
  readonly attestors: ImageValidatingPolicySpecAttestors[];

  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema ImageValidatingPolicySpec#auditAnnotations
   */
  readonly auditAnnotations?: ImageValidatingPolicySpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema ImageValidatingPolicySpec#autogen
   */
  readonly autogen?: ImageValidatingPolicySpecAutogen;

  /**
   * Credentials provides credentials that will be used for authentication with registry.
   *
   * @schema ImageValidatingPolicySpec#credentials
   */
  readonly credentials?: ImageValidatingPolicySpecCredentials;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema ImageValidatingPolicySpec#evaluation
   */
  readonly evaluation?: ImageValidatingPolicySpecEvaluation;

  /**
   * FailurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * @schema ImageValidatingPolicySpec#failurePolicy
   */
  readonly failurePolicy?: ImageValidatingPolicySpecFailurePolicy;

  /**
   * ImageExtractors is a list of CEL expression to extract images from the resource
   *
   * @schema ImageValidatingPolicySpec#images
   */
  readonly images?: ImageValidatingPolicySpecImages[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * @schema ImageValidatingPolicySpec#matchConditions
   */
  readonly matchConditions?: ImageValidatingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   *
   * @schema ImageValidatingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: ImageValidatingPolicySpecMatchConstraints;

  /**
   * MatchImageReferences is a list of Glob and CELExpressions to match images.
   * Any image that matches one of the rules is considered for validation
   * Any image that does not match a rule is skipped, even when they are passed as arguments to
   * image verification functions
   *
   * @schema ImageValidatingPolicySpec#matchImageReferences
   */
  readonly matchImageReferences?: ImageValidatingPolicySpecMatchImageReferences[];

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema ImageValidatingPolicySpec#validationActions
   */
  readonly validationActions?: ImageValidatingPolicySpecValidationActions[];

  /**
   * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
   *
   * @schema ImageValidatingPolicySpec#validationConfigurations
   */
  readonly validationConfigurations?: ImageValidatingPolicySpecValidationConfigurations;

  /**
   * Validations contain CEL expressions which is used to apply the image validation checks.
   *
   * @schema ImageValidatingPolicySpec#validations
   */
  readonly validations: ImageValidatingPolicySpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   *
   * @schema ImageValidatingPolicySpec#variables
   */
  readonly variables?: ImageValidatingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema ImageValidatingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: ImageValidatingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpec(
  obj: ImageValidatingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    attestations: obj.attestations?.map((y) =>
      toJson_ImageValidatingPolicySpecAttestations(y),
    ),
    attestors: obj.attestors?.map((y) =>
      toJson_ImageValidatingPolicySpecAttestors(y),
    ),
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_ImageValidatingPolicySpecAuditAnnotations(y),
    ),
    autogen: toJson_ImageValidatingPolicySpecAutogen(obj.autogen),
    credentials: toJson_ImageValidatingPolicySpecCredentials(obj.credentials),
    evaluation: toJson_ImageValidatingPolicySpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    images: obj.images?.map((y) => toJson_ImageValidatingPolicySpecImages(y)),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_ImageValidatingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_ImageValidatingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    matchImageReferences: obj.matchImageReferences?.map((y) =>
      toJson_ImageValidatingPolicySpecMatchImageReferences(y),
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validationConfigurations:
      toJson_ImageValidatingPolicySpecValidationConfigurations(
        obj.validationConfigurations,
      ),
    validations: obj.validations?.map((y) =>
      toJson_ImageValidatingPolicySpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_ImageValidatingPolicySpecVariables(y),
    ),
    webhookConfiguration: toJson_ImageValidatingPolicySpecWebhookConfiguration(
      obj.webhookConfiguration,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestation defines the identification details of the  metadata that has to be verified
 *
 * @schema ImageValidatingPolicySpecAttestations
 */
export interface ImageValidatingPolicySpecAttestations {
  /**
   * InToto defines the details of attestation attached using intoto format
   *
   * @schema ImageValidatingPolicySpecAttestations#intoto
   */
  readonly intoto?: ImageValidatingPolicySpecAttestationsIntoto;

  /**
   * Name is the name for this attestation. It is used to refer to the attestation in verification
   *
   * @schema ImageValidatingPolicySpecAttestations#name
   */
  readonly name: string;

  /**
   * Referrer defines the details of attestation attached using OCI 1.1 format
   *
   * @schema ImageValidatingPolicySpecAttestations#referrer
   */
  readonly referrer?: ImageValidatingPolicySpecAttestationsReferrer;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestations(
  obj: ImageValidatingPolicySpecAttestations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    intoto: toJson_ImageValidatingPolicySpecAttestationsIntoto(obj.intoto),
    name: obj.name,
    referrer: toJson_ImageValidatingPolicySpecAttestationsReferrer(
      obj.referrer,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestor is an identity that confirms or verifies the authenticity of an image or an attestation
 *
 * @schema ImageValidatingPolicySpecAttestors
 */
export interface ImageValidatingPolicySpecAttestors {
  /**
   * Cosign defines attestor configuration for Cosign based signatures
   *
   * @schema ImageValidatingPolicySpecAttestors#cosign
   */
  readonly cosign?: ImageValidatingPolicySpecAttestorsCosign;

  /**
   * Name is the name for this attestor. It is used to refer to the attestor in verification
   *
   * @schema ImageValidatingPolicySpecAttestors#name
   */
  readonly name: string;

  /**
   * Notary defines attestor configuration for Notary based signatures
   *
   * @schema ImageValidatingPolicySpecAttestors#notary
   */
  readonly notary?: ImageValidatingPolicySpecAttestorsNotary;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestors' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestors(
  obj: ImageValidatingPolicySpecAttestors | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cosign: toJson_ImageValidatingPolicySpecAttestorsCosign(obj.cosign),
    name: obj.name,
    notary: toJson_ImageValidatingPolicySpecAttestorsNotary(obj.notary),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema ImageValidatingPolicySpecAuditAnnotations
 */
export interface ImageValidatingPolicySpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema ImageValidatingPolicySpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema ImageValidatingPolicySpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAuditAnnotations(
  obj: ImageValidatingPolicySpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema ImageValidatingPolicySpecAutogen
 */
export interface ImageValidatingPolicySpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema ImageValidatingPolicySpecAutogen#podControllers
   */
  readonly podControllers?: ImageValidatingPolicySpecAutogenPodControllers;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAutogen(
  obj: ImageValidatingPolicySpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers: toJson_ImageValidatingPolicySpecAutogenPodControllers(
      obj.podControllers,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Credentials provides credentials that will be used for authentication with registry.
 *
 * @schema ImageValidatingPolicySpecCredentials
 */
export interface ImageValidatingPolicySpecCredentials {
  /**
   * AllowInsecureRegistry allows insecure access to a registry.
   *
   * @schema ImageValidatingPolicySpecCredentials#allowInsecureRegistry
   */
  readonly allowInsecureRegistry?: boolean;

  /**
   * Providers specifies a list of OCI Registry names, whose authentication providers are provided.
   * It can be of one of these values: default,google,azure,amazon,github.
   *
   * @schema ImageValidatingPolicySpecCredentials#providers
   */
  readonly providers?: ImageValidatingPolicySpecCredentialsProviders[];

  /**
   * Secrets specifies a list of secrets that are provided for credentials.
   * Secrets must live in the Kyverno namespace.
   *
   * @schema ImageValidatingPolicySpecCredentials#secrets
   */
  readonly secrets?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecCredentials' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecCredentials(
  obj: ImageValidatingPolicySpecCredentials | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowInsecureRegistry: obj.allowInsecureRegistry,
    providers: obj.providers?.map((y) => y),
    secrets: obj.secrets?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema ImageValidatingPolicySpecEvaluation
 */
export interface ImageValidatingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema ImageValidatingPolicySpecEvaluation#admission
   */
  readonly admission?: ImageValidatingPolicySpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema ImageValidatingPolicySpecEvaluation#background
   */
  readonly background?: ImageValidatingPolicySpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema ImageValidatingPolicySpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecEvaluation(
  obj: ImageValidatingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_ImageValidatingPolicySpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_ImageValidatingPolicySpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * FailurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * @schema ImageValidatingPolicySpecFailurePolicy
 */
export enum ImageValidatingPolicySpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * @schema ImageValidatingPolicySpecImages
 */
export interface ImageValidatingPolicySpecImages {
  /**
   * Expression defines CEL expression to extract images from the resource.
   *
   * @schema ImageValidatingPolicySpecImages#expression
   */
  readonly expression: string;

  /**
   * Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
   *
   * @schema ImageValidatingPolicySpecImages#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecImages' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecImages(
  obj: ImageValidatingPolicySpecImages | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema ImageValidatingPolicySpecMatchConditions
 */
export interface ImageValidatingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConditions(
  obj: ImageValidatingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 *
 * @schema ImageValidatingPolicySpecMatchConstraints
 */
export interface ImageValidatingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema ImageValidatingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema ImageValidatingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ImageValidatingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: ImageValidatingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ImageValidatingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: ImageValidatingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema ImageValidatingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: ImageValidatingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraints(
  obj: ImageValidatingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_ImageValidatingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_ImageValidatingPolicySpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_ImageValidatingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchImageReference defines a Glob or a CEL expression for matching images
 *
 * @schema ImageValidatingPolicySpecMatchImageReferences
 */
export interface ImageValidatingPolicySpecMatchImageReferences {
  /**
   * Expression defines CEL Expressions for matching images
   *
   * @schema ImageValidatingPolicySpecMatchImageReferences#expression
   */
  readonly expression?: string;

  /**
   * Glob defines a globbing pattern for matching images
   *
   * @schema ImageValidatingPolicySpecMatchImageReferences#glob
   */
  readonly glob?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchImageReferences' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchImageReferences(
  obj: ImageValidatingPolicySpecMatchImageReferences | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    glob: obj.glob,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema ImageValidatingPolicySpecValidationActions
 */
export enum ImageValidatingPolicySpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
 *
 * @schema ImageValidatingPolicySpecValidationConfigurations
 */
export interface ImageValidatingPolicySpecValidationConfigurations {
  /**
   * MutateDigest enables replacement of image tags with digests.
   * Defaults to true.
   *
   * @default true.
   * @schema ImageValidatingPolicySpecValidationConfigurations#mutateDigest
   */
  readonly mutateDigest?: boolean;

  /**
   * Required validates that images are verified, i.e., have passed a signature or attestation check.
   *
   * @schema ImageValidatingPolicySpecValidationConfigurations#required
   */
  readonly required?: boolean;

  /**
   * VerifyDigest validates that images have a digest.
   *
   * @schema ImageValidatingPolicySpecValidationConfigurations#verifyDigest
   */
  readonly verifyDigest?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecValidationConfigurations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecValidationConfigurations(
  obj: ImageValidatingPolicySpecValidationConfigurations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutateDigest: obj.mutateDigest,
    required: obj.required,
    verifyDigest: obj.verifyDigest,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema ImageValidatingPolicySpecValidations
 */
export interface ImageValidatingPolicySpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema ImageValidatingPolicySpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema ImageValidatingPolicySpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema ImageValidatingPolicySpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema ImageValidatingPolicySpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecValidations(
  obj: ImageValidatingPolicySpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema ImageValidatingPolicySpecVariables
 */
export interface ImageValidatingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema ImageValidatingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema ImageValidatingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecVariables(
  obj: ImageValidatingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema ImageValidatingPolicySpecWebhookConfiguration
 */
export interface ImageValidatingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema ImageValidatingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecWebhookConfiguration(
  obj: ImageValidatingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * InToto defines the details of attestation attached using intoto format
 *
 * @schema ImageValidatingPolicySpecAttestationsIntoto
 */
export interface ImageValidatingPolicySpecAttestationsIntoto {
  /**
   * Type defines the type of attestation contained within the statement.
   *
   * @schema ImageValidatingPolicySpecAttestationsIntoto#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestationsIntoto' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestationsIntoto(
  obj: ImageValidatingPolicySpecAttestationsIntoto | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Referrer defines the details of attestation attached using OCI 1.1 format
 *
 * @schema ImageValidatingPolicySpecAttestationsReferrer
 */
export interface ImageValidatingPolicySpecAttestationsReferrer {
  /**
   * Type defines the type of attestation attached to the image.
   *
   * @schema ImageValidatingPolicySpecAttestationsReferrer#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestationsReferrer' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestationsReferrer(
  obj: ImageValidatingPolicySpecAttestationsReferrer | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Cosign defines attestor configuration for Cosign based signatures
 *
 * @schema ImageValidatingPolicySpecAttestorsCosign
 */
export interface ImageValidatingPolicySpecAttestorsCosign {
  /**
   * Annotations are used for image verification.
   * Every specified key-value pair must exist and match in the verified payload.
   * The payload may contain other key-value pairs.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#annotations
   */
  readonly annotations?: { [key: string]: string };

  /**
   * Certificate defines the configuration for local signature verification
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#certificate
   */
  readonly certificate?: ImageValidatingPolicySpecAttestorsCosignCertificate;

  /**
   * CTLog sets the configuration to verify the authority against a Rekor instance.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#ctlog
   */
  readonly ctlog?: ImageValidatingPolicySpecAttestorsCosignCtlog;

  /**
   * Key defines the type of key to validate the image.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#key
   */
  readonly key?: ImageValidatingPolicySpecAttestorsCosignKey;

  /**
   * Keyless sets the configuration to verify the authority against a Fulcio instance.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#keyless
   */
  readonly keyless?: ImageValidatingPolicySpecAttestorsCosignKeyless;

  /**
   * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#source
   */
  readonly source?: ImageValidatingPolicySpecAttestorsCosignSource;

  /**
   * TUF defines the configuration to fetch sigstore root
   *
   * @schema ImageValidatingPolicySpecAttestorsCosign#tuf
   */
  readonly tuf?: ImageValidatingPolicySpecAttestorsCosignTuf;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosign' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosign(
  obj: ImageValidatingPolicySpecAttestorsCosign | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    annotations:
      obj.annotations === undefined
        ? undefined
        : Object.entries(obj.annotations).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
    certificate: toJson_ImageValidatingPolicySpecAttestorsCosignCertificate(
      obj.certificate,
    ),
    ctlog: toJson_ImageValidatingPolicySpecAttestorsCosignCtlog(obj.ctlog),
    key: toJson_ImageValidatingPolicySpecAttestorsCosignKey(obj.key),
    keyless: toJson_ImageValidatingPolicySpecAttestorsCosignKeyless(
      obj.keyless,
    ),
    source: toJson_ImageValidatingPolicySpecAttestorsCosignSource(obj.source),
    tuf: toJson_ImageValidatingPolicySpecAttestorsCosignTuf(obj.tuf),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Notary defines attestor configuration for Notary based signatures
 *
 * @schema ImageValidatingPolicySpecAttestorsNotary
 */
export interface ImageValidatingPolicySpecAttestorsNotary {
  /**
   * Certs define the cert chain for Notary signature verification
   *
   * @schema ImageValidatingPolicySpecAttestorsNotary#certs
   */
  readonly certs?: ImageValidatingPolicySpecAttestorsNotaryCerts;

  /**
   * TSACerts define the cert chain for verifying timestamps of notary signature
   *
   * @schema ImageValidatingPolicySpecAttestorsNotary#tsaCerts
   */
  readonly tsaCerts?: ImageValidatingPolicySpecAttestorsNotaryTsaCerts;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsNotary' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsNotary(
  obj: ImageValidatingPolicySpecAttestorsNotary | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    certs: toJson_ImageValidatingPolicySpecAttestorsNotaryCerts(obj.certs),
    tsaCerts: toJson_ImageValidatingPolicySpecAttestorsNotaryTsaCerts(
      obj.tsaCerts,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema ImageValidatingPolicySpecAutogenPodControllers
 */
export interface ImageValidatingPolicySpecAutogenPodControllers {
  /**
   * @schema ImageValidatingPolicySpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAutogenPodControllers(
  obj: ImageValidatingPolicySpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CredentialsProvidersType provides the list of credential providers required.
 *
 * @schema ImageValidatingPolicySpecCredentialsProviders
 */
export enum ImageValidatingPolicySpecCredentialsProviders {
  /** default */
  DEFAULT = "default",
  /** amazon */
  AMAZON = "amazon",
  /** azure */
  AZURE = "azure",
  /** google */
  GOOGLE = "google",
  /** github */
  GITHUB = "github",
}

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema ImageValidatingPolicySpecEvaluationAdmission
 */
export interface ImageValidatingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema ImageValidatingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecEvaluationAdmission(
  obj: ImageValidatingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema ImageValidatingPolicySpecEvaluationBackground
 */
export interface ImageValidatingPolicySpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema ImageValidatingPolicySpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecEvaluationBackground(
  obj: ImageValidatingPolicySpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules(
  obj:
    | ImageValidatingPolicySpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface ImageValidatingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraintsNamespaceSelector(
  obj: ImageValidatingPolicySpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelector
 */
export interface ImageValidatingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraintsObjectSelector(
  obj: ImageValidatingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules
 */
export interface ImageValidatingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ImageValidatingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraintsResourceRules(
  obj: ImageValidatingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate defines the configuration for local signature verification
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignCertificate
 */
export interface ImageValidatingPolicySpecAttestorsCosignCertificate {
  /**
   * Certificate is the to the public certificate for local signature verification.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCertificate#cert
   */
  readonly cert?: ImageValidatingPolicySpecAttestorsCosignCertificateCert;

  /**
   * CertificateChain is the list of CA certificates in PEM format which will be needed
   * when building the certificate chain for the signing certificate. Must start with the
   * parent intermediate CA certificate of the signing certificate and end with the root certificate
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCertificate#certChain
   */
  readonly certChain?: ImageValidatingPolicySpecAttestorsCosignCertificateCertChain;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignCertificate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignCertificate(
  obj: ImageValidatingPolicySpecAttestorsCosignCertificate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cert: toJson_ImageValidatingPolicySpecAttestorsCosignCertificateCert(
      obj.cert,
    ),
    certChain:
      toJson_ImageValidatingPolicySpecAttestorsCosignCertificateCertChain(
        obj.certChain,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CTLog sets the configuration to verify the authority against a Rekor instance.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignCtlog
 */
export interface ImageValidatingPolicySpecAttestorsCosignCtlog {
  /**
   * CTLogPubKey, if set, is used to validate SCTs against a custom source.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCtlog#ctLogPubKey
   */
  readonly ctLogPubKey?: string;

  /**
   * IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate
   * timestamp. Default is false. Set to true if this was opted out during signing.
   *
   * @default false. Set to true if this was opted out during signing.
   * @schema ImageValidatingPolicySpecAttestorsCosignCtlog#insecureIgnoreSCT
   */
  readonly insecureIgnoreSct?: boolean;

  /**
   * InsecureIgnoreTlog skips transparency log verification.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCtlog#insecureIgnoreTlog
   */
  readonly insecureIgnoreTlog?: boolean;

  /**
   * RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor.
   * If set, this will be used to validate transparency log signatures from a custom Rekor.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCtlog#rekorPubKey
   */
  readonly rekorPubKey?: string;

  /**
   * TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must
   * contain the root CA certificate. Optionally may contain intermediate CA certificates, and
   * may contain the leaf TSA certificate if not present in the timestamurce.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCtlog#tsaCertChain
   */
  readonly tsaCertChain?: string;

  /**
   * URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCtlog#url
   */
  readonly url?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignCtlog' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignCtlog(
  obj: ImageValidatingPolicySpecAttestorsCosignCtlog | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    ctLogPubKey: obj.ctLogPubKey,
    insecureIgnoreSCT: obj.insecureIgnoreSct,
    insecureIgnoreTlog: obj.insecureIgnoreTlog,
    rekorPubKey: obj.rekorPubKey,
    tsaCertChain: obj.tsaCertChain,
    url: obj.url,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Key defines the type of key to validate the image.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignKey
 */
export interface ImageValidatingPolicySpecAttestorsCosignKey {
  /**
   * Data contains the inline public key
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKey#data
   */
  readonly data?: string;

  /**
   * Expression is a Expression expression that returns the public key.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKey#expression
   */
  readonly expression?: string;

  /**
   * HashAlgorithm specifues signature algorithm for public keys. Supported values are
   * sha224, sha256, sha384 and sha512. Defaults to sha256.
   *
   * @default sha256.
   * @schema ImageValidatingPolicySpecAttestorsCosignKey#hashAlgorithm
   */
  readonly hashAlgorithm?: string;

  /**
   * KMS contains the KMS url of the public key
   * Supported formats differ based on the KMS system used.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKey#kms
   */
  readonly kms?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignKey' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignKey(
  obj: ImageValidatingPolicySpecAttestorsCosignKey | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    expression: obj.expression,
    hashAlgorithm: obj.hashAlgorithm,
    kms: obj.kms,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Keyless sets the configuration to verify the authority against a Fulcio instance.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignKeyless
 */
export interface ImageValidatingPolicySpecAttestorsCosignKeyless {
  /**
   * Identities sets a list of identities.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKeyless#identities
   */
  readonly identities: ImageValidatingPolicySpecAttestorsCosignKeylessIdentities[];

  /**
   * Roots is an optional set of PEM encoded trusted root certificates.
   * If not provided, the system roots are used.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKeyless#roots
   */
  readonly roots?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignKeyless' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignKeyless(
  obj: ImageValidatingPolicySpecAttestorsCosignKeyless | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    identities: obj.identities?.map((y) =>
      toJson_ImageValidatingPolicySpecAttestorsCosignKeylessIdentities(y),
    ),
    roots: obj.roots,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignSource
 */
export interface ImageValidatingPolicySpecAttestorsCosignSource {
  /**
   * SignaturePullSecrets is an optional list of references to secrets in the
   * same namespace as the deploying resource for pulling any of the signatures
   * used by this Source.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignSource#PullSecrets
   */
  readonly pullSecrets?: ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets[];

  /**
   * Repository defines the location from where to pull the signature / attestations.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignSource#repository
   */
  readonly repository?: string;

  /**
   * TagPrefix is an optional prefix that signature and attestations have.
   * This is the 'tag based discovery' and in the future once references are
   * fully supported that should likely be the preferred way to handle these.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignSource#tagPrefix
   */
  readonly tagPrefix?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignSource' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignSource(
  obj: ImageValidatingPolicySpecAttestorsCosignSource | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    PullSecrets: obj.pullSecrets?.map((y) =>
      toJson_ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets(y),
    ),
    repository: obj.repository,
    tagPrefix: obj.tagPrefix,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TUF defines the configuration to fetch sigstore root
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignTuf
 */
export interface ImageValidatingPolicySpecAttestorsCosignTuf {
  /**
   * Mirror is the base URL of Sigstore TUF repository
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignTuf#mirror
   */
  readonly mirror?: string;

  /**
   * Root defines the path or data of the trusted root
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignTuf#root
   */
  readonly root?: ImageValidatingPolicySpecAttestorsCosignTufRoot;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignTuf' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignTuf(
  obj: ImageValidatingPolicySpecAttestorsCosignTuf | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mirror: obj.mirror,
    root: toJson_ImageValidatingPolicySpecAttestorsCosignTufRoot(obj.root),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certs define the cert chain for Notary signature verification
 *
 * @schema ImageValidatingPolicySpecAttestorsNotaryCerts
 */
export interface ImageValidatingPolicySpecAttestorsNotaryCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicySpecAttestorsNotaryCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicySpecAttestorsNotaryCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsNotaryCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsNotaryCerts(
  obj: ImageValidatingPolicySpecAttestorsNotaryCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TSACerts define the cert chain for verifying timestamps of notary signature
 *
 * @schema ImageValidatingPolicySpecAttestorsNotaryTsaCerts
 */
export interface ImageValidatingPolicySpecAttestorsNotaryTsaCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicySpecAttestorsNotaryTsaCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicySpecAttestorsNotaryTsaCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsNotaryTsaCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsNotaryTsaCerts(
  obj: ImageValidatingPolicySpecAttestorsNotaryTsaCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | ImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | ImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate is the to the public certificate for local signature verification.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignCertificateCert
 */
export interface ImageValidatingPolicySpecAttestorsCosignCertificateCert {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCertificateCert#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCertificateCert#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignCertificateCert' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignCertificateCert(
  obj: ImageValidatingPolicySpecAttestorsCosignCertificateCert | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CertificateChain is the list of CA certificates in PEM format which will be needed
 * when building the certificate chain for the signing certificate. Must start with the
 * parent intermediate CA certificate of the signing certificate and end with the root certificate
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignCertificateCertChain
 */
export interface ImageValidatingPolicySpecAttestorsCosignCertificateCertChain {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCertificateCertChain#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignCertificateCertChain#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignCertificateCertChain' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignCertificateCertChain(
  obj: ImageValidatingPolicySpecAttestorsCosignCertificateCertChain | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Identity may contain the issuer and/or the subject found in the transparency
 * log.
 * Issuer/Subject uses a strict match, while IssuerRegExp and SubjectRegExp
 * apply a regexp for matching.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignKeylessIdentities
 */
export interface ImageValidatingPolicySpecAttestorsCosignKeylessIdentities {
  /**
   * Issuer defines the issuer for this identity.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKeylessIdentities#issuer
   */
  readonly issuer?: string;

  /**
   * IssuerRegExp specifies a regular expression to match the issuer for this identity.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKeylessIdentities#issuerRegExp
   */
  readonly issuerRegExp?: string;

  /**
   * Subject defines the subject for this identity.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKeylessIdentities#subject
   */
  readonly subject?: string;

  /**
   * SubjectRegExp specifies a regular expression to match the subject for this identity.
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignKeylessIdentities#subjectRegExp
   */
  readonly subjectRegExp?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignKeylessIdentities' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignKeylessIdentities(
  obj: ImageValidatingPolicySpecAttestorsCosignKeylessIdentities | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    issuer: obj.issuer,
    issuerRegExp: obj.issuerRegExp,
    subject: obj.subject,
    subjectRegExp: obj.subjectRegExp,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * LocalObjectReference contains enough information to let you locate the
 * referenced object inside the same namespace.
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets
 */
export interface ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets {
  /**
   * Name of the referent.
   * This field is effectively required, but due to backwards compatibility is
   * allowed to be empty. Instances of this type with an empty value here are
   * almost certainly wrong.
   * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets#name
   */
  readonly name?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets(
  obj: ImageValidatingPolicySpecAttestorsCosignSourcePullSecrets | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Root defines the path or data of the trusted root
 *
 * @schema ImageValidatingPolicySpecAttestorsCosignTufRoot
 */
export interface ImageValidatingPolicySpecAttestorsCosignTufRoot {
  /**
   * Data is the base64 encoded TUF root
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignTufRoot#data
   */
  readonly data?: string;

  /**
   * Path is the URL or File location of the TUF root
   *
   * @schema ImageValidatingPolicySpecAttestorsCosignTufRoot#path
   */
  readonly path?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicySpecAttestorsCosignTufRoot' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicySpecAttestorsCosignTufRoot(
  obj: ImageValidatingPolicySpecAttestorsCosignTufRoot | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    path: obj.path,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema ImageValidatingPolicyV1Alpha1
 */
export class ImageValidatingPolicyV1Alpha1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "ImageValidatingPolicyV1Alpha1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1alpha1",
    kind: "ImageValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "ImageValidatingPolicyV1Alpha1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: ImageValidatingPolicyV1Alpha1Props): any {
    return {
      ...ImageValidatingPolicyV1Alpha1.GVK,
      ...toJson_ImageValidatingPolicyV1Alpha1Props(props),
    };
  }

  /**
   * Defines a "ImageValidatingPolicyV1Alpha1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: ImageValidatingPolicyV1Alpha1Props,
  ) {
    super(scope, id, {
      ...ImageValidatingPolicyV1Alpha1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...ImageValidatingPolicyV1Alpha1.GVK,
      ...toJson_ImageValidatingPolicyV1Alpha1Props(resolved),
    };
  }
}

/**
 * @schema ImageValidatingPolicyV1Alpha1
 */
export interface ImageValidatingPolicyV1Alpha1Props {
  /**
   * @schema ImageValidatingPolicyV1Alpha1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
   *
   * @schema ImageValidatingPolicyV1Alpha1#spec
   */
  readonly spec: ImageValidatingPolicyV1Alpha1Spec;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1Props(
  obj: ImageValidatingPolicyV1Alpha1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_ImageValidatingPolicyV1Alpha1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
 *
 * @schema ImageValidatingPolicyV1Alpha1Spec
 */
export interface ImageValidatingPolicyV1Alpha1Spec {
  /**
   * Attestations provides a list of image metadata to verify
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#attestations
   */
  readonly attestations?: ImageValidatingPolicyV1Alpha1SpecAttestations[];

  /**
   * Attestors provides a list of trusted authorities.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#attestors
   */
  readonly attestors: ImageValidatingPolicyV1Alpha1SpecAttestors[];

  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#auditAnnotations
   */
  readonly auditAnnotations?: ImageValidatingPolicyV1Alpha1SpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#autogen
   */
  readonly autogen?: ImageValidatingPolicyV1Alpha1SpecAutogen;

  /**
   * Credentials provides credentials that will be used for authentication with registry.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#credentials
   */
  readonly credentials?: ImageValidatingPolicyV1Alpha1SpecCredentials;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#evaluation
   */
  readonly evaluation?: ImageValidatingPolicyV1Alpha1SpecEvaluation;

  /**
   * FailurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#failurePolicy
   */
  readonly failurePolicy?: ImageValidatingPolicyV1Alpha1SpecFailurePolicy;

  /**
   * ImageExtractors is a list of CEL expression to extract images from the resource
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#images
   */
  readonly images?: ImageValidatingPolicyV1Alpha1SpecImages[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#matchConditions
   */
  readonly matchConditions?: ImageValidatingPolicyV1Alpha1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#matchConstraints
   */
  readonly matchConstraints?: ImageValidatingPolicyV1Alpha1SpecMatchConstraints;

  /**
   * MatchImageReferences is a list of Glob and CELExpressions to match images.
   * Any image that matches one of the rules is considered for validation
   * Any image that does not match a rule is skipped, even when they are passed as arguments to
   * image verification functions
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#matchImageReferences
   */
  readonly matchImageReferences?: ImageValidatingPolicyV1Alpha1SpecMatchImageReferences[];

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#validationActions
   */
  readonly validationActions?: ImageValidatingPolicyV1Alpha1SpecValidationActions[];

  /**
   * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#validationConfigurations
   */
  readonly validationConfigurations?: ImageValidatingPolicyV1Alpha1SpecValidationConfigurations;

  /**
   * Validations contain CEL expressions which is used to apply the image validation checks.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#validations
   */
  readonly validations: ImageValidatingPolicyV1Alpha1SpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#variables
   */
  readonly variables?: ImageValidatingPolicyV1Alpha1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema ImageValidatingPolicyV1Alpha1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1Spec(
  obj: ImageValidatingPolicyV1Alpha1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    attestations: obj.attestations?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecAttestations(y),
    ),
    attestors: obj.attestors?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecAttestors(y),
    ),
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecAuditAnnotations(y),
    ),
    autogen: toJson_ImageValidatingPolicyV1Alpha1SpecAutogen(obj.autogen),
    credentials: toJson_ImageValidatingPolicyV1Alpha1SpecCredentials(
      obj.credentials,
    ),
    evaluation: toJson_ImageValidatingPolicyV1Alpha1SpecEvaluation(
      obj.evaluation,
    ),
    failurePolicy: obj.failurePolicy,
    images: obj.images?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecImages(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    matchImageReferences: obj.matchImageReferences?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchImageReferences(y),
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validationConfigurations:
      toJson_ImageValidatingPolicyV1Alpha1SpecValidationConfigurations(
        obj.validationConfigurations,
      ),
    validations: obj.validations?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestation defines the identification details of the  metadata that has to be verified
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestations
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestations {
  /**
   * InToto defines the details of attestation attached using intoto format
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestations#intoto
   */
  readonly intoto?: ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto;

  /**
   * Name is the name for this attestation. It is used to refer to the attestation in verification
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestations#name
   */
  readonly name: string;

  /**
   * Referrer defines the details of attestation attached using OCI 1.1 format
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestations#referrer
   */
  readonly referrer?: ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestations(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    intoto: toJson_ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto(
      obj.intoto,
    ),
    name: obj.name,
    referrer: toJson_ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer(
      obj.referrer,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestor is an identity that confirms or verifies the authenticity of an image or an attestation
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestors
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestors {
  /**
   * Cosign defines attestor configuration for Cosign based signatures
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestors#cosign
   */
  readonly cosign?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosign;

  /**
   * Name is the name for this attestor. It is used to refer to the attestor in verification
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestors#name
   */
  readonly name: string;

  /**
   * Notary defines attestor configuration for Notary based signatures
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestors#notary
   */
  readonly notary?: ImageValidatingPolicyV1Alpha1SpecAttestorsNotary;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestors' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestors(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestors | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cosign: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosign(obj.cosign),
    name: obj.name,
    notary: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsNotary(obj.notary),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAuditAnnotations
 */
export interface ImageValidatingPolicyV1Alpha1SpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAuditAnnotations(
  obj: ImageValidatingPolicyV1Alpha1SpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAutogen
 */
export interface ImageValidatingPolicyV1Alpha1SpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAutogen#podControllers
   */
  readonly podControllers?: ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAutogen(
  obj: ImageValidatingPolicyV1Alpha1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers:
      toJson_ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers(
        obj.podControllers,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Credentials provides credentials that will be used for authentication with registry.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecCredentials
 */
export interface ImageValidatingPolicyV1Alpha1SpecCredentials {
  /**
   * AllowInsecureRegistry allows insecure access to a registry.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecCredentials#allowInsecureRegistry
   */
  readonly allowInsecureRegistry?: boolean;

  /**
   * Providers specifies a list of OCI Registry names, whose authentication providers are provided.
   * It can be of one of these values: default,google,azure,amazon,github.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecCredentials#providers
   */
  readonly providers?: ImageValidatingPolicyV1Alpha1SpecCredentialsProviders[];

  /**
   * Secrets specifies a list of secrets that are provided for credentials.
   * Secrets must live in the Kyverno namespace.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecCredentials#secrets
   */
  readonly secrets?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecCredentials' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecCredentials(
  obj: ImageValidatingPolicyV1Alpha1SpecCredentials | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowInsecureRegistry: obj.allowInsecureRegistry,
    providers: obj.providers?.map((y) => y),
    secrets: obj.secrets?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecEvaluation
 */
export interface ImageValidatingPolicyV1Alpha1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecEvaluation#admission
   */
  readonly admission?: ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecEvaluation#background
   */
  readonly background?: ImageValidatingPolicyV1Alpha1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecEvaluation(
  obj: ImageValidatingPolicyV1Alpha1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_ImageValidatingPolicyV1Alpha1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * FailurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecFailurePolicy
 */
export enum ImageValidatingPolicyV1Alpha1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * @schema ImageValidatingPolicyV1Alpha1SpecImages
 */
export interface ImageValidatingPolicyV1Alpha1SpecImages {
  /**
   * Expression defines CEL expression to extract images from the resource.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecImages#expression
   */
  readonly expression: string;

  /**
   * Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecImages#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecImages' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecImages(
  obj: ImageValidatingPolicyV1Alpha1SpecImages | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConditions
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConditions(
  obj: ImageValidatingPolicyV1Alpha1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraints
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraints(
  obj: ImageValidatingPolicyV1Alpha1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchImageReference defines a Glob or a CEL expression for matching images
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchImageReferences
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchImageReferences {
  /**
   * Expression defines CEL Expressions for matching images
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchImageReferences#expression
   */
  readonly expression?: string;

  /**
   * Glob defines a globbing pattern for matching images
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchImageReferences#glob
   */
  readonly glob?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchImageReferences' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchImageReferences(
  obj: ImageValidatingPolicyV1Alpha1SpecMatchImageReferences | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    glob: obj.glob,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecValidationActions
 */
export enum ImageValidatingPolicyV1Alpha1SpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecValidationConfigurations
 */
export interface ImageValidatingPolicyV1Alpha1SpecValidationConfigurations {
  /**
   * MutateDigest enables replacement of image tags with digests.
   * Defaults to true.
   *
   * @default true.
   * @schema ImageValidatingPolicyV1Alpha1SpecValidationConfigurations#mutateDigest
   */
  readonly mutateDigest?: boolean;

  /**
   * Required validates that images are verified, i.e., have passed a signature or attestation check.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecValidationConfigurations#required
   */
  readonly required?: boolean;

  /**
   * VerifyDigest validates that images have a digest.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecValidationConfigurations#verifyDigest
   */
  readonly verifyDigest?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecValidationConfigurations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecValidationConfigurations(
  obj: ImageValidatingPolicyV1Alpha1SpecValidationConfigurations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutateDigest: obj.mutateDigest,
    required: obj.required,
    verifyDigest: obj.verifyDigest,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecValidations
 */
export interface ImageValidatingPolicyV1Alpha1SpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecValidations(
  obj: ImageValidatingPolicyV1Alpha1SpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecVariables
 */
export interface ImageValidatingPolicyV1Alpha1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecVariables(
  obj: ImageValidatingPolicyV1Alpha1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration
 */
export interface ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration(
  obj: ImageValidatingPolicyV1Alpha1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * InToto defines the details of attestation attached using intoto format
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto {
  /**
   * Type defines the type of attestation contained within the statement.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestationsIntoto | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Referrer defines the details of attestation attached using OCI 1.1 format
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer {
  /**
   * Type defines the type of attestation attached to the image.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestationsReferrer | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Cosign defines attestor configuration for Cosign based signatures
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosign {
  /**
   * Annotations are used for image verification.
   * Every specified key-value pair must exist and match in the verified payload.
   * The payload may contain other key-value pairs.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#annotations
   */
  readonly annotations?: { [key: string]: string };

  /**
   * Certificate defines the configuration for local signature verification
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#certificate
   */
  readonly certificate?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate;

  /**
   * CTLog sets the configuration to verify the authority against a Rekor instance.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#ctlog
   */
  readonly ctlog?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog;

  /**
   * Key defines the type of key to validate the image.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#key
   */
  readonly key?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey;

  /**
   * Keyless sets the configuration to verify the authority against a Fulcio instance.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#keyless
   */
  readonly keyless?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless;

  /**
   * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#source
   */
  readonly source?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource;

  /**
   * TUF defines the configuration to fetch sigstore root
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosign#tuf
   */
  readonly tuf?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosign' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosign(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosign | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    annotations:
      obj.annotations === undefined
        ? undefined
        : Object.entries(obj.annotations).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
    certificate:
      toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate(
        obj.certificate,
      ),
    ctlog: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog(
      obj.ctlog,
    ),
    key: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey(obj.key),
    keyless: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless(
      obj.keyless,
    ),
    source: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource(
      obj.source,
    ),
    tuf: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf(obj.tuf),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Notary defines attestor configuration for Notary based signatures
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotary
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsNotary {
  /**
   * Certs define the cert chain for Notary signature verification
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotary#certs
   */
  readonly certs?: ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts;

  /**
   * TSACerts define the cert chain for verifying timestamps of notary signature
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotary#tsaCerts
   */
  readonly tsaCerts?: ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsNotary' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsNotary(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsNotary | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    certs: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts(
      obj.certs,
    ),
    tsaCerts: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts(
      obj.tsaCerts,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers
 */
export interface ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers {
  /**
   * @schema ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers(
  obj: ImageValidatingPolicyV1Alpha1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CredentialsProvidersType provides the list of credential providers required.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecCredentialsProviders
 */
export enum ImageValidatingPolicyV1Alpha1SpecCredentialsProviders {
  /** default */
  DEFAULT = "default",
  /** amazon */
  AMAZON = "amazon",
  /** azure */
  AZURE = "azure",
  /** google */
  GOOGLE = "google",
  /** github */
  GITHUB = "github",
}

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission
 */
export interface ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission(
  obj: ImageValidatingPolicyV1Alpha1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecEvaluationBackground
 */
export interface ImageValidatingPolicyV1Alpha1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecEvaluationBackground(
  obj: ImageValidatingPolicyV1Alpha1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate defines the configuration for local signature verification
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate {
  /**
   * Certificate is the to the public certificate for local signature verification.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate#cert
   */
  readonly cert?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert;

  /**
   * CertificateChain is the list of CA certificates in PEM format which will be needed
   * when building the certificate chain for the signing certificate. Must start with the
   * parent intermediate CA certificate of the signing certificate and end with the root certificate
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate#certChain
   */
  readonly certChain?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cert: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert(
      obj.cert,
    ),
    certChain:
      toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain(
        obj.certChain,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CTLog sets the configuration to verify the authority against a Rekor instance.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog {
  /**
   * CTLogPubKey, if set, is used to validate SCTs against a custom source.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog#ctLogPubKey
   */
  readonly ctLogPubKey?: string;

  /**
   * IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate
   * timestamp. Default is false. Set to true if this was opted out during signing.
   *
   * @default false. Set to true if this was opted out during signing.
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog#insecureIgnoreSCT
   */
  readonly insecureIgnoreSct?: boolean;

  /**
   * InsecureIgnoreTlog skips transparency log verification.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog#insecureIgnoreTlog
   */
  readonly insecureIgnoreTlog?: boolean;

  /**
   * RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor.
   * If set, this will be used to validate transparency log signatures from a custom Rekor.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog#rekorPubKey
   */
  readonly rekorPubKey?: string;

  /**
   * TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must
   * contain the root CA certificate. Optionally may contain intermediate CA certificates, and
   * may contain the leaf TSA certificate if not present in the timestamurce.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog#tsaCertChain
   */
  readonly tsaCertChain?: string;

  /**
   * URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog#url
   */
  readonly url?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCtlog | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    ctLogPubKey: obj.ctLogPubKey,
    insecureIgnoreSCT: obj.insecureIgnoreSct,
    insecureIgnoreTlog: obj.insecureIgnoreTlog,
    rekorPubKey: obj.rekorPubKey,
    tsaCertChain: obj.tsaCertChain,
    url: obj.url,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Key defines the type of key to validate the image.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey {
  /**
   * Data contains the inline public key
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey#data
   */
  readonly data?: string;

  /**
   * Expression is a Expression expression that returns the public key.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey#expression
   */
  readonly expression?: string;

  /**
   * HashAlgorithm specifues signature algorithm for public keys. Supported values are
   * sha224, sha256, sha384 and sha512. Defaults to sha256.
   *
   * @default sha256.
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey#hashAlgorithm
   */
  readonly hashAlgorithm?: string;

  /**
   * KMS contains the KMS url of the public key
   * Supported formats differ based on the KMS system used.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey#kms
   */
  readonly kms?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKey | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    expression: obj.expression,
    hashAlgorithm: obj.hashAlgorithm,
    kms: obj.kms,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Keyless sets the configuration to verify the authority against a Fulcio instance.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless {
  /**
   * Identities sets a list of identities.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless#identities
   */
  readonly identities: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities[];

  /**
   * Roots is an optional set of PEM encoded trusted root certificates.
   * If not provided, the system roots are used.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless#roots
   */
  readonly roots?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeyless | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    identities: obj.identities?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities(
        y,
      ),
    ),
    roots: obj.roots,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource {
  /**
   * SignaturePullSecrets is an optional list of references to secrets in the
   * same namespace as the deploying resource for pulling any of the signatures
   * used by this Source.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource#PullSecrets
   */
  readonly pullSecrets?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets[];

  /**
   * Repository defines the location from where to pull the signature / attestations.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource#repository
   */
  readonly repository?: string;

  /**
   * TagPrefix is an optional prefix that signature and attestations have.
   * This is the 'tag based discovery' and in the future once references are
   * fully supported that should likely be the preferred way to handle these.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource#tagPrefix
   */
  readonly tagPrefix?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSource | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    PullSecrets: obj.pullSecrets?.map((y) =>
      toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets(
        y,
      ),
    ),
    repository: obj.repository,
    tagPrefix: obj.tagPrefix,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TUF defines the configuration to fetch sigstore root
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf {
  /**
   * Mirror is the base URL of Sigstore TUF repository
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf#mirror
   */
  readonly mirror?: string;

  /**
   * Root defines the path or data of the trusted root
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf#root
   */
  readonly root?: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTuf | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mirror: obj.mirror,
    root: toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot(
      obj.root,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certs define the cert chain for Notary signature verification
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TSACerts define the cert chain for verifying timestamps of notary signature
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsNotaryTsaCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate is the to the public certificate for local signature verification.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCert
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CertificateChain is the list of CA certificates in PEM format which will be needed
 * when building the certificate chain for the signing certificate. Must start with the
 * parent intermediate CA certificate of the signing certificate and end with the root certificate
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecAttestorsCosignCertificateCertChain
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Identity may contain the issuer and/or the subject found in the transparency
 * log.
 * Issuer/Subject uses a strict match, while IssuerRegExp and SubjectRegExp
 * apply a regexp for matching.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities {
  /**
   * Issuer defines the issuer for this identity.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities#issuer
   */
  readonly issuer?: string;

  /**
   * IssuerRegExp specifies a regular expression to match the issuer for this identity.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities#issuerRegExp
   */
  readonly issuerRegExp?: string;

  /**
   * Subject defines the subject for this identity.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities#subject
   */
  readonly subject?: string;

  /**
   * SubjectRegExp specifies a regular expression to match the subject for this identity.
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities#subjectRegExp
   */
  readonly subjectRegExp?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecAttestorsCosignKeylessIdentities
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    issuer: obj.issuer,
    issuerRegExp: obj.issuerRegExp,
    subject: obj.subject,
    subjectRegExp: obj.subjectRegExp,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * LocalObjectReference contains enough information to let you locate the
 * referenced object inside the same namespace.
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets {
  /**
   * Name of the referent.
   * This field is effectively required, but due to backwards compatibility is
   * allowed to be empty. Instances of this type with an empty value here are
   * almost certainly wrong.
   * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets#name
   */
  readonly name?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets(
  obj:
    | ImageValidatingPolicyV1Alpha1SpecAttestorsCosignSourcePullSecrets
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Root defines the path or data of the trusted root
 *
 * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot
 */
export interface ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot {
  /**
   * Data is the base64 encoded TUF root
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot#data
   */
  readonly data?: string;

  /**
   * Path is the URL or File location of the TUF root
   *
   * @schema ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot#path
   */
  readonly path?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot(
  obj: ImageValidatingPolicyV1Alpha1SpecAttestorsCosignTufRoot | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    path: obj.path,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema ImageValidatingPolicyV1Beta1
 */
export class ImageValidatingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "ImageValidatingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "ImageValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "ImageValidatingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: ImageValidatingPolicyV1Beta1Props): any {
    return {
      ...ImageValidatingPolicyV1Beta1.GVK,
      ...toJson_ImageValidatingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "ImageValidatingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: ImageValidatingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...ImageValidatingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...ImageValidatingPolicyV1Beta1.GVK,
      ...toJson_ImageValidatingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema ImageValidatingPolicyV1Beta1
 */
export interface ImageValidatingPolicyV1Beta1Props {
  /**
   * @schema ImageValidatingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
   *
   * @schema ImageValidatingPolicyV1Beta1#spec
   */
  readonly spec: ImageValidatingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1Props(
  obj: ImageValidatingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_ImageValidatingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
 *
 * @schema ImageValidatingPolicyV1Beta1Spec
 */
export interface ImageValidatingPolicyV1Beta1Spec {
  /**
   * Attestations provides a list of image metadata to verify
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#attestations
   */
  readonly attestations?: ImageValidatingPolicyV1Beta1SpecAttestations[];

  /**
   * Attestors provides a list of trusted authorities.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#attestors
   */
  readonly attestors: ImageValidatingPolicyV1Beta1SpecAttestors[];

  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#auditAnnotations
   */
  readonly auditAnnotations?: ImageValidatingPolicyV1Beta1SpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#autogen
   */
  readonly autogen?: ImageValidatingPolicyV1Beta1SpecAutogen;

  /**
   * Credentials provides credentials that will be used for authentication with registry.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#credentials
   */
  readonly credentials?: ImageValidatingPolicyV1Beta1SpecCredentials;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: ImageValidatingPolicyV1Beta1SpecEvaluation;

  /**
   * FailurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#failurePolicy
   */
  readonly failurePolicy?: ImageValidatingPolicyV1Beta1SpecFailurePolicy;

  /**
   * ImageExtractors is a list of CEL expression to extract images from the resource
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#images
   */
  readonly images?: ImageValidatingPolicyV1Beta1SpecImages[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: ImageValidatingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: ImageValidatingPolicyV1Beta1SpecMatchConstraints;

  /**
   * MatchImageReferences is a list of Glob and CELExpressions to match images.
   * Any image that matches one of the rules is considered for validation
   * Any image that does not match a rule is skipped, even when they are passed as arguments to
   * image verification functions
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#matchImageReferences
   */
  readonly matchImageReferences?: ImageValidatingPolicyV1Beta1SpecMatchImageReferences[];

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#validationActions
   */
  readonly validationActions?: ImageValidatingPolicyV1Beta1SpecValidationActions[];

  /**
   * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#validationConfigurations
   */
  readonly validationConfigurations?: ImageValidatingPolicyV1Beta1SpecValidationConfigurations;

  /**
   * Validations contain CEL expressions which is used to apply the image validation checks.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#validations
   */
  readonly validations: ImageValidatingPolicyV1Beta1SpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#variables
   */
  readonly variables?: ImageValidatingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema ImageValidatingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: ImageValidatingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1Spec(
  obj: ImageValidatingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    attestations: obj.attestations?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecAttestations(y),
    ),
    attestors: obj.attestors?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecAttestors(y),
    ),
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecAuditAnnotations(y),
    ),
    autogen: toJson_ImageValidatingPolicyV1Beta1SpecAutogen(obj.autogen),
    credentials: toJson_ImageValidatingPolicyV1Beta1SpecCredentials(
      obj.credentials,
    ),
    evaluation: toJson_ImageValidatingPolicyV1Beta1SpecEvaluation(
      obj.evaluation,
    ),
    failurePolicy: obj.failurePolicy,
    images: obj.images?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecImages(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    matchImageReferences: obj.matchImageReferences?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecMatchImageReferences(y),
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validationConfigurations:
      toJson_ImageValidatingPolicyV1Beta1SpecValidationConfigurations(
        obj.validationConfigurations,
      ),
    validations: obj.validations?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_ImageValidatingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestation defines the identification details of the  metadata that has to be verified
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestations
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestations {
  /**
   * InToto defines the details of attestation attached using intoto format
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestations#intoto
   */
  readonly intoto?: ImageValidatingPolicyV1Beta1SpecAttestationsIntoto;

  /**
   * Name is the name for this attestation. It is used to refer to the attestation in verification
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestations#name
   */
  readonly name: string;

  /**
   * Referrer defines the details of attestation attached using OCI 1.1 format
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestations#referrer
   */
  readonly referrer?: ImageValidatingPolicyV1Beta1SpecAttestationsReferrer;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestations(
  obj: ImageValidatingPolicyV1Beta1SpecAttestations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    intoto: toJson_ImageValidatingPolicyV1Beta1SpecAttestationsIntoto(
      obj.intoto,
    ),
    name: obj.name,
    referrer: toJson_ImageValidatingPolicyV1Beta1SpecAttestationsReferrer(
      obj.referrer,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestor is an identity that confirms or verifies the authenticity of an image or an attestation
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestors
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestors {
  /**
   * Cosign defines attestor configuration for Cosign based signatures
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestors#cosign
   */
  readonly cosign?: ImageValidatingPolicyV1Beta1SpecAttestorsCosign;

  /**
   * Name is the name for this attestor. It is used to refer to the attestor in verification
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestors#name
   */
  readonly name: string;

  /**
   * Notary defines attestor configuration for Notary based signatures
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestors#notary
   */
  readonly notary?: ImageValidatingPolicyV1Beta1SpecAttestorsNotary;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestors' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestors(
  obj: ImageValidatingPolicyV1Beta1SpecAttestors | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cosign: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosign(obj.cosign),
    name: obj.name,
    notary: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsNotary(obj.notary),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAuditAnnotations
 */
export interface ImageValidatingPolicyV1Beta1SpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAuditAnnotations(
  obj: ImageValidatingPolicyV1Beta1SpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAutogen
 */
export interface ImageValidatingPolicyV1Beta1SpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAutogen#podControllers
   */
  readonly podControllers?: ImageValidatingPolicyV1Beta1SpecAutogenPodControllers;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAutogen(
  obj: ImageValidatingPolicyV1Beta1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers:
      toJson_ImageValidatingPolicyV1Beta1SpecAutogenPodControllers(
        obj.podControllers,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Credentials provides credentials that will be used for authentication with registry.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecCredentials
 */
export interface ImageValidatingPolicyV1Beta1SpecCredentials {
  /**
   * AllowInsecureRegistry allows insecure access to a registry.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecCredentials#allowInsecureRegistry
   */
  readonly allowInsecureRegistry?: boolean;

  /**
   * Providers specifies a list of OCI Registry names, whose authentication providers are provided.
   * It can be of one of these values: default,google,azure,amazon,github.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecCredentials#providers
   */
  readonly providers?: ImageValidatingPolicyV1Beta1SpecCredentialsProviders[];

  /**
   * Secrets specifies a list of secrets that are provided for credentials.
   * Secrets must live in the Kyverno namespace.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecCredentials#secrets
   */
  readonly secrets?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecCredentials' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecCredentials(
  obj: ImageValidatingPolicyV1Beta1SpecCredentials | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowInsecureRegistry: obj.allowInsecureRegistry,
    providers: obj.providers?.map((y) => y),
    secrets: obj.secrets?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecEvaluation
 */
export interface ImageValidatingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: ImageValidatingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecEvaluation#background
   */
  readonly background?: ImageValidatingPolicyV1Beta1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema ImageValidatingPolicyV1Beta1SpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecEvaluation(
  obj: ImageValidatingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_ImageValidatingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_ImageValidatingPolicyV1Beta1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * FailurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecFailurePolicy
 */
export enum ImageValidatingPolicyV1Beta1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * @schema ImageValidatingPolicyV1Beta1SpecImages
 */
export interface ImageValidatingPolicyV1Beta1SpecImages {
  /**
   * Expression defines CEL expression to extract images from the resource.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecImages#expression
   */
  readonly expression: string;

  /**
   * Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
   *
   * @schema ImageValidatingPolicyV1Beta1SpecImages#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecImages' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecImages(
  obj: ImageValidatingPolicyV1Beta1SpecImages | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConditions
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConditions(
  obj: ImageValidatingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraints
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraints(
  obj: ImageValidatingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchImageReference defines a Glob or a CEL expression for matching images
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchImageReferences
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchImageReferences {
  /**
   * Expression defines CEL Expressions for matching images
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchImageReferences#expression
   */
  readonly expression?: string;

  /**
   * Glob defines a globbing pattern for matching images
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchImageReferences#glob
   */
  readonly glob?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchImageReferences' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchImageReferences(
  obj: ImageValidatingPolicyV1Beta1SpecMatchImageReferences | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    glob: obj.glob,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecValidationActions
 */
export enum ImageValidatingPolicyV1Beta1SpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecValidationConfigurations
 */
export interface ImageValidatingPolicyV1Beta1SpecValidationConfigurations {
  /**
   * MutateDigest enables replacement of image tags with digests.
   * Defaults to true.
   *
   * @default true.
   * @schema ImageValidatingPolicyV1Beta1SpecValidationConfigurations#mutateDigest
   */
  readonly mutateDigest?: boolean;

  /**
   * Required validates that images are verified, i.e., have passed a signature or attestation check.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecValidationConfigurations#required
   */
  readonly required?: boolean;

  /**
   * VerifyDigest validates that images have a digest.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecValidationConfigurations#verifyDigest
   */
  readonly verifyDigest?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecValidationConfigurations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecValidationConfigurations(
  obj: ImageValidatingPolicyV1Beta1SpecValidationConfigurations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutateDigest: obj.mutateDigest,
    required: obj.required,
    verifyDigest: obj.verifyDigest,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecValidations
 */
export interface ImageValidatingPolicyV1Beta1SpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema ImageValidatingPolicyV1Beta1SpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema ImageValidatingPolicyV1Beta1SpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecValidations(
  obj: ImageValidatingPolicyV1Beta1SpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecVariables
 */
export interface ImageValidatingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema ImageValidatingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecVariables(
  obj: ImageValidatingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface ImageValidatingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecWebhookConfiguration(
  obj: ImageValidatingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * InToto defines the details of attestation attached using intoto format
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestationsIntoto
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestationsIntoto {
  /**
   * Type defines the type of attestation contained within the statement.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestationsIntoto#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestationsIntoto' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestationsIntoto(
  obj: ImageValidatingPolicyV1Beta1SpecAttestationsIntoto | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Referrer defines the details of attestation attached using OCI 1.1 format
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestationsReferrer
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestationsReferrer {
  /**
   * Type defines the type of attestation attached to the image.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestationsReferrer#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestationsReferrer' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestationsReferrer(
  obj: ImageValidatingPolicyV1Beta1SpecAttestationsReferrer | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Cosign defines attestor configuration for Cosign based signatures
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosign {
  /**
   * Annotations are used for image verification.
   * Every specified key-value pair must exist and match in the verified payload.
   * The payload may contain other key-value pairs.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#annotations
   */
  readonly annotations?: { [key: string]: string };

  /**
   * Certificate defines the configuration for local signature verification
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#certificate
   */
  readonly certificate?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate;

  /**
   * CTLog sets the configuration to verify the authority against a Rekor instance.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#ctlog
   */
  readonly ctlog?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog;

  /**
   * Key defines the type of key to validate the image.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#key
   */
  readonly key?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey;

  /**
   * Keyless sets the configuration to verify the authority against a Fulcio instance.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#keyless
   */
  readonly keyless?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless;

  /**
   * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#source
   */
  readonly source?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource;

  /**
   * TUF defines the configuration to fetch sigstore root
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosign#tuf
   */
  readonly tuf?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosign' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosign(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosign | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    annotations:
      obj.annotations === undefined
        ? undefined
        : Object.entries(obj.annotations).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
    certificate:
      toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate(
        obj.certificate,
      ),
    ctlog: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog(
      obj.ctlog,
    ),
    key: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey(obj.key),
    keyless: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless(
      obj.keyless,
    ),
    source: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource(
      obj.source,
    ),
    tuf: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf(obj.tuf),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Notary defines attestor configuration for Notary based signatures
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotary
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsNotary {
  /**
   * Certs define the cert chain for Notary signature verification
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotary#certs
   */
  readonly certs?: ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts;

  /**
   * TSACerts define the cert chain for verifying timestamps of notary signature
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotary#tsaCerts
   */
  readonly tsaCerts?: ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsNotary' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsNotary(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsNotary | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    certs: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts(
      obj.certs,
    ),
    tsaCerts: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts(
      obj.tsaCerts,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAutogenPodControllers
 */
export interface ImageValidatingPolicyV1Beta1SpecAutogenPodControllers {
  /**
   * @schema ImageValidatingPolicyV1Beta1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAutogenPodControllers(
  obj: ImageValidatingPolicyV1Beta1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CredentialsProvidersType provides the list of credential providers required.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecCredentialsProviders
 */
export enum ImageValidatingPolicyV1Beta1SpecCredentialsProviders {
  /** default */
  DEFAULT = "default",
  /** amazon */
  AMAZON = "amazon",
  /** azure */
  AZURE = "azure",
  /** google */
  GOOGLE = "google",
  /** github */
  GITHUB = "github",
}

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface ImageValidatingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema ImageValidatingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecEvaluationAdmission(
  obj: ImageValidatingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecEvaluationBackground
 */
export interface ImageValidatingPolicyV1Beta1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema ImageValidatingPolicyV1Beta1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecEvaluationBackground(
  obj: ImageValidatingPolicyV1Beta1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | ImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj:
    | ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj:
    | ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj:
    | ImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate defines the configuration for local signature verification
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate {
  /**
   * Certificate is the to the public certificate for local signature verification.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate#cert
   */
  readonly cert?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert;

  /**
   * CertificateChain is the list of CA certificates in PEM format which will be needed
   * when building the certificate chain for the signing certificate. Must start with the
   * parent intermediate CA certificate of the signing certificate and end with the root certificate
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate#certChain
   */
  readonly certChain?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cert: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert(
      obj.cert,
    ),
    certChain:
      toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain(
        obj.certChain,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CTLog sets the configuration to verify the authority against a Rekor instance.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog {
  /**
   * CTLogPubKey, if set, is used to validate SCTs against a custom source.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#ctLogPubKey
   */
  readonly ctLogPubKey?: string;

  /**
   * IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate
   * timestamp. Default is false. Set to true if this was opted out during signing.
   *
   * @default false. Set to true if this was opted out during signing.
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#insecureIgnoreSCT
   */
  readonly insecureIgnoreSct?: boolean;

  /**
   * InsecureIgnoreTlog skips transparency log verification.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#insecureIgnoreTlog
   */
  readonly insecureIgnoreTlog?: boolean;

  /**
   * RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor.
   * If set, this will be used to validate transparency log signatures from a custom Rekor.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#rekorPubKey
   */
  readonly rekorPubKey?: string;

  /**
   * TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must
   * contain the root CA certificate. Optionally may contain intermediate CA certificates, and
   * may contain the leaf TSA certificate if not present in the timestamurce.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#tsaCertChain
   */
  readonly tsaCertChain?: string;

  /**
   * URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#url
   */
  readonly url?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    ctLogPubKey: obj.ctLogPubKey,
    insecureIgnoreSCT: obj.insecureIgnoreSct,
    insecureIgnoreTlog: obj.insecureIgnoreTlog,
    rekorPubKey: obj.rekorPubKey,
    tsaCertChain: obj.tsaCertChain,
    url: obj.url,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Key defines the type of key to validate the image.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey {
  /**
   * Data contains the inline public key
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#data
   */
  readonly data?: string;

  /**
   * Expression is a Expression expression that returns the public key.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#expression
   */
  readonly expression?: string;

  /**
   * HashAlgorithm specifues signature algorithm for public keys. Supported values are
   * sha224, sha256, sha384 and sha512. Defaults to sha256.
   *
   * @default sha256.
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#hashAlgorithm
   */
  readonly hashAlgorithm?: string;

  /**
   * KMS contains the KMS url of the public key
   * Supported formats differ based on the KMS system used.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#kms
   */
  readonly kms?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignKey | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    expression: obj.expression,
    hashAlgorithm: obj.hashAlgorithm,
    kms: obj.kms,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Keyless sets the configuration to verify the authority against a Fulcio instance.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless {
  /**
   * Identities sets a list of identities.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless#identities
   */
  readonly identities: ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities[];

  /**
   * Roots is an optional set of PEM encoded trusted root certificates.
   * If not provided, the system roots are used.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless#roots
   */
  readonly roots?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    identities: obj.identities?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities(
        y,
      ),
    ),
    roots: obj.roots,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource {
  /**
   * SignaturePullSecrets is an optional list of references to secrets in the
   * same namespace as the deploying resource for pulling any of the signatures
   * used by this Source.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource#PullSecrets
   */
  readonly pullSecrets?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets[];

  /**
   * Repository defines the location from where to pull the signature / attestations.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource#repository
   */
  readonly repository?: string;

  /**
   * TagPrefix is an optional prefix that signature and attestations have.
   * This is the 'tag based discovery' and in the future once references are
   * fully supported that should likely be the preferred way to handle these.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource#tagPrefix
   */
  readonly tagPrefix?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignSource | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    PullSecrets: obj.pullSecrets?.map((y) =>
      toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets(
        y,
      ),
    ),
    repository: obj.repository,
    tagPrefix: obj.tagPrefix,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TUF defines the configuration to fetch sigstore root
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf {
  /**
   * Mirror is the base URL of Sigstore TUF repository
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf#mirror
   */
  readonly mirror?: string;

  /**
   * Root defines the path or data of the trusted root
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf#root
   */
  readonly root?: ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mirror: obj.mirror,
    root: toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot(
      obj.root,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certs define the cert chain for Notary signature verification
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TSACerts define the cert chain for verifying timestamps of notary signature
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | ImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | ImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate is the to the public certificate for local signature verification.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert(
  obj:
    | ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CertificateChain is the list of CA certificates in PEM format which will be needed
 * when building the certificate chain for the signing certificate. Must start with the
 * parent intermediate CA certificate of the signing certificate and end with the root certificate
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain(
  obj:
    | ImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Identity may contain the issuer and/or the subject found in the transparency
 * log.
 * Issuer/Subject uses a strict match, while IssuerRegExp and SubjectRegExp
 * apply a regexp for matching.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities {
  /**
   * Issuer defines the issuer for this identity.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#issuer
   */
  readonly issuer?: string;

  /**
   * IssuerRegExp specifies a regular expression to match the issuer for this identity.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#issuerRegExp
   */
  readonly issuerRegExp?: string;

  /**
   * Subject defines the subject for this identity.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#subject
   */
  readonly subject?: string;

  /**
   * SubjectRegExp specifies a regular expression to match the subject for this identity.
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#subjectRegExp
   */
  readonly subjectRegExp?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities(
  obj:
    | ImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    issuer: obj.issuer,
    issuerRegExp: obj.issuerRegExp,
    subject: obj.subject,
    subjectRegExp: obj.subjectRegExp,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * LocalObjectReference contains enough information to let you locate the
 * referenced object inside the same namespace.
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets {
  /**
   * Name of the referent.
   * This field is effectively required, but due to backwards compatibility is
   * allowed to be empty. Instances of this type with an empty value here are
   * almost certainly wrong.
   * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets#name
   */
  readonly name?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets(
  obj:
    | ImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Root defines the path or data of the trusted root
 *
 * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot
 */
export interface ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot {
  /**
   * Data is the base64 encoded TUF root
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot#data
   */
  readonly data?: string;

  /**
   * Path is the URL or File location of the TUF root
   *
   * @schema ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot#path
   */
  readonly path?: string;
}

/**
 * Converts an object of type 'ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot(
  obj: ImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    path: obj.path,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema MutatingPolicy
 */
export class MutatingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "MutatingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "MutatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "MutatingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: MutatingPolicyProps): any {
    return {
      ...MutatingPolicy.GVK,
      ...toJson_MutatingPolicyProps(props),
    };
  }

  /**
   * Defines a "MutatingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(scope: Construct, id: string, props: MutatingPolicyProps) {
    super(scope, id, {
      ...MutatingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...MutatingPolicy.GVK,
      ...toJson_MutatingPolicyProps(resolved),
    };
  }
}

/**
 * @schema MutatingPolicy
 */
export interface MutatingPolicyProps {
  /**
   * @schema MutatingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
   *
   * @schema MutatingPolicy#spec
   */
  readonly spec: MutatingPolicySpec;
}

/**
 * Converts an object of type 'MutatingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyProps(
  obj: MutatingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_MutatingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
 *
 * @schema MutatingPolicySpec
 */
export interface MutatingPolicySpec {
  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema MutatingPolicySpec#autogen
   */
  readonly autogen?: MutatingPolicySpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for mutating policy evaluation.
   *
   * @schema MutatingPolicySpec#evaluation
   */
  readonly evaluation?: MutatingPolicySpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema MutatingPolicySpec#failurePolicy
   */
  readonly failurePolicy?: MutatingPolicySpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema MutatingPolicySpec#matchConditions
   */
  readonly matchConditions?: MutatingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to evaluate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema MutatingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: MutatingPolicySpecMatchConstraints;

  /**
   * mutations contain operations to perform on matching objects.
   * mutations may not be empty; a minimum of one mutation is required.
   * mutations are evaluated in order, and are reinvoked according to
   * the reinvocationPolicy.
   * The mutations of a policy are invoked for each binding of this policy
   * and reinvocation of mutations occurs on a per binding basis.
   *
   * @schema MutatingPolicySpec#mutations
   */
  readonly mutations?: MutatingPolicySpecMutations[];

  /**
   * reinvocationPolicy indicates whether mutations may be called multiple times per MutatingAdmissionPolicyBinding
   * as part of a single admission evaluation.
   * Allowed values are "Never" and "IfNeeded".
   *
   * Never: These mutations will not be called more than once per binding in a single admission evaluation.
   *
   * IfNeeded: These mutations may be invoked more than once per binding for a single admission request and there is no guarantee of
   * order with respect to other admission plugins, admission webhooks, bindings of this policy and admission policies.  Mutations are only
   * reinvoked when mutations change the object after this mutation is invoked.
   * Required.
   *
   * @schema MutatingPolicySpec#reinvocationPolicy
   */
  readonly reinvocationPolicy?: string;

  /**
   * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
   *
   * @schema MutatingPolicySpec#targetMatchConstraints
   */
  readonly targetMatchConstraints?: MutatingPolicySpecTargetMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema MutatingPolicySpec#variables
   */
  readonly variables?: MutatingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema MutatingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: MutatingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'MutatingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpec(
  obj: MutatingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    autogen: toJson_MutatingPolicySpecAutogen(obj.autogen),
    evaluation: toJson_MutatingPolicySpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_MutatingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_MutatingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    mutations: obj.mutations?.map((y) => toJson_MutatingPolicySpecMutations(y)),
    reinvocationPolicy: obj.reinvocationPolicy,
    targetMatchConstraints: toJson_MutatingPolicySpecTargetMatchConstraints(
      obj.targetMatchConstraints,
    ),
    variables: obj.variables?.map((y) => toJson_MutatingPolicySpecVariables(y)),
    webhookConfiguration: toJson_MutatingPolicySpecWebhookConfiguration(
      obj.webhookConfiguration,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema MutatingPolicySpecAutogen
 */
export interface MutatingPolicySpecAutogen {
  /**
   * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   *
   * @schema MutatingPolicySpecAutogen#mutatingAdmissionPolicy
   */
  readonly mutatingAdmissionPolicy?: MutatingPolicySpecAutogenMutatingAdmissionPolicy;

  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema MutatingPolicySpecAutogen#podControllers
   */
  readonly podControllers?: MutatingPolicySpecAutogenPodControllers;
}

/**
 * Converts an object of type 'MutatingPolicySpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecAutogen(
  obj: MutatingPolicySpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutatingAdmissionPolicy:
      toJson_MutatingPolicySpecAutogenMutatingAdmissionPolicy(
        obj.mutatingAdmissionPolicy,
      ),
    podControllers: toJson_MutatingPolicySpecAutogenPodControllers(
      obj.podControllers,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for mutating policy evaluation.
 *
 * @schema MutatingPolicySpecEvaluation
 */
export interface MutatingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema MutatingPolicySpecEvaluation#admission
   */
  readonly admission?: MutatingPolicySpecEvaluationAdmission;

  /**
   * Background controls policy evaluation during background scan.
   *
   * @schema MutatingPolicySpecEvaluation#background
   */
  readonly background?: MutatingPolicySpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema MutatingPolicySpecEvaluation#mode
   */
  readonly mode?: string;

  /**
   * MutateExisting controls whether existing resources are mutated.
   *
   * @schema MutatingPolicySpecEvaluation#mutateExisting
   */
  readonly mutateExisting?: MutatingPolicySpecEvaluationMutateExisting;
}

/**
 * Converts an object of type 'MutatingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecEvaluation(
  obj: MutatingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_MutatingPolicySpecEvaluationAdmission(obj.admission),
    background: toJson_MutatingPolicySpecEvaluationBackground(obj.background),
    mode: obj.mode,
    mutateExisting: toJson_MutatingPolicySpecEvaluationMutateExisting(
      obj.mutateExisting,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema MutatingPolicySpecFailurePolicy
 */
export enum MutatingPolicySpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema MutatingPolicySpecMatchConditions
 */
export interface MutatingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema MutatingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema MutatingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConditions(
  obj: MutatingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to evaluate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema MutatingPolicySpecMatchConstraints
 */
export interface MutatingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema MutatingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: MutatingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema MutatingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: MutatingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: MutatingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema MutatingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: MutatingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraints(
  obj: MutatingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_MutatingPolicySpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_MutatingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector: toJson_MutatingPolicySpecMatchConstraintsObjectSelector(
      obj.objectSelector,
    ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_MutatingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Mutation specifies the CEL expression which is used to apply the Mutation.
 *
 * @schema MutatingPolicySpecMutations
 */
export interface MutatingPolicySpecMutations {
  /**
   * applyConfiguration defines the desired configuration values of an object.
   * The configuration is applied to the admission object using
   * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
   * A CEL expression is used to create apply configuration.
   *
   * @schema MutatingPolicySpecMutations#applyConfiguration
   */
  readonly applyConfiguration?: MutatingPolicySpecMutationsApplyConfiguration;

  /**
   * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
   * A CEL expression is used to create the JSON patch.
   *
   * @schema MutatingPolicySpecMutations#jsonPatch
   */
  readonly jsonPatch?: MutatingPolicySpecMutationsJsonPatch;

  /**
   * patchType indicates the patch strategy used.
   * Allowed values are "ApplyConfiguration" and "JSONPatch".
   * Required.
   *
   * @schema MutatingPolicySpecMutations#patchType
   */
  readonly patchType: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecMutations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMutations(
  obj: MutatingPolicySpecMutations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    applyConfiguration: toJson_MutatingPolicySpecMutationsApplyConfiguration(
      obj.applyConfiguration,
    ),
    jsonPatch: toJson_MutatingPolicySpecMutationsJsonPatch(obj.jsonPatch),
    patchType: obj.patchType,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
 *
 * @schema MutatingPolicySpecTargetMatchConstraints
 */
export interface MutatingPolicySpecTargetMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema MutatingPolicySpecTargetMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules[];

  /**
   * @schema MutatingPolicySpecTargetMatchConstraints#expression
   */
  readonly expression?: string;

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema MutatingPolicySpecTargetMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicySpecTargetMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: MutatingPolicySpecTargetMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicySpecTargetMatchConstraints#objectSelector
   */
  readonly objectSelector?: MutatingPolicySpecTargetMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema MutatingPolicySpecTargetMatchConstraints#resourceRules
   */
  readonly resourceRules?: MutatingPolicySpecTargetMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraints(
  obj: MutatingPolicySpecTargetMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules(y),
    ),
    expression: obj.expression,
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_MutatingPolicySpecTargetMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_MutatingPolicySpecTargetMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_MutatingPolicySpecTargetMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema MutatingPolicySpecVariables
 */
export interface MutatingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema MutatingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema MutatingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecVariables(
  obj: MutatingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema MutatingPolicySpecWebhookConfiguration
 */
export interface MutatingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema MutatingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'MutatingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecWebhookConfiguration(
  obj: MutatingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
 *
 * @schema MutatingPolicySpecAutogenMutatingAdmissionPolicy
 */
export interface MutatingPolicySpecAutogenMutatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema MutatingPolicySpecAutogenMutatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicySpecAutogenMutatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecAutogenMutatingAdmissionPolicy(
  obj: MutatingPolicySpecAutogenMutatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema MutatingPolicySpecAutogenPodControllers
 */
export interface MutatingPolicySpecAutogenPodControllers {
  /**
   * @schema MutatingPolicySpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'MutatingPolicySpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecAutogenPodControllers(
  obj: MutatingPolicySpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema MutatingPolicySpecEvaluationAdmission
 */
export interface MutatingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema MutatingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecEvaluationAdmission(
  obj: MutatingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background controls policy evaluation during background scan.
 *
 * @schema MutatingPolicySpecEvaluationBackground
 */
export interface MutatingPolicySpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema MutatingPolicySpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicySpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecEvaluationBackground(
  obj: MutatingPolicySpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutateExisting controls whether existing resources are mutated.
 *
 * @schema MutatingPolicySpecEvaluationMutateExisting
 */
export interface MutatingPolicySpecEvaluationMutateExisting {
  /**
   * Enabled enables mutation of existing resources. Default is false.
   * When spec.targetMatchConstraints is not defined, Kyverno mutates existing resources matched in spec.matchConstraints.
   *
   * @default false.
   * @schema MutatingPolicySpecEvaluationMutateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicySpecEvaluationMutateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecEvaluationMutateExisting(
  obj: MutatingPolicySpecEvaluationMutateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface MutatingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraintsExcludeResourceRules(
  obj: MutatingPolicySpecMatchConstraintsExcludeResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface MutatingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraintsNamespaceSelector(
  obj: MutatingPolicySpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicySpecMatchConstraintsObjectSelector
 */
export interface MutatingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraintsObjectSelector(
  obj: MutatingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicySpecMatchConstraintsResourceRules
 */
export interface MutatingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraintsResourceRules(
  obj: MutatingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * applyConfiguration defines the desired configuration values of an object.
 * The configuration is applied to the admission object using
 * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
 * A CEL expression is used to create apply configuration.
 *
 * @schema MutatingPolicySpecMutationsApplyConfiguration
 */
export interface MutatingPolicySpecMutationsApplyConfiguration {
  /**
   * expression will be evaluated by CEL to create an apply configuration.
   * ref: https://github.com/google/cel-spec
   *
   * Apply configurations are declared in CEL using object initialization. For example, this CEL expression
   * returns an apply configuration to set a single field:
   *
   * Object{
   * spec: Object.spec{
   * serviceAccountName: "example"
   * }
   * }
   *
   * Apply configurations may not modify atomic structs, maps or arrays due to the risk of accidental deletion of
   * values not included in the apply configuration.
   *
   * CEL expressions have access to the object types needed to create apply configurations:
   *
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema MutatingPolicySpecMutationsApplyConfiguration#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecMutationsApplyConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMutationsApplyConfiguration(
  obj: MutatingPolicySpecMutationsApplyConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
 * A CEL expression is used to create the JSON patch.
 *
 * @schema MutatingPolicySpecMutationsJsonPatch
 */
export interface MutatingPolicySpecMutationsJsonPatch {
  /**
   * expression will be evaluated by CEL to create a [JSON patch](https://jsonpatch.com/).
   * ref: https://github.com/google/cel-spec
   *
   * expression must return an array of JSONPatch values.
   *
   * For example, this CEL expression returns a JSON patch to conditionally modify a value:
   *
   * [
   * JSONPatch{op: "test", path: "/spec/example", value: "Red"},
   * JSONPatch{op: "replace", path: "/spec/example", value: "Green"}
   * ]
   *
   * To define an object for the patch value, use Object types. For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/spec/selector",
   * value: Object.spec.selector{matchLabels: {"environment": "test"}}
   * }
   * ]
   *
   * To use strings containing '/' and '~' as JSONPatch path keys, use "jsonpatch.escapeKey". For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/metadata/labels/" + jsonpatch.escapeKey("example.com/environment"),
   * value: "test"
   * },
   * ]
   *
   * CEL expressions have access to the types needed to create JSON patches and objects:
   *
   * - 'JSONPatch' - CEL type of JSON Patch operations. JSONPatch has the fields 'op', 'from', 'path' and 'value'.
   * See [JSON patch](https://jsonpatch.com/) for more details. The 'value' field may be set to any of: string,
   * integer, array, map or object.  If set, the 'path' and 'from' fields must be set to a
   * [JSON pointer](https://datatracker.ietf.org/doc/html/rfc6901/) string, where the 'jsonpatch.escapeKey()' CEL
   * function may be used to escape path keys containing '/' and '~'.
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * CEL expressions have access to [Kubernetes CEL function libraries](https://kubernetes.io/docs/reference/using-api/cel/#cel-options-language-features-and-libraries)
   * as well as:
   *
   * - 'jsonpatch.escapeKey' - Performs JSONPatch key escaping. '~' and  '/' are escaped as '~0' and `~1' respectively).
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema MutatingPolicySpecMutationsJsonPatch#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecMutationsJsonPatch' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMutationsJsonPatch(
  obj: MutatingPolicySpecMutationsJsonPatch | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules
 */
export interface MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules(
  obj: MutatingPolicySpecTargetMatchConstraintsExcludeResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelector
 */
export interface MutatingPolicySpecTargetMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraintsNamespaceSelector(
  obj: MutatingPolicySpecTargetMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelector
 */
export interface MutatingPolicySpecTargetMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraintsObjectSelector(
  obj: MutatingPolicySpecTargetMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules
 */
export interface MutatingPolicySpecTargetMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicySpecTargetMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraintsResourceRules(
  obj: MutatingPolicySpecTargetMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | MutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | MutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | MutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions
 */
export interface MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | MutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema MutatingPolicyV1Alpha1
 */
export class MutatingPolicyV1Alpha1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "MutatingPolicyV1Alpha1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1alpha1",
    kind: "MutatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "MutatingPolicyV1Alpha1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: MutatingPolicyV1Alpha1Props): any {
    return {
      ...MutatingPolicyV1Alpha1.GVK,
      ...toJson_MutatingPolicyV1Alpha1Props(props),
    };
  }

  /**
   * Defines a "MutatingPolicyV1Alpha1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: MutatingPolicyV1Alpha1Props,
  ) {
    super(scope, id, {
      ...MutatingPolicyV1Alpha1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...MutatingPolicyV1Alpha1.GVK,
      ...toJson_MutatingPolicyV1Alpha1Props(resolved),
    };
  }
}

/**
 * @schema MutatingPolicyV1Alpha1
 */
export interface MutatingPolicyV1Alpha1Props {
  /**
   * @schema MutatingPolicyV1Alpha1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
   *
   * @schema MutatingPolicyV1Alpha1#spec
   */
  readonly spec: MutatingPolicyV1Alpha1Spec;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1Props(
  obj: MutatingPolicyV1Alpha1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_MutatingPolicyV1Alpha1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
 *
 * @schema MutatingPolicyV1Alpha1Spec
 */
export interface MutatingPolicyV1Alpha1Spec {
  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema MutatingPolicyV1Alpha1Spec#autogen
   */
  readonly autogen?: MutatingPolicyV1Alpha1SpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for mutating policy evaluation.
   *
   * @schema MutatingPolicyV1Alpha1Spec#evaluation
   */
  readonly evaluation?: MutatingPolicyV1Alpha1SpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema MutatingPolicyV1Alpha1Spec#failurePolicy
   */
  readonly failurePolicy?: MutatingPolicyV1Alpha1SpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema MutatingPolicyV1Alpha1Spec#matchConditions
   */
  readonly matchConditions?: MutatingPolicyV1Alpha1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to evaluate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1Spec#matchConstraints
   */
  readonly matchConstraints?: MutatingPolicyV1Alpha1SpecMatchConstraints;

  /**
   * mutations contain operations to perform on matching objects.
   * mutations may not be empty; a minimum of one mutation is required.
   * mutations are evaluated in order, and are reinvoked according to
   * the reinvocationPolicy.
   * The mutations of a policy are invoked for each binding of this policy
   * and reinvocation of mutations occurs on a per binding basis.
   *
   * @schema MutatingPolicyV1Alpha1Spec#mutations
   */
  readonly mutations?: MutatingPolicyV1Alpha1SpecMutations[];

  /**
   * reinvocationPolicy indicates whether mutations may be called multiple times per MutatingAdmissionPolicyBinding
   * as part of a single admission evaluation.
   * Allowed values are "Never" and "IfNeeded".
   *
   * Never: These mutations will not be called more than once per binding in a single admission evaluation.
   *
   * IfNeeded: These mutations may be invoked more than once per binding for a single admission request and there is no guarantee of
   * order with respect to other admission plugins, admission webhooks, bindings of this policy and admission policies.  Mutations are only
   * reinvoked when mutations change the object after this mutation is invoked.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1Spec#reinvocationPolicy
   */
  readonly reinvocationPolicy?: string;

  /**
   * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
   *
   * @schema MutatingPolicyV1Alpha1Spec#targetMatchConstraints
   */
  readonly targetMatchConstraints?: MutatingPolicyV1Alpha1SpecTargetMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema MutatingPolicyV1Alpha1Spec#variables
   */
  readonly variables?: MutatingPolicyV1Alpha1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema MutatingPolicyV1Alpha1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: MutatingPolicyV1Alpha1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1Spec(
  obj: MutatingPolicyV1Alpha1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    autogen: toJson_MutatingPolicyV1Alpha1SpecAutogen(obj.autogen),
    evaluation: toJson_MutatingPolicyV1Alpha1SpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_MutatingPolicyV1Alpha1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    mutations: obj.mutations?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecMutations(y),
    ),
    reinvocationPolicy: obj.reinvocationPolicy,
    targetMatchConstraints:
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraints(
        obj.targetMatchConstraints,
      ),
    variables: obj.variables?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecVariables(y),
    ),
    webhookConfiguration: toJson_MutatingPolicyV1Alpha1SpecWebhookConfiguration(
      obj.webhookConfiguration,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema MutatingPolicyV1Alpha1SpecAutogen
 */
export interface MutatingPolicyV1Alpha1SpecAutogen {
  /**
   * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   *
   * @schema MutatingPolicyV1Alpha1SpecAutogen#mutatingAdmissionPolicy
   */
  readonly mutatingAdmissionPolicy?: MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy;

  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema MutatingPolicyV1Alpha1SpecAutogen#podControllers
   */
  readonly podControllers?: MutatingPolicyV1Alpha1SpecAutogenPodControllers;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecAutogen(
  obj: MutatingPolicyV1Alpha1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutatingAdmissionPolicy:
      toJson_MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy(
        obj.mutatingAdmissionPolicy,
      ),
    podControllers: toJson_MutatingPolicyV1Alpha1SpecAutogenPodControllers(
      obj.podControllers,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for mutating policy evaluation.
 *
 * @schema MutatingPolicyV1Alpha1SpecEvaluation
 */
export interface MutatingPolicyV1Alpha1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema MutatingPolicyV1Alpha1SpecEvaluation#admission
   */
  readonly admission?: MutatingPolicyV1Alpha1SpecEvaluationAdmission;

  /**
   * Background controls policy evaluation during background scan.
   *
   * @schema MutatingPolicyV1Alpha1SpecEvaluation#background
   */
  readonly background?: MutatingPolicyV1Alpha1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema MutatingPolicyV1Alpha1SpecEvaluation#mode
   */
  readonly mode?: string;

  /**
   * MutateExisting controls whether existing resources are mutated.
   *
   * @schema MutatingPolicyV1Alpha1SpecEvaluation#mutateExisting
   */
  readonly mutateExisting?: MutatingPolicyV1Alpha1SpecEvaluationMutateExisting;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecEvaluation(
  obj: MutatingPolicyV1Alpha1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_MutatingPolicyV1Alpha1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_MutatingPolicyV1Alpha1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
    mutateExisting: toJson_MutatingPolicyV1Alpha1SpecEvaluationMutateExisting(
      obj.mutateExisting,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema MutatingPolicyV1Alpha1SpecFailurePolicy
 */
export enum MutatingPolicyV1Alpha1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema MutatingPolicyV1Alpha1SpecMatchConditions
 */
export interface MutatingPolicyV1Alpha1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConditions(
  obj: MutatingPolicyV1Alpha1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to evaluate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraints
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraints(
  obj: MutatingPolicyV1Alpha1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Mutation specifies the CEL expression which is used to apply the Mutation.
 *
 * @schema MutatingPolicyV1Alpha1SpecMutations
 */
export interface MutatingPolicyV1Alpha1SpecMutations {
  /**
   * applyConfiguration defines the desired configuration values of an object.
   * The configuration is applied to the admission object using
   * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
   * A CEL expression is used to create apply configuration.
   *
   * @schema MutatingPolicyV1Alpha1SpecMutations#applyConfiguration
   */
  readonly applyConfiguration?: MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration;

  /**
   * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
   * A CEL expression is used to create the JSON patch.
   *
   * @schema MutatingPolicyV1Alpha1SpecMutations#jsonPatch
   */
  readonly jsonPatch?: MutatingPolicyV1Alpha1SpecMutationsJsonPatch;

  /**
   * patchType indicates the patch strategy used.
   * Allowed values are "ApplyConfiguration" and "JSONPatch".
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMutations#patchType
   */
  readonly patchType: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMutations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMutations(
  obj: MutatingPolicyV1Alpha1SpecMutations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    applyConfiguration:
      toJson_MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration(
        obj.applyConfiguration,
      ),
    jsonPatch: toJson_MutatingPolicyV1Alpha1SpecMutationsJsonPatch(
      obj.jsonPatch,
    ),
    patchType: obj.patchType,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
 *
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules[];

  /**
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints#expression
   */
  readonly expression?: string;

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints#objectSelector
   */
  readonly objectSelector?: MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraints#resourceRules
   */
  readonly resourceRules?: MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraints(
  obj: MutatingPolicyV1Alpha1SpecTargetMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    expression: obj.expression,
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema MutatingPolicyV1Alpha1SpecVariables
 */
export interface MutatingPolicyV1Alpha1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema MutatingPolicyV1Alpha1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema MutatingPolicyV1Alpha1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecVariables(
  obj: MutatingPolicyV1Alpha1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema MutatingPolicyV1Alpha1SpecWebhookConfiguration
 */
export interface MutatingPolicyV1Alpha1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema MutatingPolicyV1Alpha1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecWebhookConfiguration(
  obj: MutatingPolicyV1Alpha1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
 *
 * @schema MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy
 */
export interface MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy(
  obj: MutatingPolicyV1Alpha1SpecAutogenMutatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema MutatingPolicyV1Alpha1SpecAutogenPodControllers
 */
export interface MutatingPolicyV1Alpha1SpecAutogenPodControllers {
  /**
   * @schema MutatingPolicyV1Alpha1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecAutogenPodControllers(
  obj: MutatingPolicyV1Alpha1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema MutatingPolicyV1Alpha1SpecEvaluationAdmission
 */
export interface MutatingPolicyV1Alpha1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema MutatingPolicyV1Alpha1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecEvaluationAdmission(
  obj: MutatingPolicyV1Alpha1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background controls policy evaluation during background scan.
 *
 * @schema MutatingPolicyV1Alpha1SpecEvaluationBackground
 */
export interface MutatingPolicyV1Alpha1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema MutatingPolicyV1Alpha1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecEvaluationBackground(
  obj: MutatingPolicyV1Alpha1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutateExisting controls whether existing resources are mutated.
 *
 * @schema MutatingPolicyV1Alpha1SpecEvaluationMutateExisting
 */
export interface MutatingPolicyV1Alpha1SpecEvaluationMutateExisting {
  /**
   * Enabled enables mutation of existing resources. Default is false.
   * When spec.targetMatchConstraints is not defined, Kyverno mutates existing resources matched in spec.matchConstraints.
   *
   * @default false.
   * @schema MutatingPolicyV1Alpha1SpecEvaluationMutateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecEvaluationMutateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecEvaluationMutateExisting(
  obj: MutatingPolicyV1Alpha1SpecEvaluationMutateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | MutatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
  obj: MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
  obj: MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules(
  obj: MutatingPolicyV1Alpha1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * applyConfiguration defines the desired configuration values of an object.
 * The configuration is applied to the admission object using
 * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
 * A CEL expression is used to create apply configuration.
 *
 * @schema MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration
 */
export interface MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration {
  /**
   * expression will be evaluated by CEL to create an apply configuration.
   * ref: https://github.com/google/cel-spec
   *
   * Apply configurations are declared in CEL using object initialization. For example, this CEL expression
   * returns an apply configuration to set a single field:
   *
   * Object{
   * spec: Object.spec{
   * serviceAccountName: "example"
   * }
   * }
   *
   * Apply configurations may not modify atomic structs, maps or arrays due to the risk of accidental deletion of
   * values not included in the apply configuration.
   *
   * CEL expressions have access to the object types needed to create apply configurations:
   *
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration(
  obj: MutatingPolicyV1Alpha1SpecMutationsApplyConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
 * A CEL expression is used to create the JSON patch.
 *
 * @schema MutatingPolicyV1Alpha1SpecMutationsJsonPatch
 */
export interface MutatingPolicyV1Alpha1SpecMutationsJsonPatch {
  /**
   * expression will be evaluated by CEL to create a [JSON patch](https://jsonpatch.com/).
   * ref: https://github.com/google/cel-spec
   *
   * expression must return an array of JSONPatch values.
   *
   * For example, this CEL expression returns a JSON patch to conditionally modify a value:
   *
   * [
   * JSONPatch{op: "test", path: "/spec/example", value: "Red"},
   * JSONPatch{op: "replace", path: "/spec/example", value: "Green"}
   * ]
   *
   * To define an object for the patch value, use Object types. For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/spec/selector",
   * value: Object.spec.selector{matchLabels: {"environment": "test"}}
   * }
   * ]
   *
   * To use strings containing '/' and '~' as JSONPatch path keys, use "jsonpatch.escapeKey". For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/metadata/labels/" + jsonpatch.escapeKey("example.com/environment"),
   * value: "test"
   * },
   * ]
   *
   * CEL expressions have access to the types needed to create JSON patches and objects:
   *
   * - 'JSONPatch' - CEL type of JSON Patch operations. JSONPatch has the fields 'op', 'from', 'path' and 'value'.
   * See [JSON patch](https://jsonpatch.com/) for more details. The 'value' field may be set to any of: string,
   * integer, array, map or object.  If set, the 'path' and 'from' fields must be set to a
   * [JSON pointer](https://datatracker.ietf.org/doc/html/rfc6901/) string, where the 'jsonpatch.escapeKey()' CEL
   * function may be used to escape path keys containing '/' and '~'.
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * CEL expressions have access to [Kubernetes CEL function libraries](https://kubernetes.io/docs/reference/using-api/cel/#cel-options-language-features-and-libraries)
   * as well as:
   *
   * - 'jsonpatch.escapeKey' - Performs JSONPatch key escaping. '~' and  '/' are escaped as '~0' and `~1' respectively).
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecMutationsJsonPatch#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMutationsJsonPatch' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMutationsJsonPatch(
  obj: MutatingPolicyV1Alpha1SpecMutationsJsonPatch | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules(
  obj:
    | MutatingPolicyV1Alpha1SpecTargetMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector(
  obj:
    | MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector(
  obj:
    | MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules(
  obj:
    | MutatingPolicyV1Alpha1SpecTargetMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Alpha1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions
 */
export interface MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Alpha1SpecTargetMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema MutatingPolicyV1Beta1
 */
export class MutatingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "MutatingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "MutatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "MutatingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: MutatingPolicyV1Beta1Props): any {
    return {
      ...MutatingPolicyV1Beta1.GVK,
      ...toJson_MutatingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "MutatingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: MutatingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...MutatingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...MutatingPolicyV1Beta1.GVK,
      ...toJson_MutatingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema MutatingPolicyV1Beta1
 */
export interface MutatingPolicyV1Beta1Props {
  /**
   * @schema MutatingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
   *
   * @schema MutatingPolicyV1Beta1#spec
   */
  readonly spec: MutatingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1Props(
  obj: MutatingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_MutatingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
 *
 * @schema MutatingPolicyV1Beta1Spec
 */
export interface MutatingPolicyV1Beta1Spec {
  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema MutatingPolicyV1Beta1Spec#autogen
   */
  readonly autogen?: MutatingPolicyV1Beta1SpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for mutating policy evaluation.
   *
   * @schema MutatingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: MutatingPolicyV1Beta1SpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema MutatingPolicyV1Beta1Spec#failurePolicy
   */
  readonly failurePolicy?: MutatingPolicyV1Beta1SpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema MutatingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: MutatingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to evaluate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: MutatingPolicyV1Beta1SpecMatchConstraints;

  /**
   * mutations contain operations to perform on matching objects.
   * mutations may not be empty; a minimum of one mutation is required.
   * mutations are evaluated in order, and are reinvoked according to
   * the reinvocationPolicy.
   * The mutations of a policy are invoked for each binding of this policy
   * and reinvocation of mutations occurs on a per binding basis.
   *
   * @schema MutatingPolicyV1Beta1Spec#mutations
   */
  readonly mutations?: MutatingPolicyV1Beta1SpecMutations[];

  /**
   * reinvocationPolicy indicates whether mutations may be called multiple times per MutatingAdmissionPolicyBinding
   * as part of a single admission evaluation.
   * Allowed values are "Never" and "IfNeeded".
   *
   * Never: These mutations will not be called more than once per binding in a single admission evaluation.
   *
   * IfNeeded: These mutations may be invoked more than once per binding for a single admission request and there is no guarantee of
   * order with respect to other admission plugins, admission webhooks, bindings of this policy and admission policies.  Mutations are only
   * reinvoked when mutations change the object after this mutation is invoked.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1Spec#reinvocationPolicy
   */
  readonly reinvocationPolicy?: string;

  /**
   * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
   *
   * @schema MutatingPolicyV1Beta1Spec#targetMatchConstraints
   */
  readonly targetMatchConstraints?: MutatingPolicyV1Beta1SpecTargetMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema MutatingPolicyV1Beta1Spec#variables
   */
  readonly variables?: MutatingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema MutatingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: MutatingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1Spec(
  obj: MutatingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    autogen: toJson_MutatingPolicyV1Beta1SpecAutogen(obj.autogen),
    evaluation: toJson_MutatingPolicyV1Beta1SpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_MutatingPolicyV1Beta1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    mutations: obj.mutations?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecMutations(y),
    ),
    reinvocationPolicy: obj.reinvocationPolicy,
    targetMatchConstraints:
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraints(
        obj.targetMatchConstraints,
      ),
    variables: obj.variables?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration: toJson_MutatingPolicyV1Beta1SpecWebhookConfiguration(
      obj.webhookConfiguration,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema MutatingPolicyV1Beta1SpecAutogen
 */
export interface MutatingPolicyV1Beta1SpecAutogen {
  /**
   * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   *
   * @schema MutatingPolicyV1Beta1SpecAutogen#mutatingAdmissionPolicy
   */
  readonly mutatingAdmissionPolicy?: MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy;

  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema MutatingPolicyV1Beta1SpecAutogen#podControllers
   */
  readonly podControllers?: MutatingPolicyV1Beta1SpecAutogenPodControllers;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecAutogen(
  obj: MutatingPolicyV1Beta1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutatingAdmissionPolicy:
      toJson_MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy(
        obj.mutatingAdmissionPolicy,
      ),
    podControllers: toJson_MutatingPolicyV1Beta1SpecAutogenPodControllers(
      obj.podControllers,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for mutating policy evaluation.
 *
 * @schema MutatingPolicyV1Beta1SpecEvaluation
 */
export interface MutatingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema MutatingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: MutatingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * Background controls policy evaluation during background scan.
   *
   * @schema MutatingPolicyV1Beta1SpecEvaluation#background
   */
  readonly background?: MutatingPolicyV1Beta1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema MutatingPolicyV1Beta1SpecEvaluation#mode
   */
  readonly mode?: string;

  /**
   * MutateExisting controls whether existing resources are mutated.
   *
   * @schema MutatingPolicyV1Beta1SpecEvaluation#mutateExisting
   */
  readonly mutateExisting?: MutatingPolicyV1Beta1SpecEvaluationMutateExisting;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecEvaluation(
  obj: MutatingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_MutatingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_MutatingPolicyV1Beta1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
    mutateExisting: toJson_MutatingPolicyV1Beta1SpecEvaluationMutateExisting(
      obj.mutateExisting,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema MutatingPolicyV1Beta1SpecFailurePolicy
 */
export enum MutatingPolicyV1Beta1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema MutatingPolicyV1Beta1SpecMatchConditions
 */
export interface MutatingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConditions(
  obj: MutatingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to evaluate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema MutatingPolicyV1Beta1SpecMatchConstraints
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema MutatingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraints(
  obj: MutatingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Mutation specifies the CEL expression which is used to apply the Mutation.
 *
 * @schema MutatingPolicyV1Beta1SpecMutations
 */
export interface MutatingPolicyV1Beta1SpecMutations {
  /**
   * applyConfiguration defines the desired configuration values of an object.
   * The configuration is applied to the admission object using
   * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
   * A CEL expression is used to create apply configuration.
   *
   * @schema MutatingPolicyV1Beta1SpecMutations#applyConfiguration
   */
  readonly applyConfiguration?: MutatingPolicyV1Beta1SpecMutationsApplyConfiguration;

  /**
   * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
   * A CEL expression is used to create the JSON patch.
   *
   * @schema MutatingPolicyV1Beta1SpecMutations#jsonPatch
   */
  readonly jsonPatch?: MutatingPolicyV1Beta1SpecMutationsJsonPatch;

  /**
   * patchType indicates the patch strategy used.
   * Allowed values are "ApplyConfiguration" and "JSONPatch".
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMutations#patchType
   */
  readonly patchType: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMutations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMutations(
  obj: MutatingPolicyV1Beta1SpecMutations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    applyConfiguration:
      toJson_MutatingPolicyV1Beta1SpecMutationsApplyConfiguration(
        obj.applyConfiguration,
      ),
    jsonPatch: toJson_MutatingPolicyV1Beta1SpecMutationsJsonPatch(
      obj.jsonPatch,
    ),
    patchType: obj.patchType,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
 *
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules[];

  /**
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints#expression
   */
  readonly expression?: string;

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints#objectSelector
   */
  readonly objectSelector?: MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraints#resourceRules
   */
  readonly resourceRules?: MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraints(
  obj: MutatingPolicyV1Beta1SpecTargetMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    expression: obj.expression,
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema MutatingPolicyV1Beta1SpecVariables
 */
export interface MutatingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema MutatingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema MutatingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecVariables(
  obj: MutatingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema MutatingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface MutatingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema MutatingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecWebhookConfiguration(
  obj: MutatingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
 *
 * @schema MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy
 */
export interface MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy(
  obj: MutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema MutatingPolicyV1Beta1SpecAutogenPodControllers
 */
export interface MutatingPolicyV1Beta1SpecAutogenPodControllers {
  /**
   * @schema MutatingPolicyV1Beta1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecAutogenPodControllers(
  obj: MutatingPolicyV1Beta1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema MutatingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface MutatingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema MutatingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecEvaluationAdmission(
  obj: MutatingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background controls policy evaluation during background scan.
 *
 * @schema MutatingPolicyV1Beta1SpecEvaluationBackground
 */
export interface MutatingPolicyV1Beta1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema MutatingPolicyV1Beta1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecEvaluationBackground(
  obj: MutatingPolicyV1Beta1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutateExisting controls whether existing resources are mutated.
 *
 * @schema MutatingPolicyV1Beta1SpecEvaluationMutateExisting
 */
export interface MutatingPolicyV1Beta1SpecEvaluationMutateExisting {
  /**
   * Enabled enables mutation of existing resources. Default is false.
   * When spec.targetMatchConstraints is not defined, Kyverno mutates existing resources matched in spec.matchConstraints.
   *
   * @default false.
   * @schema MutatingPolicyV1Beta1SpecEvaluationMutateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecEvaluationMutateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecEvaluationMutateExisting(
  obj: MutatingPolicyV1Beta1SpecEvaluationMutateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | MutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj: MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj: MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj: MutatingPolicyV1Beta1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * applyConfiguration defines the desired configuration values of an object.
 * The configuration is applied to the admission object using
 * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
 * A CEL expression is used to create apply configuration.
 *
 * @schema MutatingPolicyV1Beta1SpecMutationsApplyConfiguration
 */
export interface MutatingPolicyV1Beta1SpecMutationsApplyConfiguration {
  /**
   * expression will be evaluated by CEL to create an apply configuration.
   * ref: https://github.com/google/cel-spec
   *
   * Apply configurations are declared in CEL using object initialization. For example, this CEL expression
   * returns an apply configuration to set a single field:
   *
   * Object{
   * spec: Object.spec{
   * serviceAccountName: "example"
   * }
   * }
   *
   * Apply configurations may not modify atomic structs, maps or arrays due to the risk of accidental deletion of
   * values not included in the apply configuration.
   *
   * CEL expressions have access to the object types needed to create apply configurations:
   *
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMutationsApplyConfiguration#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMutationsApplyConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMutationsApplyConfiguration(
  obj: MutatingPolicyV1Beta1SpecMutationsApplyConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
 * A CEL expression is used to create the JSON patch.
 *
 * @schema MutatingPolicyV1Beta1SpecMutationsJsonPatch
 */
export interface MutatingPolicyV1Beta1SpecMutationsJsonPatch {
  /**
   * expression will be evaluated by CEL to create a [JSON patch](https://jsonpatch.com/).
   * ref: https://github.com/google/cel-spec
   *
   * expression must return an array of JSONPatch values.
   *
   * For example, this CEL expression returns a JSON patch to conditionally modify a value:
   *
   * [
   * JSONPatch{op: "test", path: "/spec/example", value: "Red"},
   * JSONPatch{op: "replace", path: "/spec/example", value: "Green"}
   * ]
   *
   * To define an object for the patch value, use Object types. For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/spec/selector",
   * value: Object.spec.selector{matchLabels: {"environment": "test"}}
   * }
   * ]
   *
   * To use strings containing '/' and '~' as JSONPatch path keys, use "jsonpatch.escapeKey". For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/metadata/labels/" + jsonpatch.escapeKey("example.com/environment"),
   * value: "test"
   * },
   * ]
   *
   * CEL expressions have access to the types needed to create JSON patches and objects:
   *
   * - 'JSONPatch' - CEL type of JSON Patch operations. JSONPatch has the fields 'op', 'from', 'path' and 'value'.
   * See [JSON patch](https://jsonpatch.com/) for more details. The 'value' field may be set to any of: string,
   * integer, array, map or object.  If set, the 'path' and 'from' fields must be set to a
   * [JSON pointer](https://datatracker.ietf.org/doc/html/rfc6901/) string, where the 'jsonpatch.escapeKey()' CEL
   * function may be used to escape path keys containing '/' and '~'.
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * CEL expressions have access to [Kubernetes CEL function libraries](https://kubernetes.io/docs/reference/using-api/cel/#cel-options-language-features-and-libraries)
   * as well as:
   *
   * - 'jsonpatch.escapeKey' - Performs JSONPatch key escaping. '~' and  '/' are escaped as '~0' and `~1' respectively).
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecMutationsJsonPatch#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMutationsJsonPatch' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMutationsJsonPatch(
  obj: MutatingPolicyV1Beta1SpecMutationsJsonPatch | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules(
  obj:
    | MutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector(
  obj:
    | MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector(
  obj:
    | MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules(
  obj: MutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions
 */
export interface MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | MutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedDeletingPolicy
 */
export class NamespacedDeletingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedDeletingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "NamespacedDeletingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedDeletingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedDeletingPolicyProps): any {
    return {
      ...NamespacedDeletingPolicy.GVK,
      ...toJson_NamespacedDeletingPolicyProps(props),
    };
  }

  /**
   * Defines a "NamespacedDeletingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedDeletingPolicyProps,
  ) {
    super(scope, id, {
      ...NamespacedDeletingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedDeletingPolicy.GVK,
      ...toJson_NamespacedDeletingPolicyProps(resolved),
    };
  }
}

/**
 * @schema NamespacedDeletingPolicy
 */
export interface NamespacedDeletingPolicyProps {
  /**
   * @schema NamespacedDeletingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
   *
   * @schema NamespacedDeletingPolicy#spec
   */
  readonly spec: NamespacedDeletingPolicySpec;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyProps(
  obj: NamespacedDeletingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedDeletingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
 *
 * @schema NamespacedDeletingPolicySpec
 */
export interface NamespacedDeletingPolicySpec {
  /**
   * Conditions is a list of conditions that must be met for a resource to be deleted.
   * Conditions filter resources that have already been matched by the match constraints,
   * namespaceSelector, and objectSelector. An empty list of conditions matches all resources.
   * There are a maximum of 64 conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY condition evaluates to FALSE, the policy is skipped.
   * 2. If ALL conditions evaluate to TRUE, the policy is executed.
   *
   * @schema NamespacedDeletingPolicySpec#conditions
   */
  readonly conditions?: NamespacedDeletingPolicySpecConditions[];

  /**
   * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
   *
   * @schema NamespacedDeletingPolicySpec#deletionPropagationPolicy
   */
  readonly deletionPropagationPolicy?: NamespacedDeletingPolicySpecDeletionPropagationPolicy;

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: NamespacedDeletingPolicySpecMatchConstraints;

  /**
   * The schedule in Cron format
   * Required.
   *
   * @schema NamespacedDeletingPolicySpec#schedule
   */
  readonly schedule: string;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedDeletingPolicySpec#variables
   */
  readonly variables?: NamespacedDeletingPolicySpecVariables[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpec(
  obj: NamespacedDeletingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    conditions: obj.conditions?.map((y) =>
      toJson_NamespacedDeletingPolicySpecConditions(y),
    ),
    deletionPropagationPolicy: obj.deletionPropagationPolicy,
    matchConstraints: toJson_NamespacedDeletingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    schedule: obj.schedule,
    variables: obj.variables?.map((y) =>
      toJson_NamespacedDeletingPolicySpecVariables(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedDeletingPolicySpecConditions
 */
export interface NamespacedDeletingPolicySpecConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecConditions(
  obj: NamespacedDeletingPolicySpecConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
 *
 * @schema NamespacedDeletingPolicySpecDeletionPropagationPolicy
 */
export enum NamespacedDeletingPolicySpecDeletionPropagationPolicy {
  /** Foreground */
  FOREGROUND = "Foreground",
  /** Background */
  BACKGROUND = "Background",
  /** Orphan */
  ORPHAN = "Orphan",
}

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedDeletingPolicySpecMatchConstraints
 */
export interface NamespacedDeletingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedDeletingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedDeletingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedDeletingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedDeletingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedDeletingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraints(
  obj: NamespacedDeletingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedDeletingPolicySpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedDeletingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedDeletingPolicySpecVariables
 */
export interface NamespacedDeletingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedDeletingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedDeletingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecVariables(
  obj: NamespacedDeletingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedDeletingPolicySpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelector
 */
export interface NamespacedDeletingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraintsObjectSelector(
  obj: NamespacedDeletingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules
 */
export interface NamespacedDeletingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedDeletingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraintsResourceRules(
  obj: NamespacedDeletingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedDeletingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedDeletingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedDeletingPolicyV1Beta1
 */
export class NamespacedDeletingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedDeletingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "NamespacedDeletingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedDeletingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedDeletingPolicyV1Beta1Props): any {
    return {
      ...NamespacedDeletingPolicyV1Beta1.GVK,
      ...toJson_NamespacedDeletingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "NamespacedDeletingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedDeletingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...NamespacedDeletingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedDeletingPolicyV1Beta1.GVK,
      ...toJson_NamespacedDeletingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema NamespacedDeletingPolicyV1Beta1
 */
export interface NamespacedDeletingPolicyV1Beta1Props {
  /**
   * @schema NamespacedDeletingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
   *
   * @schema NamespacedDeletingPolicyV1Beta1#spec
   */
  readonly spec: NamespacedDeletingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1Props(
  obj: NamespacedDeletingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedDeletingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletingPolicySpec is the specification of the desired behavior of the DeletingPolicy.
 *
 * @schema NamespacedDeletingPolicyV1Beta1Spec
 */
export interface NamespacedDeletingPolicyV1Beta1Spec {
  /**
   * Conditions is a list of conditions that must be met for a resource to be deleted.
   * Conditions filter resources that have already been matched by the match constraints,
   * namespaceSelector, and objectSelector. An empty list of conditions matches all resources.
   * There are a maximum of 64 conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY condition evaluates to FALSE, the policy is skipped.
   * 2. If ALL conditions evaluate to TRUE, the policy is executed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1Spec#conditions
   */
  readonly conditions?: NamespacedDeletingPolicyV1Beta1SpecConditions[];

  /**
   * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
   *
   * @schema NamespacedDeletingPolicyV1Beta1Spec#deletionPropagationPolicy
   */
  readonly deletionPropagationPolicy?: NamespacedDeletingPolicyV1Beta1SpecDeletionPropagationPolicy;

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraints;

  /**
   * The schedule in Cron format
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1Spec#schedule
   */
  readonly schedule: string;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedDeletingPolicyV1Beta1Spec#variables
   */
  readonly variables?: NamespacedDeletingPolicyV1Beta1SpecVariables[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1Spec(
  obj: NamespacedDeletingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    conditions: obj.conditions?.map((y) =>
      toJson_NamespacedDeletingPolicyV1Beta1SpecConditions(y),
    ),
    deletionPropagationPolicy: obj.deletionPropagationPolicy,
    matchConstraints:
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraints(
        obj.matchConstraints,
      ),
    schedule: obj.schedule,
    variables: obj.variables?.map((y) =>
      toJson_NamespacedDeletingPolicyV1Beta1SpecVariables(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecConditions
 */
export interface NamespacedDeletingPolicyV1Beta1SpecConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecConditions(
  obj: NamespacedDeletingPolicyV1Beta1SpecConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * DeletionPropagationPolicy defines how resources will be deleted (Foreground, Background, Orphan).
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecDeletionPropagationPolicy
 */
export enum NamespacedDeletingPolicyV1Beta1SpecDeletionPropagationPolicy {
  /** Foreground */
  FOREGROUND = "Foreground",
  /** Background */
  BACKGROUND = "Background",
  /** Orphan */
  ORPHAN = "Orphan",
}

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraints
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraints(
  obj: NamespacedDeletingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecVariables
 */
export interface NamespacedDeletingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecVariables(
  obj: NamespacedDeletingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj:
    | NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj:
    | NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedDeletingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespacedGeneratingPolicy is the namespaced CEL-based generating policy.
 *
 * @schema NamespacedGeneratingPolicy
 */
export class NamespacedGeneratingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedGeneratingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "NamespacedGeneratingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedGeneratingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedGeneratingPolicyProps): any {
    return {
      ...NamespacedGeneratingPolicy.GVK,
      ...toJson_NamespacedGeneratingPolicyProps(props),
    };
  }

  /**
   * Defines a "NamespacedGeneratingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedGeneratingPolicyProps,
  ) {
    super(scope, id, {
      ...NamespacedGeneratingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedGeneratingPolicy.GVK,
      ...toJson_NamespacedGeneratingPolicyProps(resolved),
    };
  }
}

/**
 * NamespacedGeneratingPolicy is the namespaced CEL-based generating policy.
 *
 * @schema NamespacedGeneratingPolicy
 */
export interface NamespacedGeneratingPolicyProps {
  /**
   * @schema NamespacedGeneratingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
   *
   * @schema NamespacedGeneratingPolicy#spec
   */
  readonly spec: NamespacedGeneratingPolicySpec;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyProps(
  obj: NamespacedGeneratingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedGeneratingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
 *
 * @schema NamespacedGeneratingPolicySpec
 */
export interface NamespacedGeneratingPolicySpec {
  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema NamespacedGeneratingPolicySpec#evaluation
   */
  readonly evaluation?: NamespacedGeneratingPolicySpecEvaluation;

  /**
   * Generation defines a set of CEL expressions that will be evaluated to generate resources.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpec#generate
   */
  readonly generate: NamespacedGeneratingPolicySpecGenerate[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema NamespacedGeneratingPolicySpec#matchConditions
   */
  readonly matchConditions?: NamespacedGeneratingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources will trigger this policy.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: NamespacedGeneratingPolicySpecMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedGeneratingPolicySpec#variables
   */
  readonly variables?: NamespacedGeneratingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedGeneratingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedGeneratingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpec(
  obj: NamespacedGeneratingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    evaluation: toJson_NamespacedGeneratingPolicySpecEvaluation(obj.evaluation),
    generate: obj.generate?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecGenerate(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_NamespacedGeneratingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedGeneratingPolicySpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema NamespacedGeneratingPolicySpecEvaluation
 */
export interface NamespacedGeneratingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedGeneratingPolicySpecEvaluation#admission
   */
  readonly admission?: NamespacedGeneratingPolicySpecEvaluationAdmission;

  /**
   * GenerateExisting defines the configuration for generating resources for existing triggeres.
   *
   * @schema NamespacedGeneratingPolicySpecEvaluation#generateExisting
   */
  readonly generateExisting?: NamespacedGeneratingPolicySpecEvaluationGenerateExisting;

  /**
   * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
   *
   * @schema NamespacedGeneratingPolicySpecEvaluation#orphanDownstreamOnPolicyDelete
   */
  readonly orphanDownstreamOnPolicyDelete?: NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete;

  /**
   * Synchronization defines the configuration for the synchronization of generated resources.
   *
   * @schema NamespacedGeneratingPolicySpecEvaluation#synchronize
   */
  readonly synchronize?: NamespacedGeneratingPolicySpecEvaluationSynchronize;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecEvaluation(
  obj: NamespacedGeneratingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedGeneratingPolicySpecEvaluationAdmission(
      obj.admission,
    ),
    generateExisting:
      toJson_NamespacedGeneratingPolicySpecEvaluationGenerateExisting(
        obj.generateExisting,
      ),
    orphanDownstreamOnPolicyDelete:
      toJson_NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete(
        obj.orphanDownstreamOnPolicyDelete,
      ),
    synchronize: toJson_NamespacedGeneratingPolicySpecEvaluationSynchronize(
      obj.synchronize,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Generation defines the configuration for the generation of resources.
 *
 * @schema NamespacedGeneratingPolicySpecGenerate
 */
export interface NamespacedGeneratingPolicySpecGenerate {
  /**
   * Expression is a CEL expression that takes a list of resources to be generated.
   *
   * @schema NamespacedGeneratingPolicySpecGenerate#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecGenerate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecGenerate(
  obj: NamespacedGeneratingPolicySpecGenerate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedGeneratingPolicySpecMatchConditions
 */
export interface NamespacedGeneratingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConditions(
  obj: NamespacedGeneratingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources will trigger this policy.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedGeneratingPolicySpecMatchConstraints
 */
export interface NamespacedGeneratingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedGeneratingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedGeneratingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedGeneratingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedGeneratingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraints(
  obj: NamespacedGeneratingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedGeneratingPolicySpecVariables
 */
export interface NamespacedGeneratingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedGeneratingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedGeneratingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecVariables(
  obj: NamespacedGeneratingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedGeneratingPolicySpecWebhookConfiguration
 */
export interface NamespacedGeneratingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedGeneratingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecWebhookConfiguration(
  obj: NamespacedGeneratingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedGeneratingPolicySpecEvaluationAdmission
 */
export interface NamespacedGeneratingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedGeneratingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecEvaluationAdmission(
  obj: NamespacedGeneratingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GenerateExisting defines the configuration for generating resources for existing triggeres.
 *
 * @schema NamespacedGeneratingPolicySpecEvaluationGenerateExisting
 */
export interface NamespacedGeneratingPolicySpecEvaluationGenerateExisting {
  /**
   * Enabled controls whether to trigger the policy for existing resources
   * If is set to "true" the policy will be triggered and applied to existing matched resources.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedGeneratingPolicySpecEvaluationGenerateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecEvaluationGenerateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecEvaluationGenerateExisting(
  obj: NamespacedGeneratingPolicySpecEvaluationGenerateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
 *
 * @schema NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete
 */
export interface NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete {
  /**
   * Enabled controls whether generated resources should be deleted when the policy that generated
   * them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete(
  obj:
    | NamespacedGeneratingPolicySpecEvaluationOrphanDownstreamOnPolicyDelete
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Synchronization defines the configuration for the synchronization of generated resources.
 *
 * @schema NamespacedGeneratingPolicySpecEvaluationSynchronize
 */
export interface NamespacedGeneratingPolicySpecEvaluationSynchronize {
  /**
   * Enabled controls if generated resources should be kept in-sync with their source resource.
   * If Synchronize is set to "true" changes to generated resources will be overwritten with resource
   * data from Data or the resource specified in the Clone declaration.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedGeneratingPolicySpecEvaluationSynchronize#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecEvaluationSynchronize' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecEvaluationSynchronize(
  obj: NamespacedGeneratingPolicySpecEvaluationSynchronize | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedGeneratingPolicySpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector
 */
export interface NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector(
  obj: NamespacedGeneratingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules
 */
export interface NamespacedGeneratingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraintsResourceRules(
  obj: NamespacedGeneratingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedGeneratingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedGeneratingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespacedGeneratingPolicy is the namespaced CEL-based generating policy.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1
 */
export class NamespacedGeneratingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedGeneratingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "NamespacedGeneratingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedGeneratingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedGeneratingPolicyV1Beta1Props): any {
    return {
      ...NamespacedGeneratingPolicyV1Beta1.GVK,
      ...toJson_NamespacedGeneratingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "NamespacedGeneratingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedGeneratingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...NamespacedGeneratingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedGeneratingPolicyV1Beta1.GVK,
      ...toJson_NamespacedGeneratingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * NamespacedGeneratingPolicy is the namespaced CEL-based generating policy.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1
 */
export interface NamespacedGeneratingPolicyV1Beta1Props {
  /**
   * @schema NamespacedGeneratingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1#spec
   */
  readonly spec: NamespacedGeneratingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1Props(
  obj: NamespacedGeneratingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedGeneratingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GeneratingPolicySpec is the specification of the desired behavior of the GeneratingPolicy.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1Spec
 */
export interface NamespacedGeneratingPolicyV1Beta1Spec {
  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: NamespacedGeneratingPolicyV1Beta1SpecEvaluation;

  /**
   * Generation defines a set of CEL expressions that will be evaluated to generate resources.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1Spec#generate
   */
  readonly generate: NamespacedGeneratingPolicyV1Beta1SpecGenerate[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema NamespacedGeneratingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: NamespacedGeneratingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources will trigger this policy.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1Spec#variables
   */
  readonly variables?: NamespacedGeneratingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1Spec(
  obj: NamespacedGeneratingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    evaluation: toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluation(
      obj.evaluation,
    ),
    generate: obj.generate?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecGenerate(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints(
        obj.matchConstraints,
      ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluation
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * GenerateExisting defines the configuration for generating resources for existing triggeres.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluation#generateExisting
   */
  readonly generateExisting?: NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting;

  /**
   * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluation#orphanDownstreamOnPolicyDelete
   */
  readonly orphanDownstreamOnPolicyDelete?: NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete;

  /**
   * Synchronization defines the configuration for the synchronization of generated resources.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluation#synchronize
   */
  readonly synchronize?: NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluation(
  obj: NamespacedGeneratingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    generateExisting:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting(
        obj.generateExisting,
      ),
    orphanDownstreamOnPolicyDelete:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete(
        obj.orphanDownstreamOnPolicyDelete,
      ),
    synchronize:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize(
        obj.synchronize,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Generation defines the configuration for the generation of resources.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecGenerate
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecGenerate {
  /**
   * Expression is a CEL expression that takes a list of resources to be generated.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecGenerate#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecGenerate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecGenerate(
  obj: NamespacedGeneratingPolicyV1Beta1SpecGenerate | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConditions
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConditions(
  obj: NamespacedGeneratingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources will trigger this policy.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints(
  obj: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecVariables
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecVariables(
  obj: NamespacedGeneratingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration(
  obj: NamespacedGeneratingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission(
  obj: NamespacedGeneratingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * GenerateExisting defines the configuration for generating resources for existing triggeres.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting {
  /**
   * Enabled controls whether to trigger the policy for existing resources
   * If is set to "true" the policy will be triggered and applied to existing matched resources.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecEvaluationGenerateExisting
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * OrphanDownstreamOnPolicyDelete defines the configuration for orphaning downstream resources on policy delete.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete {
  /**
   * Enabled controls whether generated resources should be deleted when the policy that generated
   * them is deleted with synchronization enabled. This option is only applicable to generate rules of the data type.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecEvaluationOrphanDownstreamOnPolicyDelete
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Synchronization defines the configuration for the synchronization of generated resources.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize {
  /**
   * Enabled controls if generated resources should be kept in-sync with their source resource.
   * If Synchronize is set to "true" changes to generated resources will be overwritten with resource
   * data from Data or the resource specified in the Clone declaration.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize(
  obj: NamespacedGeneratingPolicyV1Beta1SpecEvaluationSynchronize | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedGeneratingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedImageValidatingPolicy
 */
export class NamespacedImageValidatingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedImageValidatingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "NamespacedImageValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedImageValidatingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedImageValidatingPolicyProps): any {
    return {
      ...NamespacedImageValidatingPolicy.GVK,
      ...toJson_NamespacedImageValidatingPolicyProps(props),
    };
  }

  /**
   * Defines a "NamespacedImageValidatingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedImageValidatingPolicyProps,
  ) {
    super(scope, id, {
      ...NamespacedImageValidatingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedImageValidatingPolicy.GVK,
      ...toJson_NamespacedImageValidatingPolicyProps(resolved),
    };
  }
}

/**
 * @schema NamespacedImageValidatingPolicy
 */
export interface NamespacedImageValidatingPolicyProps {
  /**
   * @schema NamespacedImageValidatingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
   *
   * @schema NamespacedImageValidatingPolicy#spec
   */
  readonly spec: NamespacedImageValidatingPolicySpec;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyProps(
  obj: NamespacedImageValidatingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedImageValidatingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
 *
 * @schema NamespacedImageValidatingPolicySpec
 */
export interface NamespacedImageValidatingPolicySpec {
  /**
   * Attestations provides a list of image metadata to verify
   *
   * @schema NamespacedImageValidatingPolicySpec#attestations
   */
  readonly attestations?: NamespacedImageValidatingPolicySpecAttestations[];

  /**
   * Attestors provides a list of trusted authorities.
   *
   * @schema NamespacedImageValidatingPolicySpec#attestors
   */
  readonly attestors: NamespacedImageValidatingPolicySpecAttestors[];

  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema NamespacedImageValidatingPolicySpec#auditAnnotations
   */
  readonly auditAnnotations?: NamespacedImageValidatingPolicySpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema NamespacedImageValidatingPolicySpec#autogen
   */
  readonly autogen?: NamespacedImageValidatingPolicySpecAutogen;

  /**
   * Credentials provides credentials that will be used for authentication with registry.
   *
   * @schema NamespacedImageValidatingPolicySpec#credentials
   */
  readonly credentials?: NamespacedImageValidatingPolicySpecCredentials;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema NamespacedImageValidatingPolicySpec#evaluation
   */
  readonly evaluation?: NamespacedImageValidatingPolicySpecEvaluation;

  /**
   * FailurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * @schema NamespacedImageValidatingPolicySpec#failurePolicy
   */
  readonly failurePolicy?: NamespacedImageValidatingPolicySpecFailurePolicy;

  /**
   * ImageExtractors is a list of CEL expression to extract images from the resource
   *
   * @schema NamespacedImageValidatingPolicySpec#images
   */
  readonly images?: NamespacedImageValidatingPolicySpecImages[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * @schema NamespacedImageValidatingPolicySpec#matchConditions
   */
  readonly matchConditions?: NamespacedImageValidatingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   *
   * @schema NamespacedImageValidatingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: NamespacedImageValidatingPolicySpecMatchConstraints;

  /**
   * MatchImageReferences is a list of Glob and CELExpressions to match images.
   * Any image that matches one of the rules is considered for validation
   * Any image that does not match a rule is skipped, even when they are passed as arguments to
   * image verification functions
   *
   * @schema NamespacedImageValidatingPolicySpec#matchImageReferences
   */
  readonly matchImageReferences?: NamespacedImageValidatingPolicySpecMatchImageReferences[];

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpec#validationActions
   */
  readonly validationActions?: NamespacedImageValidatingPolicySpecValidationActions[];

  /**
   * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
   *
   * @schema NamespacedImageValidatingPolicySpec#validationConfigurations
   */
  readonly validationConfigurations?: NamespacedImageValidatingPolicySpecValidationConfigurations;

  /**
   * Validations contain CEL expressions which is used to apply the image validation checks.
   *
   * @schema NamespacedImageValidatingPolicySpec#validations
   */
  readonly validations: NamespacedImageValidatingPolicySpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   *
   * @schema NamespacedImageValidatingPolicySpec#variables
   */
  readonly variables?: NamespacedImageValidatingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedImageValidatingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedImageValidatingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpec(
  obj: NamespacedImageValidatingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    attestations: obj.attestations?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecAttestations(y),
    ),
    attestors: obj.attestors?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecAttestors(y),
    ),
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecAuditAnnotations(y),
    ),
    autogen: toJson_NamespacedImageValidatingPolicySpecAutogen(obj.autogen),
    credentials: toJson_NamespacedImageValidatingPolicySpecCredentials(
      obj.credentials,
    ),
    evaluation: toJson_NamespacedImageValidatingPolicySpecEvaluation(
      obj.evaluation,
    ),
    failurePolicy: obj.failurePolicy,
    images: obj.images?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecImages(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecMatchConditions(y),
    ),
    matchConstraints:
      toJson_NamespacedImageValidatingPolicySpecMatchConstraints(
        obj.matchConstraints,
      ),
    matchImageReferences: obj.matchImageReferences?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecMatchImageReferences(y),
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validationConfigurations:
      toJson_NamespacedImageValidatingPolicySpecValidationConfigurations(
        obj.validationConfigurations,
      ),
    validations: obj.validations?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedImageValidatingPolicySpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestation defines the identification details of the  metadata that has to be verified
 *
 * @schema NamespacedImageValidatingPolicySpecAttestations
 */
export interface NamespacedImageValidatingPolicySpecAttestations {
  /**
   * InToto defines the details of attestation attached using intoto format
   *
   * @schema NamespacedImageValidatingPolicySpecAttestations#intoto
   */
  readonly intoto?: NamespacedImageValidatingPolicySpecAttestationsIntoto;

  /**
   * Name is the name for this attestation. It is used to refer to the attestation in verification
   *
   * @schema NamespacedImageValidatingPolicySpecAttestations#name
   */
  readonly name: string;

  /**
   * Referrer defines the details of attestation attached using OCI 1.1 format
   *
   * @schema NamespacedImageValidatingPolicySpecAttestations#referrer
   */
  readonly referrer?: NamespacedImageValidatingPolicySpecAttestationsReferrer;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestations(
  obj: NamespacedImageValidatingPolicySpecAttestations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    intoto: toJson_NamespacedImageValidatingPolicySpecAttestationsIntoto(
      obj.intoto,
    ),
    name: obj.name,
    referrer: toJson_NamespacedImageValidatingPolicySpecAttestationsReferrer(
      obj.referrer,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestor is an identity that confirms or verifies the authenticity of an image or an attestation
 *
 * @schema NamespacedImageValidatingPolicySpecAttestors
 */
export interface NamespacedImageValidatingPolicySpecAttestors {
  /**
   * Cosign defines attestor configuration for Cosign based signatures
   *
   * @schema NamespacedImageValidatingPolicySpecAttestors#cosign
   */
  readonly cosign?: NamespacedImageValidatingPolicySpecAttestorsCosign;

  /**
   * Name is the name for this attestor. It is used to refer to the attestor in verification
   *
   * @schema NamespacedImageValidatingPolicySpecAttestors#name
   */
  readonly name: string;

  /**
   * Notary defines attestor configuration for Notary based signatures
   *
   * @schema NamespacedImageValidatingPolicySpecAttestors#notary
   */
  readonly notary?: NamespacedImageValidatingPolicySpecAttestorsNotary;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestors' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestors(
  obj: NamespacedImageValidatingPolicySpecAttestors | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cosign: toJson_NamespacedImageValidatingPolicySpecAttestorsCosign(
      obj.cosign,
    ),
    name: obj.name,
    notary: toJson_NamespacedImageValidatingPolicySpecAttestorsNotary(
      obj.notary,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema NamespacedImageValidatingPolicySpecAuditAnnotations
 */
export interface NamespacedImageValidatingPolicySpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAuditAnnotations(
  obj: NamespacedImageValidatingPolicySpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema NamespacedImageValidatingPolicySpecAutogen
 */
export interface NamespacedImageValidatingPolicySpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema NamespacedImageValidatingPolicySpecAutogen#podControllers
   */
  readonly podControllers?: NamespacedImageValidatingPolicySpecAutogenPodControllers;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAutogen(
  obj: NamespacedImageValidatingPolicySpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers:
      toJson_NamespacedImageValidatingPolicySpecAutogenPodControllers(
        obj.podControllers,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Credentials provides credentials that will be used for authentication with registry.
 *
 * @schema NamespacedImageValidatingPolicySpecCredentials
 */
export interface NamespacedImageValidatingPolicySpecCredentials {
  /**
   * AllowInsecureRegistry allows insecure access to a registry.
   *
   * @schema NamespacedImageValidatingPolicySpecCredentials#allowInsecureRegistry
   */
  readonly allowInsecureRegistry?: boolean;

  /**
   * Providers specifies a list of OCI Registry names, whose authentication providers are provided.
   * It can be of one of these values: default,google,azure,amazon,github.
   *
   * @schema NamespacedImageValidatingPolicySpecCredentials#providers
   */
  readonly providers?: NamespacedImageValidatingPolicySpecCredentialsProviders[];

  /**
   * Secrets specifies a list of secrets that are provided for credentials.
   * Secrets must live in the Kyverno namespace.
   *
   * @schema NamespacedImageValidatingPolicySpecCredentials#secrets
   */
  readonly secrets?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecCredentials' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecCredentials(
  obj: NamespacedImageValidatingPolicySpecCredentials | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowInsecureRegistry: obj.allowInsecureRegistry,
    providers: obj.providers?.map((y) => y),
    secrets: obj.secrets?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema NamespacedImageValidatingPolicySpecEvaluation
 */
export interface NamespacedImageValidatingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedImageValidatingPolicySpecEvaluation#admission
   */
  readonly admission?: NamespacedImageValidatingPolicySpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema NamespacedImageValidatingPolicySpecEvaluation#background
   */
  readonly background?: NamespacedImageValidatingPolicySpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema NamespacedImageValidatingPolicySpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecEvaluation(
  obj: NamespacedImageValidatingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedImageValidatingPolicySpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_NamespacedImageValidatingPolicySpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * FailurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * @schema NamespacedImageValidatingPolicySpecFailurePolicy
 */
export enum NamespacedImageValidatingPolicySpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * @schema NamespacedImageValidatingPolicySpecImages
 */
export interface NamespacedImageValidatingPolicySpecImages {
  /**
   * Expression defines CEL expression to extract images from the resource.
   *
   * @schema NamespacedImageValidatingPolicySpecImages#expression
   */
  readonly expression: string;

  /**
   * Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
   *
   * @schema NamespacedImageValidatingPolicySpecImages#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecImages' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecImages(
  obj: NamespacedImageValidatingPolicySpecImages | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedImageValidatingPolicySpecMatchConditions
 */
export interface NamespacedImageValidatingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConditions(
  obj: NamespacedImageValidatingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 *
 * @schema NamespacedImageValidatingPolicySpecMatchConstraints
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedImageValidatingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedImageValidatingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedImageValidatingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraints(
  obj: NamespacedImageValidatingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchImageReference defines a Glob or a CEL expression for matching images
 *
 * @schema NamespacedImageValidatingPolicySpecMatchImageReferences
 */
export interface NamespacedImageValidatingPolicySpecMatchImageReferences {
  /**
   * Expression defines CEL Expressions for matching images
   *
   * @schema NamespacedImageValidatingPolicySpecMatchImageReferences#expression
   */
  readonly expression?: string;

  /**
   * Glob defines a globbing pattern for matching images
   *
   * @schema NamespacedImageValidatingPolicySpecMatchImageReferences#glob
   */
  readonly glob?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchImageReferences' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchImageReferences(
  obj: NamespacedImageValidatingPolicySpecMatchImageReferences | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    glob: obj.glob,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema NamespacedImageValidatingPolicySpecValidationActions
 */
export enum NamespacedImageValidatingPolicySpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
 *
 * @schema NamespacedImageValidatingPolicySpecValidationConfigurations
 */
export interface NamespacedImageValidatingPolicySpecValidationConfigurations {
  /**
   * MutateDigest enables replacement of image tags with digests.
   * Defaults to true.
   *
   * @default true.
   * @schema NamespacedImageValidatingPolicySpecValidationConfigurations#mutateDigest
   */
  readonly mutateDigest?: boolean;

  /**
   * Required validates that images are verified, i.e., have passed a signature or attestation check.
   *
   * @schema NamespacedImageValidatingPolicySpecValidationConfigurations#required
   */
  readonly required?: boolean;

  /**
   * VerifyDigest validates that images have a digest.
   *
   * @schema NamespacedImageValidatingPolicySpecValidationConfigurations#verifyDigest
   */
  readonly verifyDigest?: boolean;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecValidationConfigurations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecValidationConfigurations(
  obj: NamespacedImageValidatingPolicySpecValidationConfigurations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutateDigest: obj.mutateDigest,
    required: obj.required,
    verifyDigest: obj.verifyDigest,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema NamespacedImageValidatingPolicySpecValidations
 */
export interface NamespacedImageValidatingPolicySpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema NamespacedImageValidatingPolicySpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema NamespacedImageValidatingPolicySpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema NamespacedImageValidatingPolicySpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecValidations(
  obj: NamespacedImageValidatingPolicySpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedImageValidatingPolicySpecVariables
 */
export interface NamespacedImageValidatingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedImageValidatingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedImageValidatingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecVariables(
  obj: NamespacedImageValidatingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedImageValidatingPolicySpecWebhookConfiguration
 */
export interface NamespacedImageValidatingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedImageValidatingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecWebhookConfiguration(
  obj: NamespacedImageValidatingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * InToto defines the details of attestation attached using intoto format
 *
 * @schema NamespacedImageValidatingPolicySpecAttestationsIntoto
 */
export interface NamespacedImageValidatingPolicySpecAttestationsIntoto {
  /**
   * Type defines the type of attestation contained within the statement.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestationsIntoto#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestationsIntoto' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestationsIntoto(
  obj: NamespacedImageValidatingPolicySpecAttestationsIntoto | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Referrer defines the details of attestation attached using OCI 1.1 format
 *
 * @schema NamespacedImageValidatingPolicySpecAttestationsReferrer
 */
export interface NamespacedImageValidatingPolicySpecAttestationsReferrer {
  /**
   * Type defines the type of attestation attached to the image.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestationsReferrer#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestationsReferrer' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestationsReferrer(
  obj: NamespacedImageValidatingPolicySpecAttestationsReferrer | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Cosign defines attestor configuration for Cosign based signatures
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosign
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosign {
  /**
   * Annotations are used for image verification.
   * Every specified key-value pair must exist and match in the verified payload.
   * The payload may contain other key-value pairs.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#annotations
   */
  readonly annotations?: { [key: string]: string };

  /**
   * Certificate defines the configuration for local signature verification
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#certificate
   */
  readonly certificate?: NamespacedImageValidatingPolicySpecAttestorsCosignCertificate;

  /**
   * CTLog sets the configuration to verify the authority against a Rekor instance.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#ctlog
   */
  readonly ctlog?: NamespacedImageValidatingPolicySpecAttestorsCosignCtlog;

  /**
   * Key defines the type of key to validate the image.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#key
   */
  readonly key?: NamespacedImageValidatingPolicySpecAttestorsCosignKey;

  /**
   * Keyless sets the configuration to verify the authority against a Fulcio instance.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#keyless
   */
  readonly keyless?: NamespacedImageValidatingPolicySpecAttestorsCosignKeyless;

  /**
   * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#source
   */
  readonly source?: NamespacedImageValidatingPolicySpecAttestorsCosignSource;

  /**
   * TUF defines the configuration to fetch sigstore root
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosign#tuf
   */
  readonly tuf?: NamespacedImageValidatingPolicySpecAttestorsCosignTuf;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosign' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosign(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosign | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    annotations:
      obj.annotations === undefined
        ? undefined
        : Object.entries(obj.annotations).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
    certificate:
      toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCertificate(
        obj.certificate,
      ),
    ctlog: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCtlog(
      obj.ctlog,
    ),
    key: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignKey(obj.key),
    keyless: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignKeyless(
      obj.keyless,
    ),
    source: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignSource(
      obj.source,
    ),
    tuf: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignTuf(obj.tuf),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Notary defines attestor configuration for Notary based signatures
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsNotary
 */
export interface NamespacedImageValidatingPolicySpecAttestorsNotary {
  /**
   * Certs define the cert chain for Notary signature verification
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsNotary#certs
   */
  readonly certs?: NamespacedImageValidatingPolicySpecAttestorsNotaryCerts;

  /**
   * TSACerts define the cert chain for verifying timestamps of notary signature
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsNotary#tsaCerts
   */
  readonly tsaCerts?: NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsNotary' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsNotary(
  obj: NamespacedImageValidatingPolicySpecAttestorsNotary | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    certs: toJson_NamespacedImageValidatingPolicySpecAttestorsNotaryCerts(
      obj.certs,
    ),
    tsaCerts: toJson_NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts(
      obj.tsaCerts,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema NamespacedImageValidatingPolicySpecAutogenPodControllers
 */
export interface NamespacedImageValidatingPolicySpecAutogenPodControllers {
  /**
   * @schema NamespacedImageValidatingPolicySpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAutogenPodControllers(
  obj: NamespacedImageValidatingPolicySpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CredentialsProvidersType provides the list of credential providers required.
 *
 * @schema NamespacedImageValidatingPolicySpecCredentialsProviders
 */
export enum NamespacedImageValidatingPolicySpecCredentialsProviders {
  /** default */
  DEFAULT = "default",
  /** amazon */
  AMAZON = "amazon",
  /** azure */
  AZURE = "azure",
  /** google */
  GOOGLE = "google",
  /** github */
  GITHUB = "github",
}

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedImageValidatingPolicySpecEvaluationAdmission
 */
export interface NamespacedImageValidatingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedImageValidatingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecEvaluationAdmission(
  obj: NamespacedImageValidatingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema NamespacedImageValidatingPolicySpecEvaluationBackground
 */
export interface NamespacedImageValidatingPolicySpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema NamespacedImageValidatingPolicySpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecEvaluationBackground(
  obj: NamespacedImageValidatingPolicySpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedImageValidatingPolicySpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector(
  obj:
    | NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules(
  obj:
    | NamespacedImageValidatingPolicySpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate defines the configuration for local signature verification
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificate
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignCertificate {
  /**
   * Certificate is the to the public certificate for local signature verification.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificate#cert
   */
  readonly cert?: NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert;

  /**
   * CertificateChain is the list of CA certificates in PEM format which will be needed
   * when building the certificate chain for the signing certificate. Must start with the
   * parent intermediate CA certificate of the signing certificate and end with the root certificate
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificate#certChain
   */
  readonly certChain?: NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignCertificate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCertificate(
  obj:
    | NamespacedImageValidatingPolicySpecAttestorsCosignCertificate
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cert: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert(
      obj.cert,
    ),
    certChain:
      toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain(
        obj.certChain,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CTLog sets the configuration to verify the authority against a Rekor instance.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignCtlog {
  /**
   * CTLogPubKey, if set, is used to validate SCTs against a custom source.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog#ctLogPubKey
   */
  readonly ctLogPubKey?: string;

  /**
   * IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate
   * timestamp. Default is false. Set to true if this was opted out during signing.
   *
   * @default false. Set to true if this was opted out during signing.
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog#insecureIgnoreSCT
   */
  readonly insecureIgnoreSct?: boolean;

  /**
   * InsecureIgnoreTlog skips transparency log verification.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog#insecureIgnoreTlog
   */
  readonly insecureIgnoreTlog?: boolean;

  /**
   * RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor.
   * If set, this will be used to validate transparency log signatures from a custom Rekor.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog#rekorPubKey
   */
  readonly rekorPubKey?: string;

  /**
   * TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must
   * contain the root CA certificate. Optionally may contain intermediate CA certificates, and
   * may contain the leaf TSA certificate if not present in the timestamurce.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog#tsaCertChain
   */
  readonly tsaCertChain?: string;

  /**
   * URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCtlog#url
   */
  readonly url?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignCtlog' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCtlog(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosignCtlog | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    ctLogPubKey: obj.ctLogPubKey,
    insecureIgnoreSCT: obj.insecureIgnoreSct,
    insecureIgnoreTlog: obj.insecureIgnoreTlog,
    rekorPubKey: obj.rekorPubKey,
    tsaCertChain: obj.tsaCertChain,
    url: obj.url,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Key defines the type of key to validate the image.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKey
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignKey {
  /**
   * Data contains the inline public key
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKey#data
   */
  readonly data?: string;

  /**
   * Expression is a Expression expression that returns the public key.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKey#expression
   */
  readonly expression?: string;

  /**
   * HashAlgorithm specifues signature algorithm for public keys. Supported values are
   * sha224, sha256, sha384 and sha512. Defaults to sha256.
   *
   * @default sha256.
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKey#hashAlgorithm
   */
  readonly hashAlgorithm?: string;

  /**
   * KMS contains the KMS url of the public key
   * Supported formats differ based on the KMS system used.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKey#kms
   */
  readonly kms?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignKey' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignKey(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosignKey | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    expression: obj.expression,
    hashAlgorithm: obj.hashAlgorithm,
    kms: obj.kms,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Keyless sets the configuration to verify the authority against a Fulcio instance.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeyless
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignKeyless {
  /**
   * Identities sets a list of identities.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeyless#identities
   */
  readonly identities: NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities[];

  /**
   * Roots is an optional set of PEM encoded trusted root certificates.
   * If not provided, the system roots are used.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeyless#roots
   */
  readonly roots?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignKeyless' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignKeyless(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosignKeyless | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    identities: obj.identities?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities(
        y,
      ),
    ),
    roots: obj.roots,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignSource
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignSource {
  /**
   * SignaturePullSecrets is an optional list of references to secrets in the
   * same namespace as the deploying resource for pulling any of the signatures
   * used by this Source.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignSource#PullSecrets
   */
  readonly pullSecrets?: NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets[];

  /**
   * Repository defines the location from where to pull the signature / attestations.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignSource#repository
   */
  readonly repository?: string;

  /**
   * TagPrefix is an optional prefix that signature and attestations have.
   * This is the 'tag based discovery' and in the future once references are
   * fully supported that should likely be the preferred way to handle these.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignSource#tagPrefix
   */
  readonly tagPrefix?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignSource' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignSource(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosignSource | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    PullSecrets: obj.pullSecrets?.map((y) =>
      toJson_NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets(
        y,
      ),
    ),
    repository: obj.repository,
    tagPrefix: obj.tagPrefix,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TUF defines the configuration to fetch sigstore root
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignTuf
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignTuf {
  /**
   * Mirror is the base URL of Sigstore TUF repository
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignTuf#mirror
   */
  readonly mirror?: string;

  /**
   * Root defines the path or data of the trusted root
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignTuf#root
   */
  readonly root?: NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignTuf' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignTuf(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosignTuf | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mirror: obj.mirror,
    root: toJson_NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot(
      obj.root,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certs define the cert chain for Notary signature verification
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsNotaryCerts
 */
export interface NamespacedImageValidatingPolicySpecAttestorsNotaryCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsNotaryCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsNotaryCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsNotaryCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsNotaryCerts(
  obj: NamespacedImageValidatingPolicySpecAttestorsNotaryCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TSACerts define the cert chain for verifying timestamps of notary signature
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts
 */
export interface NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts(
  obj: NamespacedImageValidatingPolicySpecAttestorsNotaryTsaCerts | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedImageValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedImageValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate is the to the public certificate for local signature verification.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert(
  obj:
    | NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCert
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CertificateChain is the list of CA certificates in PEM format which will be needed
 * when building the certificate chain for the signing certificate. Must start with the
 * parent intermediate CA certificate of the signing certificate and end with the root certificate
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain(
  obj:
    | NamespacedImageValidatingPolicySpecAttestorsCosignCertificateCertChain
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Identity may contain the issuer and/or the subject found in the transparency
 * log.
 * Issuer/Subject uses a strict match, while IssuerRegExp and SubjectRegExp
 * apply a regexp for matching.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities {
  /**
   * Issuer defines the issuer for this identity.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities#issuer
   */
  readonly issuer?: string;

  /**
   * IssuerRegExp specifies a regular expression to match the issuer for this identity.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities#issuerRegExp
   */
  readonly issuerRegExp?: string;

  /**
   * Subject defines the subject for this identity.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities#subject
   */
  readonly subject?: string;

  /**
   * SubjectRegExp specifies a regular expression to match the subject for this identity.
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities#subjectRegExp
   */
  readonly subjectRegExp?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities(
  obj:
    | NamespacedImageValidatingPolicySpecAttestorsCosignKeylessIdentities
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    issuer: obj.issuer,
    issuerRegExp: obj.issuerRegExp,
    subject: obj.subject,
    subjectRegExp: obj.subjectRegExp,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * LocalObjectReference contains enough information to let you locate the
 * referenced object inside the same namespace.
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets {
  /**
   * Name of the referent.
   * This field is effectively required, but due to backwards compatibility is
   * allowed to be empty. Instances of this type with an empty value here are
   * almost certainly wrong.
   * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets#name
   */
  readonly name?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets(
  obj:
    | NamespacedImageValidatingPolicySpecAttestorsCosignSourcePullSecrets
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Root defines the path or data of the trusted root
 *
 * @schema NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot
 */
export interface NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot {
  /**
   * Data is the base64 encoded TUF root
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot#data
   */
  readonly data?: string;

  /**
   * Path is the URL or File location of the TUF root
   *
   * @schema NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot#path
   */
  readonly path?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot(
  obj: NamespacedImageValidatingPolicySpecAttestorsCosignTufRoot | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    path: obj.path,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1
 */
export class NamespacedImageValidatingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedImageValidatingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "NamespacedImageValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedImageValidatingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(
    props: NamespacedImageValidatingPolicyV1Beta1Props,
  ): any {
    return {
      ...NamespacedImageValidatingPolicyV1Beta1.GVK,
      ...toJson_NamespacedImageValidatingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "NamespacedImageValidatingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedImageValidatingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...NamespacedImageValidatingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedImageValidatingPolicyV1Beta1.GVK,
      ...toJson_NamespacedImageValidatingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema NamespacedImageValidatingPolicyV1Beta1
 */
export interface NamespacedImageValidatingPolicyV1Beta1Props {
  /**
   * @schema NamespacedImageValidatingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1#spec
   */
  readonly spec: NamespacedImageValidatingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1Props(
  obj: NamespacedImageValidatingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedImageValidatingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ImageValidatingPolicySpec is the specification of the desired behavior of the ImageValidatingPolicy.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1Spec
 */
export interface NamespacedImageValidatingPolicyV1Beta1Spec {
  /**
   * Attestations provides a list of image metadata to verify
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#attestations
   */
  readonly attestations?: NamespacedImageValidatingPolicyV1Beta1SpecAttestations[];

  /**
   * Attestors provides a list of trusted authorities.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#attestors
   */
  readonly attestors: NamespacedImageValidatingPolicyV1Beta1SpecAttestors[];

  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#auditAnnotations
   */
  readonly auditAnnotations?: NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#autogen
   */
  readonly autogen?: NamespacedImageValidatingPolicyV1Beta1SpecAutogen;

  /**
   * Credentials provides credentials that will be used for authentication with registry.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#credentials
   */
  readonly credentials?: NamespacedImageValidatingPolicyV1Beta1SpecCredentials;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: NamespacedImageValidatingPolicyV1Beta1SpecEvaluation;

  /**
   * FailurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#failurePolicy
   */
  readonly failurePolicy?: NamespacedImageValidatingPolicyV1Beta1SpecFailurePolicy;

  /**
   * ImageExtractors is a list of CEL expression to extract images from the resource
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#images
   */
  readonly images?: NamespacedImageValidatingPolicyV1Beta1SpecImages[];

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints;

  /**
   * MatchImageReferences is a list of Glob and CELExpressions to match images.
   * Any image that matches one of the rules is considered for validation
   * Any image that does not match a rule is skipped, even when they are passed as arguments to
   * image verification functions
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#matchImageReferences
   */
  readonly matchImageReferences?: NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences[];

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#validationActions
   */
  readonly validationActions?: NamespacedImageValidatingPolicyV1Beta1SpecValidationActions[];

  /**
   * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#validationConfigurations
   */
  readonly validationConfigurations?: NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations;

  /**
   * Validations contain CEL expressions which is used to apply the image validation checks.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#validations
   */
  readonly validations: NamespacedImageValidatingPolicyV1Beta1SpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#variables
   */
  readonly variables?: NamespacedImageValidatingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1Spec(
  obj: NamespacedImageValidatingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    attestations: obj.attestations?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestations(y),
    ),
    attestors: obj.attestors?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestors(y),
    ),
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations(y),
    ),
    autogen: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAutogen(
      obj.autogen,
    ),
    credentials: toJson_NamespacedImageValidatingPolicyV1Beta1SpecCredentials(
      obj.credentials,
    ),
    evaluation: toJson_NamespacedImageValidatingPolicyV1Beta1SpecEvaluation(
      obj.evaluation,
    ),
    failurePolicy: obj.failurePolicy,
    images: obj.images?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecImages(y),
    ),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints(
        obj.matchConstraints,
      ),
    matchImageReferences: obj.matchImageReferences?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences(y),
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validationConfigurations:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations(
        obj.validationConfigurations,
      ),
    validations: obj.validations?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestation defines the identification details of the  metadata that has to be verified
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestations
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestations {
  /**
   * InToto defines the details of attestation attached using intoto format
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestations#intoto
   */
  readonly intoto?: NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto;

  /**
   * Name is the name for this attestation. It is used to refer to the attestation in verification
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestations#name
   */
  readonly name: string;

  /**
   * Referrer defines the details of attestation attached using OCI 1.1 format
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestations#referrer
   */
  readonly referrer?: NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestations(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    intoto: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto(
      obj.intoto,
    ),
    name: obj.name,
    referrer:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer(
        obj.referrer,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Attestor is an identity that confirms or verifies the authenticity of an image or an attestation
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestors
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestors {
  /**
   * Cosign defines attestor configuration for Cosign based signatures
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestors#cosign
   */
  readonly cosign?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign;

  /**
   * Name is the name for this attestor. It is used to refer to the attestor in verification
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestors#name
   */
  readonly name: string;

  /**
   * Notary defines attestor configuration for Notary based signatures
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestors#notary
   */
  readonly notary?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestors' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestors(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestors | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cosign: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign(
      obj.cosign,
    ),
    name: obj.name,
    notary: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary(
      obj.notary,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAutogen
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAutogen#podControllers
   */
  readonly podControllers?: NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAutogen(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers(
        obj.podControllers,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Credentials provides credentials that will be used for authentication with registry.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecCredentials
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecCredentials {
  /**
   * AllowInsecureRegistry allows insecure access to a registry.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecCredentials#allowInsecureRegistry
   */
  readonly allowInsecureRegistry?: boolean;

  /**
   * Providers specifies a list of OCI Registry names, whose authentication providers are provided.
   * It can be of one of these values: default,google,azure,amazon,github.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecCredentials#providers
   */
  readonly providers?: NamespacedImageValidatingPolicyV1Beta1SpecCredentialsProviders[];

  /**
   * Secrets specifies a list of secrets that are provided for credentials.
   * Secrets must live in the Kyverno namespace.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecCredentials#secrets
   */
  readonly secrets?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecCredentials' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecCredentials(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecCredentials | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowInsecureRegistry: obj.allowInsecureRegistry,
    providers: obj.providers?.map((y) => y),
    secrets: obj.secrets?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluation
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluation#background
   */
  readonly background?: NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecEvaluation(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission(
        obj.admission,
      ),
    background:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground(
        obj.background,
      ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * FailurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecFailurePolicy
 */
export enum NamespacedImageValidatingPolicyV1Beta1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecImages
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecImages {
  /**
   * Expression defines CEL expression to extract images from the resource.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecImages#expression
   */
  readonly expression: string;

  /**
   * Name is the name for this imageList. It is used to refer to the images in verification block as images.<name>
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecImages#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecImages' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecImages(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecImages | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchImageReference defines a Glob or a CEL expression for matching images
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences {
  /**
   * Expression defines CEL Expressions for matching images
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences#expression
   */
  readonly expression?: string;

  /**
   * Glob defines a globbing pattern for matching images
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences#glob
   */
  readonly glob?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchImageReferences
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    glob: obj.glob,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidationActions
 */
export enum NamespacedImageValidatingPolicyV1Beta1SpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * ValidationConfigurations defines settings for mutating and verifying image digests, and enforcing image verification through signatures.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations {
  /**
   * MutateDigest enables replacement of image tags with digests.
   * Defaults to true.
   *
   * @default true.
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations#mutateDigest
   */
  readonly mutateDigest?: boolean;

  /**
   * Required validates that images are verified, i.e., have passed a signature or attestation check.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations#required
   */
  readonly required?: boolean;

  /**
   * VerifyDigest validates that images have a digest.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations#verifyDigest
   */
  readonly verifyDigest?: boolean;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecValidationConfigurations
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutateDigest: obj.mutateDigest,
    required: obj.required,
    verifyDigest: obj.verifyDigest,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidations
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecValidations(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecVariables
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecVariables(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecWebhookConfiguration
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * InToto defines the details of attestation attached using intoto format
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto {
  /**
   * Type defines the type of attestation contained within the statement.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestationsIntoto | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Referrer defines the details of attestation attached using OCI 1.1 format
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer {
  /**
   * Type defines the type of attestation attached to the image.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer#type
   */
  readonly type: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestationsReferrer
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    type: obj.type,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Cosign defines attestor configuration for Cosign based signatures
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign {
  /**
   * Annotations are used for image verification.
   * Every specified key-value pair must exist and match in the verified payload.
   * The payload may contain other key-value pairs.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#annotations
   */
  readonly annotations?: { [key: string]: string };

  /**
   * Certificate defines the configuration for local signature verification
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#certificate
   */
  readonly certificate?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate;

  /**
   * CTLog sets the configuration to verify the authority against a Rekor instance.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#ctlog
   */
  readonly ctlog?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog;

  /**
   * Key defines the type of key to validate the image.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#key
   */
  readonly key?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey;

  /**
   * Keyless sets the configuration to verify the authority against a Fulcio instance.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#keyless
   */
  readonly keyless?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless;

  /**
   * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#source
   */
  readonly source?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource;

  /**
   * TUF defines the configuration to fetch sigstore root
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign#tuf
   */
  readonly tuf?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosign | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    annotations:
      obj.annotations === undefined
        ? undefined
        : Object.entries(obj.annotations).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
    certificate:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate(
        obj.certificate,
      ),
    ctlog:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog(
        obj.ctlog,
      ),
    key: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey(
      obj.key,
    ),
    keyless:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless(
        obj.keyless,
      ),
    source:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource(
        obj.source,
      ),
    tuf: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf(
      obj.tuf,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Notary defines attestor configuration for Notary based signatures
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary {
  /**
   * Certs define the cert chain for Notary signature verification
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary#certs
   */
  readonly certs?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts;

  /**
   * TSACerts define the cert chain for verifying timestamps of notary signature
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary#tsaCerts
   */
  readonly tsaCerts?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotary | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    certs:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts(
        obj.certs,
      ),
    tsaCerts:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts(
        obj.tsaCerts,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers {
  /**
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAutogenPodControllers
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CredentialsProvidersType provides the list of credential providers required.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecCredentialsProviders
 */
export enum NamespacedImageValidatingPolicyV1Beta1SpecCredentialsProviders {
  /** default */
  DEFAULT = "default",
  /** amazon */
  AMAZON = "amazon",
  /** azure */
  AZURE = "azure",
  /** google */
  GOOGLE = "google",
  /** github */
  GITHUB = "github",
}

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecEvaluationAdmission
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecEvaluationBackground
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate defines the configuration for local signature verification
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate {
  /**
   * Certificate is the to the public certificate for local signature verification.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate#cert
   */
  readonly cert?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert;

  /**
   * CertificateChain is the list of CA certificates in PEM format which will be needed
   * when building the certificate chain for the signing certificate. Must start with the
   * parent intermediate CA certificate of the signing certificate and end with the root certificate
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate#certChain
   */
  readonly certChain?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificate
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    cert: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert(
      obj.cert,
    ),
    certChain:
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain(
        obj.certChain,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CTLog sets the configuration to verify the authority against a Rekor instance.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog {
  /**
   * CTLogPubKey, if set, is used to validate SCTs against a custom source.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#ctLogPubKey
   */
  readonly ctLogPubKey?: string;

  /**
   * IgnoreSCT defines whether to use the Signed Certificate Timestamp (SCT) log to check for a certificate
   * timestamp. Default is false. Set to true if this was opted out during signing.
   *
   * @default false. Set to true if this was opted out during signing.
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#insecureIgnoreSCT
   */
  readonly insecureIgnoreSct?: boolean;

  /**
   * InsecureIgnoreTlog skips transparency log verification.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#insecureIgnoreTlog
   */
  readonly insecureIgnoreTlog?: boolean;

  /**
   * RekorPubKey is an optional PEM-encoded public key to use for a custom Rekor.
   * If set, this will be used to validate transparency log signatures from a custom Rekor.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#rekorPubKey
   */
  readonly rekorPubKey?: string;

  /**
   * TSACertChain, if set, is the PEM-encoded certificate chain file for the RFC3161 timestamp authority. Must
   * contain the root CA certificate. Optionally may contain intermediate CA certificates, and
   * may contain the leaf TSA certificate if not present in the timestamurce.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#tsaCertChain
   */
  readonly tsaCertChain?: string;

  /**
   * URL sets the url to the rekor instance (by default the public rekor.sigstore.dev)
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog#url
   */
  readonly url?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCtlog
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    ctLogPubKey: obj.ctLogPubKey,
    insecureIgnoreSCT: obj.insecureIgnoreSct,
    insecureIgnoreTlog: obj.insecureIgnoreTlog,
    rekorPubKey: obj.rekorPubKey,
    tsaCertChain: obj.tsaCertChain,
    url: obj.url,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Key defines the type of key to validate the image.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey {
  /**
   * Data contains the inline public key
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#data
   */
  readonly data?: string;

  /**
   * Expression is a Expression expression that returns the public key.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#expression
   */
  readonly expression?: string;

  /**
   * HashAlgorithm specifues signature algorithm for public keys. Supported values are
   * sha224, sha256, sha384 and sha512. Defaults to sha256.
   *
   * @default sha256.
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#hashAlgorithm
   */
  readonly hashAlgorithm?: string;

  /**
   * KMS contains the KMS url of the public key
   * Supported formats differ based on the KMS system used.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey#kms
   */
  readonly kms?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKey | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    expression: obj.expression,
    hashAlgorithm: obj.hashAlgorithm,
    kms: obj.kms,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Keyless sets the configuration to verify the authority against a Fulcio instance.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless {
  /**
   * Identities sets a list of identities.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless#identities
   */
  readonly identities: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities[];

  /**
   * Roots is an optional set of PEM encoded trusted root certificates.
   * If not provided, the system roots are used.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless#roots
   */
  readonly roots?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeyless
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    identities: obj.identities?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities(
        y,
      ),
    ),
    roots: obj.roots,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Sources sets the configuration to specify the sources from where to consume the signature and attestations.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource {
  /**
   * SignaturePullSecrets is an optional list of references to secrets in the
   * same namespace as the deploying resource for pulling any of the signatures
   * used by this Source.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource#PullSecrets
   */
  readonly pullSecrets?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets[];

  /**
   * Repository defines the location from where to pull the signature / attestations.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource#repository
   */
  readonly repository?: string;

  /**
   * TagPrefix is an optional prefix that signature and attestations have.
   * This is the 'tag based discovery' and in the future once references are
   * fully supported that should likely be the preferred way to handle these.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource#tagPrefix
   */
  readonly tagPrefix?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSource
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    PullSecrets: obj.pullSecrets?.map((y) =>
      toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets(
        y,
      ),
    ),
    repository: obj.repository,
    tagPrefix: obj.tagPrefix,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TUF defines the configuration to fetch sigstore root
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf {
  /**
   * Mirror is the base URL of Sigstore TUF repository
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf#mirror
   */
  readonly mirror?: string;

  /**
   * Root defines the path or data of the trusted root
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf#root
   */
  readonly root?: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf(
  obj: NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTuf | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mirror: obj.mirror,
    root: toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot(
      obj.root,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certs define the cert chain for Notary signature verification
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryCerts
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TSACerts define the cert chain for verifying timestamps of notary signature
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsNotaryTsaCerts
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Certificate is the to the public certificate for local signature verification.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCert
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * CertificateChain is the list of CA certificates in PEM format which will be needed
 * when building the certificate chain for the signing certificate. Must start with the
 * parent intermediate CA certificate of the signing certificate and end with the root certificate
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain {
  /**
   * Expression defines the a CEL expression input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain#expression
   */
  readonly expression?: string;

  /**
   * Value defines the raw string input.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain#value
   */
  readonly value?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignCertificateCertChain
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    value: obj.value,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Identity may contain the issuer and/or the subject found in the transparency
 * log.
 * Issuer/Subject uses a strict match, while IssuerRegExp and SubjectRegExp
 * apply a regexp for matching.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities {
  /**
   * Issuer defines the issuer for this identity.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#issuer
   */
  readonly issuer?: string;

  /**
   * IssuerRegExp specifies a regular expression to match the issuer for this identity.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#issuerRegExp
   */
  readonly issuerRegExp?: string;

  /**
   * Subject defines the subject for this identity.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#subject
   */
  readonly subject?: string;

  /**
   * SubjectRegExp specifies a regular expression to match the subject for this identity.
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities#subjectRegExp
   */
  readonly subjectRegExp?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignKeylessIdentities
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    issuer: obj.issuer,
    issuerRegExp: obj.issuerRegExp,
    subject: obj.subject,
    subjectRegExp: obj.subjectRegExp,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * LocalObjectReference contains enough information to let you locate the
 * referenced object inside the same namespace.
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets {
  /**
   * Name of the referent.
   * This field is effectively required, but due to backwards compatibility is
   * allowed to be empty. Instances of this type with an empty value here are
   * almost certainly wrong.
   * More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets#name
   */
  readonly name?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignSourcePullSecrets
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Root defines the path or data of the trusted root
 *
 * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot
 */
export interface NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot {
  /**
   * Data is the base64 encoded TUF root
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot#data
   */
  readonly data?: string;

  /**
   * Path is the URL or File location of the TUF root
   *
   * @schema NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot#path
   */
  readonly path?: string;
}

/**
 * Converts an object of type 'NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot(
  obj:
    | NamespacedImageValidatingPolicyV1Beta1SpecAttestorsCosignTufRoot
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    data: obj.data,
    path: obj.path,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedMutatingPolicy
 */
export class NamespacedMutatingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedMutatingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "NamespacedMutatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedMutatingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedMutatingPolicyProps): any {
    return {
      ...NamespacedMutatingPolicy.GVK,
      ...toJson_NamespacedMutatingPolicyProps(props),
    };
  }

  /**
   * Defines a "NamespacedMutatingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedMutatingPolicyProps,
  ) {
    super(scope, id, {
      ...NamespacedMutatingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedMutatingPolicy.GVK,
      ...toJson_NamespacedMutatingPolicyProps(resolved),
    };
  }
}

/**
 * @schema NamespacedMutatingPolicy
 */
export interface NamespacedMutatingPolicyProps {
  /**
   * @schema NamespacedMutatingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
   *
   * @schema NamespacedMutatingPolicy#spec
   */
  readonly spec: NamespacedMutatingPolicySpec;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyProps(
  obj: NamespacedMutatingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedMutatingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
 *
 * @schema NamespacedMutatingPolicySpec
 */
export interface NamespacedMutatingPolicySpec {
  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema NamespacedMutatingPolicySpec#autogen
   */
  readonly autogen?: NamespacedMutatingPolicySpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for mutating policy evaluation.
   *
   * @schema NamespacedMutatingPolicySpec#evaluation
   */
  readonly evaluation?: NamespacedMutatingPolicySpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema NamespacedMutatingPolicySpec#failurePolicy
   */
  readonly failurePolicy?: NamespacedMutatingPolicySpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema NamespacedMutatingPolicySpec#matchConditions
   */
  readonly matchConditions?: NamespacedMutatingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to evaluate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: NamespacedMutatingPolicySpecMatchConstraints;

  /**
   * mutations contain operations to perform on matching objects.
   * mutations may not be empty; a minimum of one mutation is required.
   * mutations are evaluated in order, and are reinvoked according to
   * the reinvocationPolicy.
   * The mutations of a policy are invoked for each binding of this policy
   * and reinvocation of mutations occurs on a per binding basis.
   *
   * @schema NamespacedMutatingPolicySpec#mutations
   */
  readonly mutations?: NamespacedMutatingPolicySpecMutations[];

  /**
   * reinvocationPolicy indicates whether mutations may be called multiple times per MutatingAdmissionPolicyBinding
   * as part of a single admission evaluation.
   * Allowed values are "Never" and "IfNeeded".
   *
   * Never: These mutations will not be called more than once per binding in a single admission evaluation.
   *
   * IfNeeded: These mutations may be invoked more than once per binding for a single admission request and there is no guarantee of
   * order with respect to other admission plugins, admission webhooks, bindings of this policy and admission policies.  Mutations are only
   * reinvoked when mutations change the object after this mutation is invoked.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpec#reinvocationPolicy
   */
  readonly reinvocationPolicy?: string;

  /**
   * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
   *
   * @schema NamespacedMutatingPolicySpec#targetMatchConstraints
   */
  readonly targetMatchConstraints?: NamespacedMutatingPolicySpecTargetMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedMutatingPolicySpec#variables
   */
  readonly variables?: NamespacedMutatingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedMutatingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedMutatingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpec(
  obj: NamespacedMutatingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    autogen: toJson_NamespacedMutatingPolicySpecAutogen(obj.autogen),
    evaluation: toJson_NamespacedMutatingPolicySpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedMutatingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_NamespacedMutatingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    mutations: obj.mutations?.map((y) =>
      toJson_NamespacedMutatingPolicySpecMutations(y),
    ),
    reinvocationPolicy: obj.reinvocationPolicy,
    targetMatchConstraints:
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraints(
        obj.targetMatchConstraints,
      ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedMutatingPolicySpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedMutatingPolicySpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema NamespacedMutatingPolicySpecAutogen
 */
export interface NamespacedMutatingPolicySpecAutogen {
  /**
   * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   *
   * @schema NamespacedMutatingPolicySpecAutogen#mutatingAdmissionPolicy
   */
  readonly mutatingAdmissionPolicy?: NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy;

  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema NamespacedMutatingPolicySpecAutogen#podControllers
   */
  readonly podControllers?: NamespacedMutatingPolicySpecAutogenPodControllers;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecAutogen(
  obj: NamespacedMutatingPolicySpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutatingAdmissionPolicy:
      toJson_NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy(
        obj.mutatingAdmissionPolicy,
      ),
    podControllers: toJson_NamespacedMutatingPolicySpecAutogenPodControllers(
      obj.podControllers,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for mutating policy evaluation.
 *
 * @schema NamespacedMutatingPolicySpecEvaluation
 */
export interface NamespacedMutatingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedMutatingPolicySpecEvaluation#admission
   */
  readonly admission?: NamespacedMutatingPolicySpecEvaluationAdmission;

  /**
   * Background controls policy evaluation during background scan.
   *
   * @schema NamespacedMutatingPolicySpecEvaluation#background
   */
  readonly background?: NamespacedMutatingPolicySpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema NamespacedMutatingPolicySpecEvaluation#mode
   */
  readonly mode?: string;

  /**
   * MutateExisting controls whether existing resources are mutated.
   *
   * @schema NamespacedMutatingPolicySpecEvaluation#mutateExisting
   */
  readonly mutateExisting?: NamespacedMutatingPolicySpecEvaluationMutateExisting;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecEvaluation(
  obj: NamespacedMutatingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedMutatingPolicySpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_NamespacedMutatingPolicySpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
    mutateExisting: toJson_NamespacedMutatingPolicySpecEvaluationMutateExisting(
      obj.mutateExisting,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema NamespacedMutatingPolicySpecFailurePolicy
 */
export enum NamespacedMutatingPolicySpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedMutatingPolicySpecMatchConditions
 */
export interface NamespacedMutatingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConditions(
  obj: NamespacedMutatingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to evaluate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedMutatingPolicySpecMatchConstraints
 */
export interface NamespacedMutatingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedMutatingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedMutatingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedMutatingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraints(
  obj: NamespacedMutatingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedMutatingPolicySpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Mutation specifies the CEL expression which is used to apply the Mutation.
 *
 * @schema NamespacedMutatingPolicySpecMutations
 */
export interface NamespacedMutatingPolicySpecMutations {
  /**
   * applyConfiguration defines the desired configuration values of an object.
   * The configuration is applied to the admission object using
   * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
   * A CEL expression is used to create apply configuration.
   *
   * @schema NamespacedMutatingPolicySpecMutations#applyConfiguration
   */
  readonly applyConfiguration?: NamespacedMutatingPolicySpecMutationsApplyConfiguration;

  /**
   * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
   * A CEL expression is used to create the JSON patch.
   *
   * @schema NamespacedMutatingPolicySpecMutations#jsonPatch
   */
  readonly jsonPatch?: NamespacedMutatingPolicySpecMutationsJsonPatch;

  /**
   * patchType indicates the patch strategy used.
   * Allowed values are "ApplyConfiguration" and "JSONPatch".
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMutations#patchType
   */
  readonly patchType: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMutations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMutations(
  obj: NamespacedMutatingPolicySpecMutations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    applyConfiguration:
      toJson_NamespacedMutatingPolicySpecMutationsApplyConfiguration(
        obj.applyConfiguration,
      ),
    jsonPatch: toJson_NamespacedMutatingPolicySpecMutationsJsonPatch(
      obj.jsonPatch,
    ),
    patchType: obj.patchType,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
 *
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraints
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules[];

  /**
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraints#expression
   */
  readonly expression?: string;

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraints(
  obj: NamespacedMutatingPolicySpecTargetMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    expression: obj.expression,
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedMutatingPolicySpecVariables
 */
export interface NamespacedMutatingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedMutatingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedMutatingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecVariables(
  obj: NamespacedMutatingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedMutatingPolicySpecWebhookConfiguration
 */
export interface NamespacedMutatingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedMutatingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecWebhookConfiguration(
  obj: NamespacedMutatingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
 *
 * @schema NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy
 */
export interface NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy(
  obj: NamespacedMutatingPolicySpecAutogenMutatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema NamespacedMutatingPolicySpecAutogenPodControllers
 */
export interface NamespacedMutatingPolicySpecAutogenPodControllers {
  /**
   * @schema NamespacedMutatingPolicySpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecAutogenPodControllers(
  obj: NamespacedMutatingPolicySpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedMutatingPolicySpecEvaluationAdmission
 */
export interface NamespacedMutatingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedMutatingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecEvaluationAdmission(
  obj: NamespacedMutatingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background controls policy evaluation during background scan.
 *
 * @schema NamespacedMutatingPolicySpecEvaluationBackground
 */
export interface NamespacedMutatingPolicySpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema NamespacedMutatingPolicySpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecEvaluationBackground(
  obj: NamespacedMutatingPolicySpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutateExisting controls whether existing resources are mutated.
 *
 * @schema NamespacedMutatingPolicySpecEvaluationMutateExisting
 */
export interface NamespacedMutatingPolicySpecEvaluationMutateExisting {
  /**
   * Enabled enables mutation of existing resources. Default is false.
   * When spec.targetMatchConstraints is not defined, Kyverno mutates existing resources matched in spec.matchConstraints.
   *
   * @default false.
   * @schema NamespacedMutatingPolicySpecEvaluationMutateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecEvaluationMutateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecEvaluationMutateExisting(
  obj: NamespacedMutatingPolicySpecEvaluationMutateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedMutatingPolicySpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelector
 */
export interface NamespacedMutatingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraintsObjectSelector(
  obj: NamespacedMutatingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules
 */
export interface NamespacedMutatingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraintsResourceRules(
  obj: NamespacedMutatingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * applyConfiguration defines the desired configuration values of an object.
 * The configuration is applied to the admission object using
 * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
 * A CEL expression is used to create apply configuration.
 *
 * @schema NamespacedMutatingPolicySpecMutationsApplyConfiguration
 */
export interface NamespacedMutatingPolicySpecMutationsApplyConfiguration {
  /**
   * expression will be evaluated by CEL to create an apply configuration.
   * ref: https://github.com/google/cel-spec
   *
   * Apply configurations are declared in CEL using object initialization. For example, this CEL expression
   * returns an apply configuration to set a single field:
   *
   * Object{
   * spec: Object.spec{
   * serviceAccountName: "example"
   * }
   * }
   *
   * Apply configurations may not modify atomic structs, maps or arrays due to the risk of accidental deletion of
   * values not included in the apply configuration.
   *
   * CEL expressions have access to the object types needed to create apply configurations:
   *
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMutationsApplyConfiguration#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMutationsApplyConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMutationsApplyConfiguration(
  obj: NamespacedMutatingPolicySpecMutationsApplyConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
 * A CEL expression is used to create the JSON patch.
 *
 * @schema NamespacedMutatingPolicySpecMutationsJsonPatch
 */
export interface NamespacedMutatingPolicySpecMutationsJsonPatch {
  /**
   * expression will be evaluated by CEL to create a [JSON patch](https://jsonpatch.com/).
   * ref: https://github.com/google/cel-spec
   *
   * expression must return an array of JSONPatch values.
   *
   * For example, this CEL expression returns a JSON patch to conditionally modify a value:
   *
   * [
   * JSONPatch{op: "test", path: "/spec/example", value: "Red"},
   * JSONPatch{op: "replace", path: "/spec/example", value: "Green"}
   * ]
   *
   * To define an object for the patch value, use Object types. For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/spec/selector",
   * value: Object.spec.selector{matchLabels: {"environment": "test"}}
   * }
   * ]
   *
   * To use strings containing '/' and '~' as JSONPatch path keys, use "jsonpatch.escapeKey". For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/metadata/labels/" + jsonpatch.escapeKey("example.com/environment"),
   * value: "test"
   * },
   * ]
   *
   * CEL expressions have access to the types needed to create JSON patches and objects:
   *
   * - 'JSONPatch' - CEL type of JSON Patch operations. JSONPatch has the fields 'op', 'from', 'path' and 'value'.
   * See [JSON patch](https://jsonpatch.com/) for more details. The 'value' field may be set to any of: string,
   * integer, array, map or object.  If set, the 'path' and 'from' fields must be set to a
   * [JSON pointer](https://datatracker.ietf.org/doc/html/rfc6901/) string, where the 'jsonpatch.escapeKey()' CEL
   * function may be used to escape path keys containing '/' and '~'.
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * CEL expressions have access to [Kubernetes CEL function libraries](https://kubernetes.io/docs/reference/using-api/cel/#cel-options-language-features-and-libraries)
   * as well as:
   *
   * - 'jsonpatch.escapeKey' - Performs JSONPatch key escaping. '~' and  '/' are escaped as '~0' and `~1' respectively).
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecMutationsJsonPatch#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMutationsJsonPatch' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMutationsJsonPatch(
  obj: NamespacedMutatingPolicySpecMutationsJsonPatch | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedMutatingPolicySpecTargetMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector(
  obj:
    | NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules(
  obj:
    | NamespacedMutatingPolicySpecTargetMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicySpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicySpecTargetMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedMutatingPolicyV1Beta1
 */
export class NamespacedMutatingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedMutatingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "NamespacedMutatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedMutatingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedMutatingPolicyV1Beta1Props): any {
    return {
      ...NamespacedMutatingPolicyV1Beta1.GVK,
      ...toJson_NamespacedMutatingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "NamespacedMutatingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedMutatingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...NamespacedMutatingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedMutatingPolicyV1Beta1.GVK,
      ...toJson_NamespacedMutatingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema NamespacedMutatingPolicyV1Beta1
 */
export interface NamespacedMutatingPolicyV1Beta1Props {
  /**
   * @schema NamespacedMutatingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
   *
   * @schema NamespacedMutatingPolicyV1Beta1#spec
   */
  readonly spec: NamespacedMutatingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1Props(
  obj: NamespacedMutatingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedMutatingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingPolicySpec is the specification of the desired behavior of the MutatingPolicy.
 *
 * @schema NamespacedMutatingPolicyV1Beta1Spec
 */
export interface NamespacedMutatingPolicyV1Beta1Spec {
  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#autogen
   */
  readonly autogen?: NamespacedMutatingPolicyV1Beta1SpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for mutating policy evaluation.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: NamespacedMutatingPolicyV1Beta1SpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema NamespacedMutatingPolicyV1Beta1Spec#failurePolicy
   */
  readonly failurePolicy?: NamespacedMutatingPolicyV1Beta1SpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: NamespacedMutatingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to evaluate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraints;

  /**
   * mutations contain operations to perform on matching objects.
   * mutations may not be empty; a minimum of one mutation is required.
   * mutations are evaluated in order, and are reinvoked according to
   * the reinvocationPolicy.
   * The mutations of a policy are invoked for each binding of this policy
   * and reinvocation of mutations occurs on a per binding basis.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#mutations
   */
  readonly mutations?: NamespacedMutatingPolicyV1Beta1SpecMutations[];

  /**
   * reinvocationPolicy indicates whether mutations may be called multiple times per MutatingAdmissionPolicyBinding
   * as part of a single admission evaluation.
   * Allowed values are "Never" and "IfNeeded".
   *
   * Never: These mutations will not be called more than once per binding in a single admission evaluation.
   *
   * IfNeeded: These mutations may be invoked more than once per binding for a single admission request and there is no guarantee of
   * order with respect to other admission plugins, admission webhooks, bindings of this policy and admission policies.  Mutations are only
   * reinvoked when mutations change the object after this mutation is invoked.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#reinvocationPolicy
   */
  readonly reinvocationPolicy?: string;

  /**
   * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#targetMatchConstraints
   */
  readonly targetMatchConstraints?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints;

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#variables
   */
  readonly variables?: NamespacedMutatingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedMutatingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1Spec(
  obj: NamespacedMutatingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    autogen: toJson_NamespacedMutatingPolicyV1Beta1SpecAutogen(obj.autogen),
    evaluation: toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluation(
      obj.evaluation,
    ),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints:
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraints(
        obj.matchConstraints,
      ),
    mutations: obj.mutations?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecMutations(y),
    ),
    reinvocationPolicy: obj.reinvocationPolicy,
    targetMatchConstraints:
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints(
        obj.targetMatchConstraints,
      ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecAutogen
 */
export interface NamespacedMutatingPolicyV1Beta1SpecAutogen {
  /**
   * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecAutogen#mutatingAdmissionPolicy
   */
  readonly mutatingAdmissionPolicy?: NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy;

  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecAutogen#podControllers
   */
  readonly podControllers?: NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecAutogen(
  obj: NamespacedMutatingPolicyV1Beta1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    mutatingAdmissionPolicy:
      toJson_NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy(
        obj.mutatingAdmissionPolicy,
      ),
    podControllers:
      toJson_NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers(
        obj.podControllers,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for mutating policy evaluation.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluation
 */
export interface NamespacedMutatingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * Background controls policy evaluation during background scan.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluation#background
   */
  readonly background?: NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluation#mode
   */
  readonly mode?: string;

  /**
   * MutateExisting controls whether existing resources are mutated.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluation#mutateExisting
   */
  readonly mutateExisting?: NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluation(
  obj: NamespacedMutatingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
    mutateExisting:
      toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting(
        obj.mutateExisting,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema NamespacedMutatingPolicyV1Beta1SpecFailurePolicy
 */
export enum NamespacedMutatingPolicyV1Beta1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConditions
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConditions(
  obj: NamespacedMutatingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to evaluate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraints
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraints(
  obj: NamespacedMutatingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Mutation specifies the CEL expression which is used to apply the Mutation.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMutations
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMutations {
  /**
   * applyConfiguration defines the desired configuration values of an object.
   * The configuration is applied to the admission object using
   * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
   * A CEL expression is used to create apply configuration.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMutations#applyConfiguration
   */
  readonly applyConfiguration?: NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration;

  /**
   * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
   * A CEL expression is used to create the JSON patch.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMutations#jsonPatch
   */
  readonly jsonPatch?: NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch;

  /**
   * patchType indicates the patch strategy used.
   * Allowed values are "ApplyConfiguration" and "JSONPatch".
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMutations#patchType
   */
  readonly patchType: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMutations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMutations(
  obj: NamespacedMutatingPolicyV1Beta1SpecMutations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    applyConfiguration:
      toJson_NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration(
        obj.applyConfiguration,
      ),
    jsonPatch: toJson_NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch(
      obj.jsonPatch,
    ),
    patchType: obj.patchType,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * TargetMatchConstraints specifies what target mutation resources this policy is designed to evaluate.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules[];

  /**
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints#expression
   */
  readonly expression?: string;

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints(
  obj: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    expression: obj.expression,
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecVariables
 */
export interface NamespacedMutatingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecVariables(
  obj: NamespacedMutatingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration(
  obj: NamespacedMutatingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutatingAdmissionPolicy specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy
 */
export interface NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes MutatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecAutogenMutatingAdmissionPolicy
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers
 */
export interface NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers {
  /**
   * @schema NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers(
  obj: NamespacedMutatingPolicyV1Beta1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission(
  obj: NamespacedMutatingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background controls policy evaluation during background scan.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground
 */
export interface NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground(
  obj: NamespacedMutatingPolicyV1Beta1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MutateExisting controls whether existing resources are mutated.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting
 */
export interface NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting {
  /**
   * Enabled enables mutation of existing resources. Default is false.
   * When spec.targetMatchConstraints is not defined, Kyverno mutates existing resources matched in spec.matchConstraints.
   *
   * @default false.
   * @schema NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting(
  obj: NamespacedMutatingPolicyV1Beta1SpecEvaluationMutateExisting | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * applyConfiguration defines the desired configuration values of an object.
 * The configuration is applied to the admission object using
 * [structured merge diff](https://github.com/kubernetes-sigs/structured-merge-diff).
 * A CEL expression is used to create apply configuration.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration {
  /**
   * expression will be evaluated by CEL to create an apply configuration.
   * ref: https://github.com/google/cel-spec
   *
   * Apply configurations are declared in CEL using object initialization. For example, this CEL expression
   * returns an apply configuration to set a single field:
   *
   * Object{
   * spec: Object.spec{
   * serviceAccountName: "example"
   * }
   * }
   *
   * Apply configurations may not modify atomic structs, maps or arrays due to the risk of accidental deletion of
   * values not included in the apply configuration.
   *
   * CEL expressions have access to the object types needed to create apply configurations:
   *
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMutationsApplyConfiguration
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * jsonPatch defines a [JSON patch](https://jsonpatch.com/) operation to perform a mutation to the object.
 * A CEL expression is used to create the JSON patch.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch {
  /**
   * expression will be evaluated by CEL to create a [JSON patch](https://jsonpatch.com/).
   * ref: https://github.com/google/cel-spec
   *
   * expression must return an array of JSONPatch values.
   *
   * For example, this CEL expression returns a JSON patch to conditionally modify a value:
   *
   * [
   * JSONPatch{op: "test", path: "/spec/example", value: "Red"},
   * JSONPatch{op: "replace", path: "/spec/example", value: "Green"}
   * ]
   *
   * To define an object for the patch value, use Object types. For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/spec/selector",
   * value: Object.spec.selector{matchLabels: {"environment": "test"}}
   * }
   * ]
   *
   * To use strings containing '/' and '~' as JSONPatch path keys, use "jsonpatch.escapeKey". For example:
   *
   * [
   * JSONPatch{
   * op: "add",
   * path: "/metadata/labels/" + jsonpatch.escapeKey("example.com/environment"),
   * value: "test"
   * },
   * ]
   *
   * CEL expressions have access to the types needed to create JSON patches and objects:
   *
   * - 'JSONPatch' - CEL type of JSON Patch operations. JSONPatch has the fields 'op', 'from', 'path' and 'value'.
   * See [JSON patch](https://jsonpatch.com/) for more details. The 'value' field may be set to any of: string,
   * integer, array, map or object.  If set, the 'path' and 'from' fields must be set to a
   * [JSON pointer](https://datatracker.ietf.org/doc/html/rfc6901/) string, where the 'jsonpatch.escapeKey()' CEL
   * function may be used to escape path keys containing '/' and '~'.
   * - 'Object' - CEL type of the resource object.
   * - 'Object.<fieldName>' - CEL type of object field (such as 'Object.spec')
   * - 'Object.<fieldName1>.<fieldName2>...<fieldNameN>` - CEL type of nested field (such as 'Object.spec.containers')
   *
   * CEL expressions have access to the contents of the API request, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * CEL expressions have access to [Kubernetes CEL function libraries](https://kubernetes.io/docs/reference/using-api/cel/#cel-options-language-features-and-libraries)
   * as well as:
   *
   * - 'jsonpatch.escapeKey' - Performs JSONPatch key escaping. '~' and  '/' are escaped as '~0' and `~1' respectively).
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch#expression
   */
  readonly expression?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch(
  obj: NamespacedMutatingPolicyV1Beta1SpecMutationsJsonPatch | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedMutatingPolicyV1Beta1SpecTargetMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedValidatingPolicy
 */
export class NamespacedValidatingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedValidatingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "NamespacedValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedValidatingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedValidatingPolicyProps): any {
    return {
      ...NamespacedValidatingPolicy.GVK,
      ...toJson_NamespacedValidatingPolicyProps(props),
    };
  }

  /**
   * Defines a "NamespacedValidatingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedValidatingPolicyProps,
  ) {
    super(scope, id, {
      ...NamespacedValidatingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedValidatingPolicy.GVK,
      ...toJson_NamespacedValidatingPolicyProps(resolved),
    };
  }
}

/**
 * @schema NamespacedValidatingPolicy
 */
export interface NamespacedValidatingPolicyProps {
  /**
   * @schema NamespacedValidatingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
   *
   * @schema NamespacedValidatingPolicy#spec
   */
  readonly spec: NamespacedValidatingPolicySpec;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyProps(
  obj: NamespacedValidatingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedValidatingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
 *
 * @schema NamespacedValidatingPolicySpec
 */
export interface NamespacedValidatingPolicySpec {
  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema NamespacedValidatingPolicySpec#auditAnnotations
   */
  readonly auditAnnotations?: NamespacedValidatingPolicySpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema NamespacedValidatingPolicySpec#autogen
   */
  readonly autogen?: NamespacedValidatingPolicySpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema NamespacedValidatingPolicySpec#evaluation
   */
  readonly evaluation?: NamespacedValidatingPolicySpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema NamespacedValidatingPolicySpec#failurePolicy
   */
  readonly failurePolicy?: NamespacedValidatingPolicySpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema NamespacedValidatingPolicySpec#matchConditions
   */
  readonly matchConditions?: NamespacedValidatingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: NamespacedValidatingPolicySpecMatchConstraints;

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicySpec#validationActions
   */
  readonly validationActions?: NamespacedValidatingPolicySpecValidationActions[];

  /**
   * Validations contain CEL expressions which is used to apply the validation.
   * Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is
   * required.
   *
   * @schema NamespacedValidatingPolicySpec#validations
   */
  readonly validations?: NamespacedValidatingPolicySpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedValidatingPolicySpec#variables
   */
  readonly variables?: NamespacedValidatingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedValidatingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedValidatingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpec(
  obj: NamespacedValidatingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_NamespacedValidatingPolicySpecAuditAnnotations(y),
    ),
    autogen: toJson_NamespacedValidatingPolicySpecAutogen(obj.autogen),
    evaluation: toJson_NamespacedValidatingPolicySpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedValidatingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_NamespacedValidatingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validations: obj.validations?.map((y) =>
      toJson_NamespacedValidatingPolicySpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedValidatingPolicySpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedValidatingPolicySpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema NamespacedValidatingPolicySpecAuditAnnotations
 */
export interface NamespacedValidatingPolicySpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecAuditAnnotations(
  obj: NamespacedValidatingPolicySpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema NamespacedValidatingPolicySpecAutogen
 */
export interface NamespacedValidatingPolicySpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema NamespacedValidatingPolicySpecAutogen#podControllers
   */
  readonly podControllers?: NamespacedValidatingPolicySpecAutogenPodControllers;

  /**
   * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   *
   * @schema NamespacedValidatingPolicySpecAutogen#validatingAdmissionPolicy
   */
  readonly validatingAdmissionPolicy?: NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecAutogen(
  obj: NamespacedValidatingPolicySpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers: toJson_NamespacedValidatingPolicySpecAutogenPodControllers(
      obj.podControllers,
    ),
    validatingAdmissionPolicy:
      toJson_NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy(
        obj.validatingAdmissionPolicy,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema NamespacedValidatingPolicySpecEvaluation
 */
export interface NamespacedValidatingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedValidatingPolicySpecEvaluation#admission
   */
  readonly admission?: NamespacedValidatingPolicySpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema NamespacedValidatingPolicySpecEvaluation#background
   */
  readonly background?: NamespacedValidatingPolicySpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema NamespacedValidatingPolicySpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecEvaluation(
  obj: NamespacedValidatingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedValidatingPolicySpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_NamespacedValidatingPolicySpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema NamespacedValidatingPolicySpecFailurePolicy
 */
export enum NamespacedValidatingPolicySpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedValidatingPolicySpecMatchConditions
 */
export interface NamespacedValidatingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConditions(
  obj: NamespacedValidatingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedValidatingPolicySpecMatchConstraints
 */
export interface NamespacedValidatingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedValidatingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedValidatingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedValidatingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedValidatingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedValidatingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraints(
  obj: NamespacedValidatingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedValidatingPolicySpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedValidatingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema NamespacedValidatingPolicySpecValidationActions
 */
export enum NamespacedValidatingPolicySpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema NamespacedValidatingPolicySpecValidations
 */
export interface NamespacedValidatingPolicySpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema NamespacedValidatingPolicySpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema NamespacedValidatingPolicySpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema NamespacedValidatingPolicySpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecValidations(
  obj: NamespacedValidatingPolicySpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedValidatingPolicySpecVariables
 */
export interface NamespacedValidatingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedValidatingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedValidatingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecVariables(
  obj: NamespacedValidatingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedValidatingPolicySpecWebhookConfiguration
 */
export interface NamespacedValidatingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedValidatingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecWebhookConfiguration(
  obj: NamespacedValidatingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema NamespacedValidatingPolicySpecAutogenPodControllers
 */
export interface NamespacedValidatingPolicySpecAutogenPodControllers {
  /**
   * @schema NamespacedValidatingPolicySpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecAutogenPodControllers(
  obj: NamespacedValidatingPolicySpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
 *
 * @schema NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy
 */
export interface NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy(
  obj:
    | NamespacedValidatingPolicySpecAutogenValidatingAdmissionPolicy
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedValidatingPolicySpecEvaluationAdmission
 */
export interface NamespacedValidatingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedValidatingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecEvaluationAdmission(
  obj: NamespacedValidatingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema NamespacedValidatingPolicySpecEvaluationBackground
 */
export interface NamespacedValidatingPolicySpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema NamespacedValidatingPolicySpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecEvaluationBackground(
  obj: NamespacedValidatingPolicySpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedValidatingPolicySpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelector
 */
export interface NamespacedValidatingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraintsObjectSelector(
  obj: NamespacedValidatingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules
 */
export interface NamespacedValidatingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedValidatingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraintsResourceRules(
  obj: NamespacedValidatingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema NamespacedValidatingPolicyV1Beta1
 */
export class NamespacedValidatingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "NamespacedValidatingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "NamespacedValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "NamespacedValidatingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: NamespacedValidatingPolicyV1Beta1Props): any {
    return {
      ...NamespacedValidatingPolicyV1Beta1.GVK,
      ...toJson_NamespacedValidatingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "NamespacedValidatingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: NamespacedValidatingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...NamespacedValidatingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...NamespacedValidatingPolicyV1Beta1.GVK,
      ...toJson_NamespacedValidatingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema NamespacedValidatingPolicyV1Beta1
 */
export interface NamespacedValidatingPolicyV1Beta1Props {
  /**
   * @schema NamespacedValidatingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
   *
   * @schema NamespacedValidatingPolicyV1Beta1#spec
   */
  readonly spec: NamespacedValidatingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1Props(
  obj: NamespacedValidatingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_NamespacedValidatingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
 *
 * @schema NamespacedValidatingPolicyV1Beta1Spec
 */
export interface NamespacedValidatingPolicyV1Beta1Spec {
  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#auditAnnotations
   */
  readonly auditAnnotations?: NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#autogen
   */
  readonly autogen?: NamespacedValidatingPolicyV1Beta1SpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: NamespacedValidatingPolicyV1Beta1SpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema NamespacedValidatingPolicyV1Beta1Spec#failurePolicy
   */
  readonly failurePolicy?: NamespacedValidatingPolicyV1Beta1SpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: NamespacedValidatingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraints;

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#validationActions
   */
  readonly validationActions?: NamespacedValidatingPolicyV1Beta1SpecValidationActions[];

  /**
   * Validations contain CEL expressions which is used to apply the validation.
   * Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is
   * required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#validations
   */
  readonly validations?: NamespacedValidatingPolicyV1Beta1SpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#variables
   */
  readonly variables?: NamespacedValidatingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema NamespacedValidatingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1Spec(
  obj: NamespacedValidatingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations(y),
    ),
    autogen: toJson_NamespacedValidatingPolicyV1Beta1SpecAutogen(obj.autogen),
    evaluation: toJson_NamespacedValidatingPolicyV1Beta1SpecEvaluation(
      obj.evaluation,
    ),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints:
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraints(
        obj.matchConstraints,
      ),
    validationActions: obj.validationActions?.map((y) => y),
    validations: obj.validations?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations
 */
export interface NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations(
  obj: NamespacedValidatingPolicyV1Beta1SpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecAutogen
 */
export interface NamespacedValidatingPolicyV1Beta1SpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecAutogen#podControllers
   */
  readonly podControllers?: NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers;

  /**
   * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecAutogen#validatingAdmissionPolicy
   */
  readonly validatingAdmissionPolicy?: NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecAutogen(
  obj: NamespacedValidatingPolicyV1Beta1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers:
      toJson_NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers(
        obj.podControllers,
      ),
    validatingAdmissionPolicy:
      toJson_NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy(
        obj.validatingAdmissionPolicy,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluation
 */
export interface NamespacedValidatingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluation#background
   */
  readonly background?: NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecEvaluation(
  obj: NamespacedValidatingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    background:
      toJson_NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground(
        obj.background,
      ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema NamespacedValidatingPolicyV1Beta1SpecFailurePolicy
 */
export enum NamespacedValidatingPolicyV1Beta1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConditions
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConditions(
  obj: NamespacedValidatingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraints
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraints(
  obj: NamespacedValidatingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
        y,
      ),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecValidationActions
 */
export enum NamespacedValidatingPolicyV1Beta1SpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecValidations
 */
export interface NamespacedValidatingPolicyV1Beta1SpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecValidations(
  obj: NamespacedValidatingPolicyV1Beta1SpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecVariables
 */
export interface NamespacedValidatingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecVariables(
  obj: NamespacedValidatingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration(
  obj: NamespacedValidatingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers
 */
export interface NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers {
  /**
   * @schema NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers(
  obj: NamespacedValidatingPolicyV1Beta1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy
 */
export interface NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission(
  obj: NamespacedValidatingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground
 */
export interface NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground(
  obj: NamespacedValidatingPolicyV1Beta1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | NamespacedValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PolicyException declares resources to be excluded from specified policies.
 *
 * @schema PolicyException
 */
export class PolicyException extends ApiObject {
  /**
   * Returns the apiVersion and kind for "PolicyException"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "PolicyException",
  };

  /**
   * Renders a Kubernetes manifest for "PolicyException".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: PolicyExceptionProps): any {
    return {
      ...PolicyException.GVK,
      ...toJson_PolicyExceptionProps(props),
    };
  }

  /**
   * Defines a "PolicyException" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: PolicyExceptionProps,
  ) {
    super(scope, id, {
      ...PolicyException.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...PolicyException.GVK,
      ...toJson_PolicyExceptionProps(resolved),
    };
  }
}

/**
 * PolicyException declares resources to be excluded from specified policies.
 *
 * @schema PolicyException
 */
export interface PolicyExceptionProps {
  /**
   * @schema PolicyException#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * Spec declares policy exception behaviors.
   *
   * @schema PolicyException#spec
   */
  readonly spec: PolicyExceptionSpec;
}

/**
 * Converts an object of type 'PolicyExceptionProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionProps(
  obj: PolicyExceptionProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_PolicyExceptionSpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Spec declares policy exception behaviors.
 *
 * @schema PolicyExceptionSpec
 */
export interface PolicyExceptionSpec {
  /**
   * AllowedValues specifies values that can be used in CEL expressions to bypass policy checks.
   * These values can be referenced in CEL expressions via `exceptions.allowedValues`.
   *
   * @schema PolicyExceptionSpec#allowedValues
   */
  readonly allowedValues?: string[];

  /**
   * Images specifies container images to be excluded from policy evaluation.
   * These excluded images can be referenced in CEL expressions via `exceptions.allowedImages`.
   *
   * @schema PolicyExceptionSpec#images
   */
  readonly images?: string[];

  /**
   * MatchConditions is a list of CEL expressions that must be met for a resource to be excluded.
   *
   * @schema PolicyExceptionSpec#matchConditions
   */
  readonly matchConditions?: PolicyExceptionSpecMatchConditions[];

  /**
   * PolicyRefs identifies the policies to which the exception is applied.
   *
   * @schema PolicyExceptionSpec#policyRefs
   */
  readonly policyRefs: PolicyExceptionSpecPolicyRefs[];

  /**
   * ReportResult indicates whether the policy exception should be reported in the policy report
   * as a skip result or pass result. Defaults to "skip".
   *
   * @default skip".
   * @schema PolicyExceptionSpec#reportResult
   */
  readonly reportResult?: PolicyExceptionSpecReportResult;
}

/**
 * Converts an object of type 'PolicyExceptionSpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionSpec(
  obj: PolicyExceptionSpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowedValues: obj.allowedValues?.map((y) => y),
    images: obj.images?.map((y) => y),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_PolicyExceptionSpecMatchConditions(y),
    ),
    policyRefs: obj.policyRefs?.map((y) =>
      toJson_PolicyExceptionSpecPolicyRefs(y),
    ),
    reportResult: obj.reportResult,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema PolicyExceptionSpecMatchConditions
 */
export interface PolicyExceptionSpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema PolicyExceptionSpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema PolicyExceptionSpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'PolicyExceptionSpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionSpecMatchConditions(
  obj: PolicyExceptionSpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema PolicyExceptionSpecPolicyRefs
 */
export interface PolicyExceptionSpecPolicyRefs {
  /**
   * Kind is the kind of the policy
   *
   * @schema PolicyExceptionSpecPolicyRefs#kind
   */
  readonly kind: string;

  /**
   * Name is the name of the policy
   *
   * @schema PolicyExceptionSpecPolicyRefs#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'PolicyExceptionSpecPolicyRefs' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionSpecPolicyRefs(
  obj: PolicyExceptionSpecPolicyRefs | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    kind: obj.kind,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ReportResult indicates whether the policy exception should be reported in the policy report
 * as a skip result or pass result. Defaults to "skip".
 *
 * @default skip".
 * @schema PolicyExceptionSpecReportResult
 */
export enum PolicyExceptionSpecReportResult {
  /** skip */
  SKIP = "skip",
  /** pass */
  PASS = "pass",
}

/**
 * PolicyException declares resources to be excluded from specified policies.
 *
 * @schema PolicyExceptionV1Alpha1
 */
export class PolicyExceptionV1Alpha1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "PolicyExceptionV1Alpha1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1alpha1",
    kind: "PolicyException",
  };

  /**
   * Renders a Kubernetes manifest for "PolicyExceptionV1Alpha1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: PolicyExceptionV1Alpha1Props): any {
    return {
      ...PolicyExceptionV1Alpha1.GVK,
      ...toJson_PolicyExceptionV1Alpha1Props(props),
    };
  }

  /**
   * Defines a "PolicyExceptionV1Alpha1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: PolicyExceptionV1Alpha1Props,
  ) {
    super(scope, id, {
      ...PolicyExceptionV1Alpha1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...PolicyExceptionV1Alpha1.GVK,
      ...toJson_PolicyExceptionV1Alpha1Props(resolved),
    };
  }
}

/**
 * PolicyException declares resources to be excluded from specified policies.
 *
 * @schema PolicyExceptionV1Alpha1
 */
export interface PolicyExceptionV1Alpha1Props {
  /**
   * @schema PolicyExceptionV1Alpha1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * Spec declares policy exception behaviors.
   *
   * @schema PolicyExceptionV1Alpha1#spec
   */
  readonly spec: PolicyExceptionV1Alpha1Spec;
}

/**
 * Converts an object of type 'PolicyExceptionV1Alpha1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Alpha1Props(
  obj: PolicyExceptionV1Alpha1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_PolicyExceptionV1Alpha1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Spec declares policy exception behaviors.
 *
 * @schema PolicyExceptionV1Alpha1Spec
 */
export interface PolicyExceptionV1Alpha1Spec {
  /**
   * AllowedValues specifies values that can be used in CEL expressions to bypass policy checks.
   * These values can be referenced in CEL expressions via `exceptions.allowedValues`.
   *
   * @schema PolicyExceptionV1Alpha1Spec#allowedValues
   */
  readonly allowedValues?: string[];

  /**
   * Images specifies container images to be excluded from policy evaluation.
   * These excluded images can be referenced in CEL expressions via `exceptions.allowedImages`.
   *
   * @schema PolicyExceptionV1Alpha1Spec#images
   */
  readonly images?: string[];

  /**
   * MatchConditions is a list of CEL expressions that must be met for a resource to be excluded.
   *
   * @schema PolicyExceptionV1Alpha1Spec#matchConditions
   */
  readonly matchConditions?: PolicyExceptionV1Alpha1SpecMatchConditions[];

  /**
   * PolicyRefs identifies the policies to which the exception is applied.
   *
   * @schema PolicyExceptionV1Alpha1Spec#policyRefs
   */
  readonly policyRefs: PolicyExceptionV1Alpha1SpecPolicyRefs[];

  /**
   * ReportResult indicates whether the policy exception should be reported in the policy report
   * as a skip result or pass result. Defaults to "skip".
   *
   * @default skip".
   * @schema PolicyExceptionV1Alpha1Spec#reportResult
   */
  readonly reportResult?: PolicyExceptionV1Alpha1SpecReportResult;
}

/**
 * Converts an object of type 'PolicyExceptionV1Alpha1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Alpha1Spec(
  obj: PolicyExceptionV1Alpha1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowedValues: obj.allowedValues?.map((y) => y),
    images: obj.images?.map((y) => y),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_PolicyExceptionV1Alpha1SpecMatchConditions(y),
    ),
    policyRefs: obj.policyRefs?.map((y) =>
      toJson_PolicyExceptionV1Alpha1SpecPolicyRefs(y),
    ),
    reportResult: obj.reportResult,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema PolicyExceptionV1Alpha1SpecMatchConditions
 */
export interface PolicyExceptionV1Alpha1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema PolicyExceptionV1Alpha1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema PolicyExceptionV1Alpha1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'PolicyExceptionV1Alpha1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Alpha1SpecMatchConditions(
  obj: PolicyExceptionV1Alpha1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema PolicyExceptionV1Alpha1SpecPolicyRefs
 */
export interface PolicyExceptionV1Alpha1SpecPolicyRefs {
  /**
   * Kind is the kind of the policy
   *
   * @schema PolicyExceptionV1Alpha1SpecPolicyRefs#kind
   */
  readonly kind: string;

  /**
   * Name is the name of the policy
   *
   * @schema PolicyExceptionV1Alpha1SpecPolicyRefs#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'PolicyExceptionV1Alpha1SpecPolicyRefs' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Alpha1SpecPolicyRefs(
  obj: PolicyExceptionV1Alpha1SpecPolicyRefs | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    kind: obj.kind,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ReportResult indicates whether the policy exception should be reported in the policy report
 * as a skip result or pass result. Defaults to "skip".
 *
 * @default skip".
 * @schema PolicyExceptionV1Alpha1SpecReportResult
 */
export enum PolicyExceptionV1Alpha1SpecReportResult {
  /** skip */
  SKIP = "skip",
  /** pass */
  PASS = "pass",
}

/**
 * PolicyException declares resources to be excluded from specified policies.
 *
 * @schema PolicyExceptionV1Beta1
 */
export class PolicyExceptionV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "PolicyExceptionV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "PolicyException",
  };

  /**
   * Renders a Kubernetes manifest for "PolicyExceptionV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: PolicyExceptionV1Beta1Props): any {
    return {
      ...PolicyExceptionV1Beta1.GVK,
      ...toJson_PolicyExceptionV1Beta1Props(props),
    };
  }

  /**
   * Defines a "PolicyExceptionV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: PolicyExceptionV1Beta1Props,
  ) {
    super(scope, id, {
      ...PolicyExceptionV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...PolicyExceptionV1Beta1.GVK,
      ...toJson_PolicyExceptionV1Beta1Props(resolved),
    };
  }
}

/**
 * PolicyException declares resources to be excluded from specified policies.
 *
 * @schema PolicyExceptionV1Beta1
 */
export interface PolicyExceptionV1Beta1Props {
  /**
   * @schema PolicyExceptionV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * Spec declares policy exception behaviors.
   *
   * @schema PolicyExceptionV1Beta1#spec
   */
  readonly spec: PolicyExceptionV1Beta1Spec;
}

/**
 * Converts an object of type 'PolicyExceptionV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Beta1Props(
  obj: PolicyExceptionV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_PolicyExceptionV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Spec declares policy exception behaviors.
 *
 * @schema PolicyExceptionV1Beta1Spec
 */
export interface PolicyExceptionV1Beta1Spec {
  /**
   * AllowedValues specifies values that can be used in CEL expressions to bypass policy checks.
   * These values can be referenced in CEL expressions via `exceptions.allowedValues`.
   *
   * @schema PolicyExceptionV1Beta1Spec#allowedValues
   */
  readonly allowedValues?: string[];

  /**
   * Images specifies container images to be excluded from policy evaluation.
   * These excluded images can be referenced in CEL expressions via `exceptions.allowedImages`.
   *
   * @schema PolicyExceptionV1Beta1Spec#images
   */
  readonly images?: string[];

  /**
   * MatchConditions is a list of CEL expressions that must be met for a resource to be excluded.
   *
   * @schema PolicyExceptionV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: PolicyExceptionV1Beta1SpecMatchConditions[];

  /**
   * PolicyRefs identifies the policies to which the exception is applied.
   *
   * @schema PolicyExceptionV1Beta1Spec#policyRefs
   */
  readonly policyRefs: PolicyExceptionV1Beta1SpecPolicyRefs[];

  /**
   * ReportResult indicates whether the policy exception should be reported in the policy report
   * as a skip result or pass result. Defaults to "skip".
   *
   * @default skip".
   * @schema PolicyExceptionV1Beta1Spec#reportResult
   */
  readonly reportResult?: PolicyExceptionV1Beta1SpecReportResult;
}

/**
 * Converts an object of type 'PolicyExceptionV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Beta1Spec(
  obj: PolicyExceptionV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    allowedValues: obj.allowedValues?.map((y) => y),
    images: obj.images?.map((y) => y),
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_PolicyExceptionV1Beta1SpecMatchConditions(y),
    ),
    policyRefs: obj.policyRefs?.map((y) =>
      toJson_PolicyExceptionV1Beta1SpecPolicyRefs(y),
    ),
    reportResult: obj.reportResult,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema PolicyExceptionV1Beta1SpecMatchConditions
 */
export interface PolicyExceptionV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema PolicyExceptionV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema PolicyExceptionV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'PolicyExceptionV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Beta1SpecMatchConditions(
  obj: PolicyExceptionV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * @schema PolicyExceptionV1Beta1SpecPolicyRefs
 */
export interface PolicyExceptionV1Beta1SpecPolicyRefs {
  /**
   * Kind is the kind of the policy
   *
   * @schema PolicyExceptionV1Beta1SpecPolicyRefs#kind
   */
  readonly kind: string;

  /**
   * Name is the name of the policy
   *
   * @schema PolicyExceptionV1Beta1SpecPolicyRefs#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'PolicyExceptionV1Beta1SpecPolicyRefs' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_PolicyExceptionV1Beta1SpecPolicyRefs(
  obj: PolicyExceptionV1Beta1SpecPolicyRefs | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    kind: obj.kind,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ReportResult indicates whether the policy exception should be reported in the policy report
 * as a skip result or pass result. Defaults to "skip".
 *
 * @default skip".
 * @schema PolicyExceptionV1Beta1SpecReportResult
 */
export enum PolicyExceptionV1Beta1SpecReportResult {
  /** skip */
  SKIP = "skip",
  /** pass */
  PASS = "pass",
}

/**
 *
 *
 * @schema ValidatingPolicy
 */
export class ValidatingPolicy extends ApiObject {
  /**
   * Returns the apiVersion and kind for "ValidatingPolicy"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1",
    kind: "ValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "ValidatingPolicy".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: ValidatingPolicyProps): any {
    return {
      ...ValidatingPolicy.GVK,
      ...toJson_ValidatingPolicyProps(props),
    };
  }

  /**
   * Defines a "ValidatingPolicy" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: ValidatingPolicyProps,
  ) {
    super(scope, id, {
      ...ValidatingPolicy.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...ValidatingPolicy.GVK,
      ...toJson_ValidatingPolicyProps(resolved),
    };
  }
}

/**
 * @schema ValidatingPolicy
 */
export interface ValidatingPolicyProps {
  /**
   * @schema ValidatingPolicy#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
   *
   * @schema ValidatingPolicy#spec
   */
  readonly spec: ValidatingPolicySpec;
}

/**
 * Converts an object of type 'ValidatingPolicyProps' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyProps(
  obj: ValidatingPolicyProps | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_ValidatingPolicySpec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
 *
 * @schema ValidatingPolicySpec
 */
export interface ValidatingPolicySpec {
  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema ValidatingPolicySpec#auditAnnotations
   */
  readonly auditAnnotations?: ValidatingPolicySpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema ValidatingPolicySpec#autogen
   */
  readonly autogen?: ValidatingPolicySpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema ValidatingPolicySpec#evaluation
   */
  readonly evaluation?: ValidatingPolicySpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema ValidatingPolicySpec#failurePolicy
   */
  readonly failurePolicy?: ValidatingPolicySpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema ValidatingPolicySpec#matchConditions
   */
  readonly matchConditions?: ValidatingPolicySpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema ValidatingPolicySpec#matchConstraints
   */
  readonly matchConstraints?: ValidatingPolicySpecMatchConstraints;

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema ValidatingPolicySpec#validationActions
   */
  readonly validationActions?: ValidatingPolicySpecValidationActions[];

  /**
   * Validations contain CEL expressions which is used to apply the validation.
   * Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is
   * required.
   *
   * @schema ValidatingPolicySpec#validations
   */
  readonly validations?: ValidatingPolicySpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema ValidatingPolicySpec#variables
   */
  readonly variables?: ValidatingPolicySpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema ValidatingPolicySpec#webhookConfiguration
   */
  readonly webhookConfiguration?: ValidatingPolicySpecWebhookConfiguration;
}

/**
 * Converts an object of type 'ValidatingPolicySpec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpec(
  obj: ValidatingPolicySpec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_ValidatingPolicySpecAuditAnnotations(y),
    ),
    autogen: toJson_ValidatingPolicySpecAutogen(obj.autogen),
    evaluation: toJson_ValidatingPolicySpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_ValidatingPolicySpecMatchConditions(y),
    ),
    matchConstraints: toJson_ValidatingPolicySpecMatchConstraints(
      obj.matchConstraints,
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validations: obj.validations?.map((y) =>
      toJson_ValidatingPolicySpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_ValidatingPolicySpecVariables(y),
    ),
    webhookConfiguration: toJson_ValidatingPolicySpecWebhookConfiguration(
      obj.webhookConfiguration,
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema ValidatingPolicySpecAuditAnnotations
 */
export interface ValidatingPolicySpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema ValidatingPolicySpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema ValidatingPolicySpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecAuditAnnotations(
  obj: ValidatingPolicySpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema ValidatingPolicySpecAutogen
 */
export interface ValidatingPolicySpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema ValidatingPolicySpecAutogen#podControllers
   */
  readonly podControllers?: ValidatingPolicySpecAutogenPodControllers;

  /**
   * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   *
   * @schema ValidatingPolicySpecAutogen#validatingAdmissionPolicy
   */
  readonly validatingAdmissionPolicy?: ValidatingPolicySpecAutogenValidatingAdmissionPolicy;
}

/**
 * Converts an object of type 'ValidatingPolicySpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecAutogen(
  obj: ValidatingPolicySpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers: toJson_ValidatingPolicySpecAutogenPodControllers(
      obj.podControllers,
    ),
    validatingAdmissionPolicy:
      toJson_ValidatingPolicySpecAutogenValidatingAdmissionPolicy(
        obj.validatingAdmissionPolicy,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema ValidatingPolicySpecEvaluation
 */
export interface ValidatingPolicySpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema ValidatingPolicySpecEvaluation#admission
   */
  readonly admission?: ValidatingPolicySpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema ValidatingPolicySpecEvaluation#background
   */
  readonly background?: ValidatingPolicySpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema ValidatingPolicySpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecEvaluation(
  obj: ValidatingPolicySpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_ValidatingPolicySpecEvaluationAdmission(obj.admission),
    background: toJson_ValidatingPolicySpecEvaluationBackground(obj.background),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema ValidatingPolicySpecFailurePolicy
 */
export enum ValidatingPolicySpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema ValidatingPolicySpecMatchConditions
 */
export interface ValidatingPolicySpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConditions(
  obj: ValidatingPolicySpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema ValidatingPolicySpecMatchConstraints
 */
export interface ValidatingPolicySpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema ValidatingPolicySpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: ValidatingPolicySpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema ValidatingPolicySpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ValidatingPolicySpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: ValidatingPolicySpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ValidatingPolicySpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: ValidatingPolicySpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema ValidatingPolicySpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: ValidatingPolicySpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraints(
  obj: ValidatingPolicySpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_ValidatingPolicySpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_ValidatingPolicySpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector: toJson_ValidatingPolicySpecMatchConstraintsObjectSelector(
      obj.objectSelector,
    ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_ValidatingPolicySpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema ValidatingPolicySpecValidationActions
 */
export enum ValidatingPolicySpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema ValidatingPolicySpecValidations
 */
export interface ValidatingPolicySpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema ValidatingPolicySpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema ValidatingPolicySpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema ValidatingPolicySpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema ValidatingPolicySpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecValidations(
  obj: ValidatingPolicySpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema ValidatingPolicySpecVariables
 */
export interface ValidatingPolicySpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema ValidatingPolicySpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema ValidatingPolicySpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecVariables(
  obj: ValidatingPolicySpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema ValidatingPolicySpecWebhookConfiguration
 */
export interface ValidatingPolicySpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema ValidatingPolicySpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'ValidatingPolicySpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecWebhookConfiguration(
  obj: ValidatingPolicySpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema ValidatingPolicySpecAutogenPodControllers
 */
export interface ValidatingPolicySpecAutogenPodControllers {
  /**
   * @schema ValidatingPolicySpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicySpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecAutogenPodControllers(
  obj: ValidatingPolicySpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
 *
 * @schema ValidatingPolicySpecAutogenValidatingAdmissionPolicy
 */
export interface ValidatingPolicySpecAutogenValidatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema ValidatingPolicySpecAutogenValidatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicySpecAutogenValidatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecAutogenValidatingAdmissionPolicy(
  obj: ValidatingPolicySpecAutogenValidatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema ValidatingPolicySpecEvaluationAdmission
 */
export interface ValidatingPolicySpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema ValidatingPolicySpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicySpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecEvaluationAdmission(
  obj: ValidatingPolicySpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema ValidatingPolicySpecEvaluationBackground
 */
export interface ValidatingPolicySpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema ValidatingPolicySpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicySpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecEvaluationBackground(
  obj: ValidatingPolicySpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules
 */
export interface ValidatingPolicySpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ValidatingPolicySpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraintsExcludeResourceRules(
  obj: ValidatingPolicySpecMatchConstraintsExcludeResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelector
 */
export interface ValidatingPolicySpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraintsNamespaceSelector(
  obj: ValidatingPolicySpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ValidatingPolicySpecMatchConstraintsObjectSelector
 */
export interface ValidatingPolicySpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ValidatingPolicySpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ValidatingPolicySpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraintsObjectSelector(
  obj: ValidatingPolicySpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ValidatingPolicySpecMatchConstraintsResourceRules
 */
export interface ValidatingPolicySpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ValidatingPolicySpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ValidatingPolicySpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ValidatingPolicySpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraintsResourceRules(
  obj: ValidatingPolicySpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | ValidatingPolicySpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | ValidatingPolicySpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema ValidatingPolicyV1Alpha1
 */
export class ValidatingPolicyV1Alpha1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "ValidatingPolicyV1Alpha1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1alpha1",
    kind: "ValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "ValidatingPolicyV1Alpha1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: ValidatingPolicyV1Alpha1Props): any {
    return {
      ...ValidatingPolicyV1Alpha1.GVK,
      ...toJson_ValidatingPolicyV1Alpha1Props(props),
    };
  }

  /**
   * Defines a "ValidatingPolicyV1Alpha1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: ValidatingPolicyV1Alpha1Props,
  ) {
    super(scope, id, {
      ...ValidatingPolicyV1Alpha1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...ValidatingPolicyV1Alpha1.GVK,
      ...toJson_ValidatingPolicyV1Alpha1Props(resolved),
    };
  }
}

/**
 * @schema ValidatingPolicyV1Alpha1
 */
export interface ValidatingPolicyV1Alpha1Props {
  /**
   * @schema ValidatingPolicyV1Alpha1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
   *
   * @schema ValidatingPolicyV1Alpha1#spec
   */
  readonly spec: ValidatingPolicyV1Alpha1Spec;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1Props(
  obj: ValidatingPolicyV1Alpha1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_ValidatingPolicyV1Alpha1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
 *
 * @schema ValidatingPolicyV1Alpha1Spec
 */
export interface ValidatingPolicyV1Alpha1Spec {
  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#auditAnnotations
   */
  readonly auditAnnotations?: ValidatingPolicyV1Alpha1SpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#autogen
   */
  readonly autogen?: ValidatingPolicyV1Alpha1SpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#evaluation
   */
  readonly evaluation?: ValidatingPolicyV1Alpha1SpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema ValidatingPolicyV1Alpha1Spec#failurePolicy
   */
  readonly failurePolicy?: ValidatingPolicyV1Alpha1SpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema ValidatingPolicyV1Alpha1Spec#matchConditions
   */
  readonly matchConditions?: ValidatingPolicyV1Alpha1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#matchConstraints
   */
  readonly matchConstraints?: ValidatingPolicyV1Alpha1SpecMatchConstraints;

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#validationActions
   */
  readonly validationActions?: ValidatingPolicyV1Alpha1SpecValidationActions[];

  /**
   * Validations contain CEL expressions which is used to apply the validation.
   * Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is
   * required.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#validations
   */
  readonly validations?: ValidatingPolicyV1Alpha1SpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#variables
   */
  readonly variables?: ValidatingPolicyV1Alpha1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema ValidatingPolicyV1Alpha1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: ValidatingPolicyV1Alpha1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1Spec(
  obj: ValidatingPolicyV1Alpha1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecAuditAnnotations(y),
    ),
    autogen: toJson_ValidatingPolicyV1Alpha1SpecAutogen(obj.autogen),
    evaluation: toJson_ValidatingPolicyV1Alpha1SpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_ValidatingPolicyV1Alpha1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validations: obj.validations?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_ValidatingPolicyV1Alpha1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema ValidatingPolicyV1Alpha1SpecAuditAnnotations
 */
export interface ValidatingPolicyV1Alpha1SpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecAuditAnnotations(
  obj: ValidatingPolicyV1Alpha1SpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema ValidatingPolicyV1Alpha1SpecAutogen
 */
export interface ValidatingPolicyV1Alpha1SpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema ValidatingPolicyV1Alpha1SpecAutogen#podControllers
   */
  readonly podControllers?: ValidatingPolicyV1Alpha1SpecAutogenPodControllers;

  /**
   * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   *
   * @schema ValidatingPolicyV1Alpha1SpecAutogen#validatingAdmissionPolicy
   */
  readonly validatingAdmissionPolicy?: ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecAutogen(
  obj: ValidatingPolicyV1Alpha1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers: toJson_ValidatingPolicyV1Alpha1SpecAutogenPodControllers(
      obj.podControllers,
    ),
    validatingAdmissionPolicy:
      toJson_ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy(
        obj.validatingAdmissionPolicy,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema ValidatingPolicyV1Alpha1SpecEvaluation
 */
export interface ValidatingPolicyV1Alpha1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema ValidatingPolicyV1Alpha1SpecEvaluation#admission
   */
  readonly admission?: ValidatingPolicyV1Alpha1SpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema ValidatingPolicyV1Alpha1SpecEvaluation#background
   */
  readonly background?: ValidatingPolicyV1Alpha1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema ValidatingPolicyV1Alpha1SpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecEvaluation(
  obj: ValidatingPolicyV1Alpha1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_ValidatingPolicyV1Alpha1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_ValidatingPolicyV1Alpha1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema ValidatingPolicyV1Alpha1SpecFailurePolicy
 */
export enum ValidatingPolicyV1Alpha1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema ValidatingPolicyV1Alpha1SpecMatchConditions
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConditions(
  obj: ValidatingPolicyV1Alpha1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraints
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraints(
  obj: ValidatingPolicyV1Alpha1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
        y,
      ),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema ValidatingPolicyV1Alpha1SpecValidationActions
 */
export enum ValidatingPolicyV1Alpha1SpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema ValidatingPolicyV1Alpha1SpecValidations
 */
export interface ValidatingPolicyV1Alpha1SpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema ValidatingPolicyV1Alpha1SpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema ValidatingPolicyV1Alpha1SpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema ValidatingPolicyV1Alpha1SpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecValidations(
  obj: ValidatingPolicyV1Alpha1SpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema ValidatingPolicyV1Alpha1SpecVariables
 */
export interface ValidatingPolicyV1Alpha1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema ValidatingPolicyV1Alpha1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema ValidatingPolicyV1Alpha1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecVariables(
  obj: ValidatingPolicyV1Alpha1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema ValidatingPolicyV1Alpha1SpecWebhookConfiguration
 */
export interface ValidatingPolicyV1Alpha1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema ValidatingPolicyV1Alpha1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecWebhookConfiguration(
  obj: ValidatingPolicyV1Alpha1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema ValidatingPolicyV1Alpha1SpecAutogenPodControllers
 */
export interface ValidatingPolicyV1Alpha1SpecAutogenPodControllers {
  /**
   * @schema ValidatingPolicyV1Alpha1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecAutogenPodControllers(
  obj: ValidatingPolicyV1Alpha1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
 *
 * @schema ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy
 */
export interface ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy(
  obj: ValidatingPolicyV1Alpha1SpecAutogenValidatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema ValidatingPolicyV1Alpha1SpecEvaluationAdmission
 */
export interface ValidatingPolicyV1Alpha1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema ValidatingPolicyV1Alpha1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecEvaluationAdmission(
  obj: ValidatingPolicyV1Alpha1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema ValidatingPolicyV1Alpha1SpecEvaluationBackground
 */
export interface ValidatingPolicyV1Alpha1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema ValidatingPolicyV1Alpha1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecEvaluationBackground(
  obj: ValidatingPolicyV1Alpha1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | ValidatingPolicyV1Alpha1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector(
  obj:
    | ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelector
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector(
  obj: ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules(
  obj: ValidatingPolicyV1Alpha1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | ValidatingPolicyV1Alpha1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | ValidatingPolicyV1Alpha1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 *
 *
 * @schema ValidatingPolicyV1Beta1
 */
export class ValidatingPolicyV1Beta1 extends ApiObject {
  /**
   * Returns the apiVersion and kind for "ValidatingPolicyV1Beta1"
   */
  public static readonly GVK: GroupVersionKind = {
    apiVersion: "policies.kyverno.io/v1beta1",
    kind: "ValidatingPolicy",
  };

  /**
   * Renders a Kubernetes manifest for "ValidatingPolicyV1Beta1".
   *
   * This can be used to inline resource manifests inside other objects (e.g. as templates).
   *
   * @param props initialization props
   */
  public static manifest(props: ValidatingPolicyV1Beta1Props): any {
    return {
      ...ValidatingPolicyV1Beta1.GVK,
      ...toJson_ValidatingPolicyV1Beta1Props(props),
    };
  }

  /**
   * Defines a "ValidatingPolicyV1Beta1" API object
   * @param scope the scope in which to define this object
   * @param id a scope-local name for the object
   * @param props initialization props
   */
  public constructor(
    scope: Construct,
    id: string,
    props: ValidatingPolicyV1Beta1Props,
  ) {
    super(scope, id, {
      ...ValidatingPolicyV1Beta1.GVK,
      ...props,
    });
  }

  /**
   * Renders the object to Kubernetes JSON.
   */
  public override toJson(): any {
    const resolved = super.toJson();

    return {
      ...ValidatingPolicyV1Beta1.GVK,
      ...toJson_ValidatingPolicyV1Beta1Props(resolved),
    };
  }
}

/**
 * @schema ValidatingPolicyV1Beta1
 */
export interface ValidatingPolicyV1Beta1Props {
  /**
   * @schema ValidatingPolicyV1Beta1#metadata
   */
  readonly metadata?: ApiObjectMetadata;

  /**
   * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
   *
   * @schema ValidatingPolicyV1Beta1#spec
   */
  readonly spec: ValidatingPolicyV1Beta1Spec;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1Props' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1Props(
  obj: ValidatingPolicyV1Beta1Props | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    metadata: obj.metadata,
    spec: toJson_ValidatingPolicyV1Beta1Spec(obj.spec),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingPolicySpec is the specification of the desired behavior of the ValidatingPolicy.
 *
 * @schema ValidatingPolicyV1Beta1Spec
 */
export interface ValidatingPolicyV1Beta1Spec {
  /**
   * auditAnnotations contains CEL expressions which are used to produce audit
   * annotations for the audit event of the API request.
   * validations and auditAnnotations may not both be empty; a least one of validations or auditAnnotations is
   * required.
   *
   * @schema ValidatingPolicyV1Beta1Spec#auditAnnotations
   */
  readonly auditAnnotations?: ValidatingPolicyV1Beta1SpecAuditAnnotations[];

  /**
   * AutogenConfiguration defines the configuration for the generation controller.
   *
   * @schema ValidatingPolicyV1Beta1Spec#autogen
   */
  readonly autogen?: ValidatingPolicyV1Beta1SpecAutogen;

  /**
   * EvaluationConfiguration defines the configuration for the policy evaluation.
   *
   * @schema ValidatingPolicyV1Beta1Spec#evaluation
   */
  readonly evaluation?: ValidatingPolicyV1Beta1SpecEvaluation;

  /**
   * failurePolicy defines how to handle failures for the admission policy. Failures can
   * occur from CEL expression parse errors, type check errors, runtime errors and invalid
   * or mis-configured policy definitions or bindings.
   *
   * failurePolicy does not define how validations that evaluate to false are handled.
   *
   * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
   *
   * Allowed values are Ignore or Fail. Defaults to Fail.
   *
   * @default Fail.
   * @schema ValidatingPolicyV1Beta1Spec#failurePolicy
   */
  readonly failurePolicy?: ValidatingPolicyV1Beta1SpecFailurePolicy;

  /**
   * MatchConditions is a list of conditions that must be met for a request to be validated.
   * Match conditions filter requests that have already been matched by the rules,
   * namespaceSelector, and objectSelector. An empty list of matchConditions matches all requests.
   * There are a maximum of 64 match conditions allowed.
   *
   * The exact matching logic is (in order):
   * 1. If ANY matchCondition evaluates to FALSE, the policy is skipped.
   * 2. If ALL matchConditions evaluate to TRUE, the policy is evaluated.
   * 3. If any matchCondition evaluates to an error (but none are FALSE):
   * - If failurePolicy=Fail, reject the request
   * - If failurePolicy=Ignore, the policy is skipped
   *
   * @schema ValidatingPolicyV1Beta1Spec#matchConditions
   */
  readonly matchConditions?: ValidatingPolicyV1Beta1SpecMatchConditions[];

  /**
   * MatchConstraints specifies what resources this policy is designed to validate.
   * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1Spec#matchConstraints
   */
  readonly matchConstraints?: ValidatingPolicyV1Beta1SpecMatchConstraints;

  /**
   * ValidationAction specifies the action to be taken when the matched resource violates the policy.
   * If a validation evaluates to false it is always enforced according to these actions.
   *
   * Failures defined by the ValidatingAdmissionPolicy's FailurePolicy are enforced according
   * to these actions only if the FailurePolicy is set to Fail, otherwise the failures are
   * ignored. This includes compilation errors, runtime errors and misconfigurations of the policy.
   *
   * validationActions is declared as a set of action values. Order does
   * not matter. validationActions may not contain duplicates of the same action.
   *
   * The supported actions values are:
   *
   * "Deny" specifies that a validation failure results in a denied request.
   *
   * "Warn" specifies that a validation failure is reported to the request client
   * in HTTP Warning headers, with a warning code of 299. Warnings can be sent
   * both for allowed or denied admission responses.
   *
   * "Audit" specifies that a validation failure is recorded in the created reports.
   *
   * Clients should expect to handle additional values by ignoring
   * any values not recognized.
   *
   * "Deny" and "Warn" may not be used together since this combination
   * needlessly duplicates the validation failure both in the
   * API response body and the HTTP warning headers.
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1Spec#validationActions
   */
  readonly validationActions?: ValidatingPolicyV1Beta1SpecValidationActions[];

  /**
   * Validations contain CEL expressions which is used to apply the validation.
   * Validations and AuditAnnotations may not both be empty; a minimum of one Validations or AuditAnnotations is
   * required.
   *
   * @schema ValidatingPolicyV1Beta1Spec#validations
   */
  readonly validations?: ValidatingPolicyV1Beta1SpecValidations[];

  /**
   * Variables contain definitions of variables that can be used in composition of other expressions.
   * Each variable is defined as a named CEL expression.
   * The variables defined here will be available under `variables` in other expressions of the policy
   * except MatchConditions because MatchConditions are evaluated before the rest of the policy.
   *
   * The expression of a variable can refer to other variables defined earlier in the list but not those after.
   * Thus, Variables must be sorted by the order of first appearance and acyclic.
   *
   * @schema ValidatingPolicyV1Beta1Spec#variables
   */
  readonly variables?: ValidatingPolicyV1Beta1SpecVariables[];

  /**
   * WebhookConfiguration defines the configuration for the webhook.
   *
   * @schema ValidatingPolicyV1Beta1Spec#webhookConfiguration
   */
  readonly webhookConfiguration?: ValidatingPolicyV1Beta1SpecWebhookConfiguration;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1Spec' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1Spec(
  obj: ValidatingPolicyV1Beta1Spec | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    auditAnnotations: obj.auditAnnotations?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecAuditAnnotations(y),
    ),
    autogen: toJson_ValidatingPolicyV1Beta1SpecAutogen(obj.autogen),
    evaluation: toJson_ValidatingPolicyV1Beta1SpecEvaluation(obj.evaluation),
    failurePolicy: obj.failurePolicy,
    matchConditions: obj.matchConditions?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecMatchConditions(y),
    ),
    matchConstraints: toJson_ValidatingPolicyV1Beta1SpecMatchConstraints(
      obj.matchConstraints,
    ),
    validationActions: obj.validationActions?.map((y) => y),
    validations: obj.validations?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecValidations(y),
    ),
    variables: obj.variables?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecVariables(y),
    ),
    webhookConfiguration:
      toJson_ValidatingPolicyV1Beta1SpecWebhookConfiguration(
        obj.webhookConfiguration,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AuditAnnotation describes how to produce an audit annotation for an API request.
 *
 * @schema ValidatingPolicyV1Beta1SpecAuditAnnotations
 */
export interface ValidatingPolicyV1Beta1SpecAuditAnnotations {
  /**
   * key specifies the audit annotation key. The audit annotation keys of
   * a ValidatingAdmissionPolicy must be unique. The key must be a qualified
   * name ([A-Za-z0-9][-A-Za-z0-9_.]*) no more than 63 bytes in length.
   *
   * The key is combined with the resource name of the
   * ValidatingAdmissionPolicy to construct an audit annotation key:
   * "{ValidatingAdmissionPolicy name}/{key}".
   *
   * If an admission webhook uses the same resource name as this ValidatingAdmissionPolicy
   * and the same audit annotation key, the annotation key will be identical.
   * In this case, the first annotation written with the key will be included
   * in the audit event and all subsequent annotations with the same key
   * will be discarded.
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecAuditAnnotations#key
   */
  readonly key: string;

  /**
   * valueExpression represents the expression which is evaluated by CEL to
   * produce an audit annotation value. The expression must evaluate to either
   * a string or null value. If the expression evaluates to a string, the
   * audit annotation is included with the string value. If the expression
   * evaluates to null or empty string the audit annotation will be omitted.
   * The valueExpression may be no longer than 5kb in length.
   * If the result of the valueExpression is more than 10kb in length, it
   * will be truncated to 10kb.
   *
   * If multiple ValidatingAdmissionPolicyBinding resources match an
   * API request, then the valueExpression will be evaluated for
   * each binding. All unique values produced by the valueExpressions
   * will be joined together in a comma-separated list.
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecAuditAnnotations#valueExpression
   */
  readonly valueExpression: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecAuditAnnotations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecAuditAnnotations(
  obj: ValidatingPolicyV1Beta1SpecAuditAnnotations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    valueExpression: obj.valueExpression,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * AutogenConfiguration defines the configuration for the generation controller.
 *
 * @schema ValidatingPolicyV1Beta1SpecAutogen
 */
export interface ValidatingPolicyV1Beta1SpecAutogen {
  /**
   * PodControllers specifies whether to generate a pod controllers rules.
   *
   * @schema ValidatingPolicyV1Beta1SpecAutogen#podControllers
   */
  readonly podControllers?: ValidatingPolicyV1Beta1SpecAutogenPodControllers;

  /**
   * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   *
   * @schema ValidatingPolicyV1Beta1SpecAutogen#validatingAdmissionPolicy
   */
  readonly validatingAdmissionPolicy?: ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecAutogen' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecAutogen(
  obj: ValidatingPolicyV1Beta1SpecAutogen | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    podControllers: toJson_ValidatingPolicyV1Beta1SpecAutogenPodControllers(
      obj.podControllers,
    ),
    validatingAdmissionPolicy:
      toJson_ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy(
        obj.validatingAdmissionPolicy,
      ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * EvaluationConfiguration defines the configuration for the policy evaluation.
 *
 * @schema ValidatingPolicyV1Beta1SpecEvaluation
 */
export interface ValidatingPolicyV1Beta1SpecEvaluation {
  /**
   * Admission controls policy evaluation during admission.
   *
   * @schema ValidatingPolicyV1Beta1SpecEvaluation#admission
   */
  readonly admission?: ValidatingPolicyV1Beta1SpecEvaluationAdmission;

  /**
   * Background  controls policy evaluation during background scan.
   *
   * @schema ValidatingPolicyV1Beta1SpecEvaluation#background
   */
  readonly background?: ValidatingPolicyV1Beta1SpecEvaluationBackground;

  /**
   * Mode is the mode of policy evaluation.
   * Allowed values are "Kubernetes" or "JSON".
   * Optional. Default value is "Kubernetes".
   *
   * @schema ValidatingPolicyV1Beta1SpecEvaluation#mode
   */
  readonly mode?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecEvaluation' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecEvaluation(
  obj: ValidatingPolicyV1Beta1SpecEvaluation | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    admission: toJson_ValidatingPolicyV1Beta1SpecEvaluationAdmission(
      obj.admission,
    ),
    background: toJson_ValidatingPolicyV1Beta1SpecEvaluationBackground(
      obj.background,
    ),
    mode: obj.mode,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * failurePolicy defines how to handle failures for the admission policy. Failures can
 * occur from CEL expression parse errors, type check errors, runtime errors and invalid
 * or mis-configured policy definitions or bindings.
 *
 * failurePolicy does not define how validations that evaluate to false are handled.
 *
 * When failurePolicy is set to Fail, the validationActions field define how failures are enforced.
 *
 * Allowed values are Ignore or Fail. Defaults to Fail.
 *
 * @default Fail.
 * @schema ValidatingPolicyV1Beta1SpecFailurePolicy
 */
export enum ValidatingPolicyV1Beta1SpecFailurePolicy {
  /** Ignore */
  IGNORE = "Ignore",
  /** Fail */
  FAIL = "Fail",
}

/**
 * MatchCondition represents a condition which must by fulfilled for a request to be sent to a webhook.
 *
 * @schema ValidatingPolicyV1Beta1SpecMatchConditions
 */
export interface ValidatingPolicyV1Beta1SpecMatchConditions {
  /**
   * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool.
   * CEL expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL variables:
   *
   * 'object' - The object from the incoming request. The value is null for DELETE requests.
   * 'oldObject' - The existing object. The value is null for CREATE requests.
   * 'request' - Attributes of the admission request(/pkg/apis/admission/types.go#AdmissionRequest).
   * 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConditions#expression
   */
  readonly expression: string;

  /**
   * Name is an identifier for this match condition, used for strategic merging of MatchConditions,
   * as well as providing an identifier for logging purposes. A good name should be descriptive of
   * the associated expression.
   * Name must be a qualified name consisting of alphanumeric characters, '-', '_' or '.', and
   * must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
   * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an
   * optional DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
   *
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConditions#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConditions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConditions(
  obj: ValidatingPolicyV1Beta1SpecMatchConditions | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * MatchConstraints specifies what resources this policy is designed to validate.
 * The AdmissionPolicy cares about a request if it matches _all_ Constraints.
 * Required.
 *
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraints
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraints {
  /**
   * ExcludeResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy should not care about.
   * The exclude rules take precedence over include rules (if a resource matches both, it is excluded)
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraints#excludeResourceRules
   */
  readonly excludeResourceRules?: ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules[];

  /**
   * matchPolicy defines how the "MatchResources" list is used to match incoming requests.
   * Allowed values are "Exact" or "Equivalent".
   *
   * - Exact: match a request only if it exactly matches a specified rule.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * but "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would not be sent to the ValidatingAdmissionPolicy.
   *
   * - Equivalent: match a request if modifies a resource listed in rules, even via another API group or version.
   * For example, if deployments can be modified via apps/v1, apps/v1beta1, and extensions/v1beta1,
   * and "rules" only included `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]`,
   * a request to apps/v1beta1 or extensions/v1beta1 would be converted to apps/v1 and sent to the ValidatingAdmissionPolicy.
   *
   * Defaults to "Equivalent"
   *
   * @default Equivalent"
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraints#matchPolicy
   */
  readonly matchPolicy?: string;

  /**
   * NamespaceSelector decides whether to run the admission control policy on an object based
   * on whether the namespace for that object matches the selector. If the
   * object itself is a namespace, the matching is performed on
   * object.metadata.labels. If the object is another cluster scoped resource,
   * it never skips the policy.
   *
   * For example, to run the webhook on any objects whose namespace is not
   * associated with "runlevel" of "0" or "1";  you will set the selector as
   * follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "runlevel",
   * "operator": "NotIn",
   * "values": [
   * "0",
   * "1"
   * ]
   * }
   * ]
   * }
   *
   * If instead you want to only run the policy on any objects whose
   * namespace is associated with the "environment" of "prod" or "staging";
   * you will set the selector as follows:
   * "namespaceSelector": {
   * "matchExpressions": [
   * {
   * "key": "environment",
   * "operator": "In",
   * "values": [
   * "prod",
   * "staging"
   * ]
   * }
   * ]
   * }
   *
   * See
   * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
   * for more examples of label selectors.
   *
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraints#namespaceSelector
   */
  readonly namespaceSelector?: ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector;

  /**
   * ObjectSelector decides whether to run the validation based on if the
   * object has matching labels. objectSelector is evaluated against both
   * the oldObject and newObject that would be sent to the cel validation, and
   * is considered to match if either object matches the selector. A null
   * object (oldObject in the case of create, or newObject in the case of
   * delete) or an object that cannot have labels (like a
   * DeploymentRollback or a PodProxyOptions object) is not considered to
   * match.
   * Use the object selector only if the webhook is opt-in, because end
   * users may skip the admission webhook by setting the labels.
   * Default to the empty LabelSelector, which matches everything.
   *
   * @default the empty LabelSelector, which matches everything.
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraints#objectSelector
   */
  readonly objectSelector?: ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector;

  /**
   * ResourceRules describes what operations on what resources/subresources the ValidatingAdmissionPolicy matches.
   * The policy cares about an operation if it matches _any_ Rule.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraints#resourceRules
   */
  readonly resourceRules?: ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraints' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraints(
  obj: ValidatingPolicyV1Beta1SpecMatchConstraints | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    excludeResourceRules: obj.excludeResourceRules?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(y),
    ),
    matchPolicy: obj.matchPolicy,
    namespaceSelector:
      toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
        obj.namespaceSelector,
      ),
    objectSelector:
      toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
        obj.objectSelector,
      ),
    resourceRules: obj.resourceRules?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(y),
    ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidationAction specifies a policy enforcement action.
 *
 * @schema ValidatingPolicyV1Beta1SpecValidationActions
 */
export enum ValidatingPolicyV1Beta1SpecValidationActions {
  /** Deny */
  DENY = "Deny",
  /** Audit */
  AUDIT = "Audit",
  /** Warn */
  WARN = "Warn",
}

/**
 * Validation specifies the CEL expression which is used to apply the validation.
 *
 * @schema ValidatingPolicyV1Beta1SpecValidations
 */
export interface ValidatingPolicyV1Beta1SpecValidations {
  /**
   * Expression represents the expression which will be evaluated by CEL.
   * ref: https://github.com/google/cel-spec
   * CEL expressions have access to the contents of the API request/response, organized into CEL variables as well as some other useful variables:
   *
   * - 'object' - The object from the incoming request. The value is null for DELETE requests.
   * - 'oldObject' - The existing object. The value is null for CREATE requests.
   * - 'request' - Attributes of the API request([ref](/pkg/apis/admission/types.go#AdmissionRequest)).
   * - 'params' - Parameter resource referred to by the policy binding being evaluated. Only populated if the policy has a ParamKind.
   * - 'namespaceObject' - The namespace object that the incoming object belongs to. The value is null for cluster-scoped resources.
   * - 'variables' - Map of composited variables, from its name to its lazily evaluated value.
   * For example, a variable named 'foo' can be accessed as 'variables.foo'.
   * - 'authorizer' - A CEL Authorizer. May be used to perform authorization checks for the principal (user or service account) of the request.
   * See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
   * - 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured with the
   * request resource.
   *
   * The `apiVersion`, `kind`, `metadata.name` and `metadata.generateName` are always accessible from the root of the
   * object. No other metadata properties are accessible.
   *
   * Only property names of the form `[a-zA-Z_.-/][a-zA-Z0-9_.-/]*` are accessible.
   * Accessible property names are escaped according to the following rules when accessed in the expression:
   * - '__' escapes to '__underscores__'
   * - '.' escapes to '__dot__'
   * - '-' escapes to '__dash__'
   * - '/' escapes to '__slash__'
   * - Property names that exactly match a CEL RESERVED keyword escape to '__{keyword}__'. The keywords are:
   * "true", "false", "null", "in", "as", "break", "const", "continue", "else", "for", "function", "if",
   * "import", "let", "loop", "package", "namespace", "return".
   * Examples:
   * - Expression accessing a property named "namespace": {"Expression": "object.__namespace__ > 0"}
   * - Expression accessing a property named "x-prop": {"Expression": "object.x__dash__prop > 0"}
   * - Expression accessing a property named "redact__d": {"Expression": "object.redact__underscores__d > 0"}
   *
   * Equality on arrays with list type of 'set' or 'map' ignores element order, i.e. [1, 2] == [2, 1].
   * Concatenation on arrays with x-kubernetes-list-type use the semantics of the list type:
   * - 'set': `X + Y` performs a union where the array positions of all elements in `X` are preserved and
   * non-intersecting elements in `Y` are appended, retaining their partial order.
   * - 'map': `X + Y` performs a merge where the array positions of all keys in `X` are preserved but the values
   * are overwritten by values in `Y` when the key sets of `X` and `Y` intersect. Elements in `Y` with
   * non-intersecting keys are appended, retaining their partial order.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecValidations#expression
   */
  readonly expression: string;

  /**
   * Message represents the message displayed when validation fails. The message is required if the Expression contains
   * line breaks. The message must not contain line breaks.
   * If unset, the message is "failed rule: {Rule}".
   * e.g. "must be a URL with the host matching spec.host"
   * If the Expression contains line breaks. Message is required.
   * The message must not contain line breaks.
   * If unset, the message is "failed Expression: {Expression}".
   *
   * @schema ValidatingPolicyV1Beta1SpecValidations#message
   */
  readonly message?: string;

  /**
   * messageExpression declares a CEL expression that evaluates to the validation failure message that is returned when this rule fails.
   * Since messageExpression is used as a failure message, it must evaluate to a string.
   * If both message and messageExpression are present on a validation, then messageExpression will be used if validation fails.
   * If messageExpression results in a runtime error, the runtime error is logged, and the validation failure message is produced
   * as if the messageExpression field were unset. If messageExpression evaluates to an empty string, a string with only spaces, or a string
   * that contains line breaks, then the validation failure message will also be produced as if the messageExpression field were unset, and
   * the fact that messageExpression produced an empty string/string with only spaces/string with line breaks will be logged.
   * messageExpression has access to all the same variables as the `expression` except for 'authorizer' and 'authorizer.requestResource'.
   * Example:
   * "object.x must be less than max ("+string(params.max)+")"
   *
   * @schema ValidatingPolicyV1Beta1SpecValidations#messageExpression
   */
  readonly messageExpression?: string;

  /**
   * Reason represents a machine-readable description of why this validation failed.
   * If this is the first validation in the list to fail, this reason, as well as the
   * corresponding HTTP response code, are used in the
   * HTTP response to the client.
   * The currently supported reasons are: "Unauthorized", "Forbidden", "Invalid", "RequestEntityTooLarge".
   * If not set, StatusReasonInvalid is used in the response to the client.
   *
   * @schema ValidatingPolicyV1Beta1SpecValidations#reason
   */
  readonly reason?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecValidations' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecValidations(
  obj: ValidatingPolicyV1Beta1SpecValidations | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    message: obj.message,
    messageExpression: obj.messageExpression,
    reason: obj.reason,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Variable is the definition of a variable that is used for composition. A variable is defined as a named expression.
 *
 * @schema ValidatingPolicyV1Beta1SpecVariables
 */
export interface ValidatingPolicyV1Beta1SpecVariables {
  /**
   * Expression is the expression that will be evaluated as the value of the variable.
   * The CEL expression has access to the same identifiers as the CEL expressions in Validation.
   *
   * @schema ValidatingPolicyV1Beta1SpecVariables#expression
   */
  readonly expression: string;

  /**
   * Name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
   * The variable can be accessed in other expressions through `variables`
   * For example, if name is "foo", the variable will be available as `variables.foo`
   *
   * @schema ValidatingPolicyV1Beta1SpecVariables#name
   */
  readonly name: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecVariables' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecVariables(
  obj: ValidatingPolicyV1Beta1SpecVariables | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    expression: obj.expression,
    name: obj.name,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * WebhookConfiguration defines the configuration for the webhook.
 *
 * @schema ValidatingPolicyV1Beta1SpecWebhookConfiguration
 */
export interface ValidatingPolicyV1Beta1SpecWebhookConfiguration {
  /**
   * TimeoutSeconds specifies the maximum time in seconds allowed to apply this policy.
   * After the configured time expires, the admission request may fail, or may simply ignore the policy results,
   * based on the failure policy. The default timeout is 10s, the value must be between 1 and 30 seconds.
   *
   * @schema ValidatingPolicyV1Beta1SpecWebhookConfiguration#timeoutSeconds
   */
  readonly timeoutSeconds?: number;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecWebhookConfiguration' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecWebhookConfiguration(
  obj: ValidatingPolicyV1Beta1SpecWebhookConfiguration | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    timeoutSeconds: obj.timeoutSeconds,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * PodControllers specifies whether to generate a pod controllers rules.
 *
 * @schema ValidatingPolicyV1Beta1SpecAutogenPodControllers
 */
export interface ValidatingPolicyV1Beta1SpecAutogenPodControllers {
  /**
   * @schema ValidatingPolicyV1Beta1SpecAutogenPodControllers#controllers
   */
  readonly controllers?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecAutogenPodControllers' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecAutogenPodControllers(
  obj: ValidatingPolicyV1Beta1SpecAutogenPodControllers | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    controllers: obj.controllers?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ValidatingAdmissionPolicy specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
 *
 * @schema ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy
 */
export interface ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy {
  /**
   * Enabled specifies whether to generate a Kubernetes ValidatingAdmissionPolicy.
   * Optional. Defaults to "false" if not specified.
   *
   * @default false" if not specified.
   * @schema ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy(
  obj: ValidatingPolicyV1Beta1SpecAutogenValidatingAdmissionPolicy | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Admission controls policy evaluation during admission.
 *
 * @schema ValidatingPolicyV1Beta1SpecEvaluationAdmission
 */
export interface ValidatingPolicyV1Beta1SpecEvaluationAdmission {
  /**
   * Enabled controls if rules are applied during admission.
   * Optional. Default value is "true".
   *
   * @schema ValidatingPolicyV1Beta1SpecEvaluationAdmission#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecEvaluationAdmission' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecEvaluationAdmission(
  obj: ValidatingPolicyV1Beta1SpecEvaluationAdmission | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * Background  controls policy evaluation during background scan.
 *
 * @schema ValidatingPolicyV1Beta1SpecEvaluationBackground
 */
export interface ValidatingPolicyV1Beta1SpecEvaluationBackground {
  /**
   * Enabled controls if rules are applied to existing resources during a background scan.
   * Optional. Default value is "true". The value must be set to "false" if the policy rule
   * uses variables that are only available in the admission review request (e.g. user name).
   *
   * @schema ValidatingPolicyV1Beta1SpecEvaluationBackground#enabled
   */
  readonly enabled?: boolean;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecEvaluationBackground' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecEvaluationBackground(
  obj: ValidatingPolicyV1Beta1SpecEvaluationBackground | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    enabled: obj.enabled,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules(
  obj:
    | ValidatingPolicyV1Beta1SpecMatchConstraintsExcludeResourceRules
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamespaceSelector decides whether to run the admission control policy on an object based
 * on whether the namespace for that object matches the selector. If the
 * object itself is a namespace, the matching is performed on
 * object.metadata.labels. If the object is another cluster scoped resource,
 * it never skips the policy.
 *
 * For example, to run the webhook on any objects whose namespace is not
 * associated with "runlevel" of "0" or "1";  you will set the selector as
 * follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "runlevel",
 * "operator": "NotIn",
 * "values": [
 * "0",
 * "1"
 * ]
 * }
 * ]
 * }
 *
 * If instead you want to only run the policy on any objects whose
 * namespace is associated with the "environment" of "prod" or "staging";
 * you will set the selector as follows:
 * "namespaceSelector": {
 * "matchExpressions": [
 * {
 * "key": "environment",
 * "operator": "In",
 * "values": [
 * "prod",
 * "staging"
 * ]
 * }
 * ]
 * }
 *
 * See
 * https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
 * for more examples of label selectors.
 *
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchExpressions
   */
  readonly matchExpressions?: ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector(
  obj: ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * ObjectSelector decides whether to run the validation based on if the
 * object has matching labels. objectSelector is evaluated against both
 * the oldObject and newObject that would be sent to the cel validation, and
 * is considered to match if either object matches the selector. A null
 * object (oldObject in the case of create, or newObject in the case of
 * delete) or an object that cannot have labels (like a
 * DeploymentRollback or a PodProxyOptions object) is not considered to
 * match.
 * Use the object selector only if the webhook is opt-in, because end
 * users may skip the admission webhook by setting the labels.
 * Default to the empty LabelSelector, which matches everything.
 *
 * @default the empty LabelSelector, which matches everything.
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector {
  /**
   * matchExpressions is a list of label selector requirements. The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchExpressions
   */
  readonly matchExpressions?: ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions[];

  /**
   * matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
   * map is equivalent to an element of matchExpressions, whose key field is "key", the
   * operator is "In", and the values array contains only "value". The requirements are ANDed.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector#matchLabels
   */
  readonly matchLabels?: { [key: string]: string };
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector(
  obj: ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelector | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    matchExpressions: obj.matchExpressions?.map((y) =>
      toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
        y,
      ),
    ),
    matchLabels:
      obj.matchLabels === undefined
        ? undefined
        : Object.entries(obj.matchLabels).reduce(
            (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
            {},
          ),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * NamedRuleWithOperations is a tuple of Operations and Resources with ResourceNames.
 *
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules {
  /**
   * APIGroups is the API groups the resources belong to. '*' is all groups.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiGroups
   */
  readonly apiGroups?: string[];

  /**
   * APIVersions is the API versions the resources belong to. '*' is all versions.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#apiVersions
   */
  readonly apiVersions?: string[];

  /**
   * Operations is the operations the admission hook cares about - CREATE, UPDATE, DELETE, CONNECT or *
   * for all of those operations and any future admission operations that are added.
   * If '*' is present, the length of the slice must be one.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#operations
   */
  readonly operations?: string[];

  /**
   * ResourceNames is an optional white list of names that the rule applies to.  An empty set means that everything is allowed.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resourceNames
   */
  readonly resourceNames?: string[];

  /**
   * Resources is a list of resources this rule applies to.
   *
   * For example:
   * 'pods' means pods.
   * 'pods/log' means the log subresource of pods.
   * '*' means all resources, but not subresources.
   * 'pods/*' means all subresources of pods.
   * '_/scale' means all scale subresources.
   * '_/*' means all resources and their subresources.
   *
   * If wildcard is present, the validation rule will ensure resources do not
   * overlap with each other.
   *
   * Depending on the enclosing object, subresources might not be allowed.
   * Required.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#resources
   */
  readonly resources?: string[];

  /**
   * scope specifies the scope of this rule.
   * Valid values are "Cluster", "Namespaced", and "*"
   * "Cluster" means that only cluster-scoped resources will match this rule.
   * Namespace API objects are cluster-scoped.
   * "Namespaced" means that only namespaced resources will match this rule.
   * "*" means that there are no scope restrictions.
   * Subresources match the scope of their parent resource.
   * Default is "*".
   *
   * @default .
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules#scope
   */
  readonly scope?: string;
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules(
  obj: ValidatingPolicyV1Beta1SpecMatchConstraintsResourceRules | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    apiGroups: obj.apiGroups?.map((y) => y),
    apiVersions: obj.apiVersions?.map((y) => y),
    operations: obj.operations?.map((y) => y),
    resourceNames: obj.resourceNames?.map((y) => y),
    resources: obj.resources?.map((y) => y),
    scope: obj.scope,
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions(
  obj:
    | ValidatingPolicyV1Beta1SpecMatchConstraintsNamespaceSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */

/**
 * A label selector requirement is a selector that contains values, a key, and an operator that
 * relates the key and values.
 *
 * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
 */
export interface ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions {
  /**
   * key is the label key that the selector applies to.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#key
   */
  readonly key: string;

  /**
   * operator represents a key's relationship to a set of values.
   * Valid operators are In, NotIn, Exists and DoesNotExist.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#operator
   */
  readonly operator: string;

  /**
   * values is an array of string values. If the operator is In or NotIn,
   * the values array must be non-empty. If the operator is Exists or DoesNotExist,
   * the values array must be empty. This array is replaced during a strategic
   * merge patch.
   *
   * @schema ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions#values
   */
  readonly values?: string[];
}

/**
 * Converts an object of type 'ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions' to JSON representation.
 */
/* eslint-disable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
export function toJson_ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions(
  obj:
    | ValidatingPolicyV1Beta1SpecMatchConstraintsObjectSelectorMatchExpressions
    | undefined,
): Record<string, any> | undefined {
  if (obj === undefined) {
    return undefined;
  }
  const result = {
    key: obj.key,
    operator: obj.operator,
    values: obj.values?.map((y) => y),
  };
  // filter undefined values
  return Object.entries(result).reduce(
    (r, i) => (i[1] === undefined ? r : { ...r, [i[0]]: i[1] }),
    {},
  );
}
/* eslint-enable max-len, @stylistic/max-len, quote-props, @stylistic/quote-props */
