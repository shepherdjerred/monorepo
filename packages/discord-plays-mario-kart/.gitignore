dist
node_modules
coverage
.cache
config*.toml
!config.example.toml
!config.dev.toml
error.png
.tmp-earthly-out
*.gba
# The N64Wasm core is built from wasm-src/ by `scripts/build-wasm.sh` and
# staged into the backend assets dir — never commit the binaries or generated
# glue/assets.
*.wasm
packages/backend/assets/n64wasm/
logs
saves
data
# Generated Prisma client (leaderboard) — codegen output, never committed.
**/generated/
.DS_Store
.claude/settings.local.json
wasm-src/dist/
