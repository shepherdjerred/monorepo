---
app: Ghostty
icon: terminal.fill
color: "#1C1C1E"
website: https://ghostty.org
category: New in 1.3.0
---

- Create modal keybinding workflows with named key tables — activate a mode, then use simple keys for actions within it.
- Chain multiple actions to a single keybind so one keystroke triggers a sequence of actions.
- Use the special `catch_all` key to define a fallback that matches any unbound key within a key table.
