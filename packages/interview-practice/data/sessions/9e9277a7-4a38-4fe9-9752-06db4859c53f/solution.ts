// Two Sum
// Input: int n, int[] nums, int target
// Output: int i, int j (space-separated indices)
// First line: n (array length). Second line: n space-separated integers. Third line: target.

const input = await Bun.stdin.text();
const lines = input.trim().split("\n");

// TODO: Parse input and solve

console.log("TODO");
