---
app: jq
icon: curlybraces
color: "#B5BD68"
website: https://jqlang.github.io/jq
category: Filters & Transformation
---

- `map(select(.score > 50))` — Combine `map` and `select` to filter an array in one step
