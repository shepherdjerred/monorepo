---
app: Git
icon: arrow.triangle.branch
color: "#F05032"
website: https://git-scm.com
category: New in 2.48
---

- `git notes add -e <commit>` — Open your $GIT_EDITOR to compose a note before saving it. The new `-e` flag works with both `git notes add` and `git notes append`, letting you write multi-line notes interactively instead of using `-m`.
