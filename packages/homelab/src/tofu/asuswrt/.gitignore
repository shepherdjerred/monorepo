# The asuswrt provider is a custom filesystem-mirror plugin, so its lock file
# holds only the local platform's hash (no multi-platform registry hashes) and
# would break other machines. It is regenerated by `tofu init`; do not commit it.
.terraform.lock.hcl
