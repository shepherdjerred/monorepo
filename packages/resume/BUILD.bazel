load("@npm//:defs.bzl", "npm_link_all_packages")

npm_link_all_packages(name = "node_modules")

exports_files(["package.json"])

# LaTeX resume -- build and deploy handled by Dagger, not Bazel.
# No sources to expose; this is a stub for workspace consistency.
