{
  "name": "homelab",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@shepherdjerred/eslint-config": "file:../eslint-config",
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/bun": "^1.2.18",
    "@typescript-eslint/utils": "^8.46.1",
    "eslint": "^9.30.1",
    "eslint-plugin-unicorn": "^62.0.0",
    "jiti": "^2.6.0",
    "markdownlint-cli2": "^0.20.0",
    "typescript": "^5",
    "typescript-eslint": "^8.35.1"
  },
  "trustedDependencies": [],
  "scripts": {
    "build": "cd src/cdk8s && bun run build",
    "test": "cd src/cdk8s && bun run test && cd ../helm-types && bun run test",
    "lint": "cd src/cdk8s && bun run lint && cd ../deps-email && bun run lint && cd ../helm-types && bun run lint",
    "typecheck": "cd src/cdk8s && bun run typecheck && cd ../deps-email && bun run typecheck && cd ../helm-types && bun run typecheck"
  }
}
