#!/bin/bash
set -euo pipefail
{{ if eq .chezmoi.os "darwin" -}}
if [[ ! -x "$HOME/bin/sync-theme.sh" ]]; then
    echo "ERROR: $HOME/bin/sync-theme.sh missing or not executable; cannot align live theme to macOS appearance." >&2
    exit 1
fi
"$HOME/bin/sync-theme.sh"
{{ end -}}
