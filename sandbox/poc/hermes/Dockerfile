# Thin layer over the official image adding the CLI coding agents Hermes
# delegates to. The base stays pinned; bump deliberately.
FROM nousresearch/hermes-agent:v2026.9.11

RUN npm install -g @anthropic-ai/claude-code @openai/codex
