# PR 816 CI Fix

## Status

Complete

## Context

PR [#816](https://github.com/shepherdjerred/monorepo/pull/816) was failing Buildkite checks after updating old standalone GitHub repository links to monorepo paths.

## Session Log - 2026-05-17

### Done

- Inspected PR #816 and Buildkite build 2458.
- Fixed the failed Scout lint issues in `packages/scout-for-lol/packages/frontend/src/components/FeatureWithImage.astro`, `GalleryItem.astro`, and `WhatsNewBanner.astro`.
- Replaced the invalid GitHub web URL module path in `packages/terraform-provider-asuswrt` with `github.com/shepherdjerred/monorepo/packages/terraform-provider-asuswrt`.
- Ran Prettier on files reported by CI and fixed markdown table alignment in `packages/docs/plans/2026-04-25_polyrepo-link-audit.md`.
- Verified:
  - `bash .buildkite/scripts/prettier.sh`
  - `bun run markdownlint`
  - `GOCACHE=/private/tmp/go-build-cache GOLANGCI_LINT_CACHE=/private/tmp/golangci-lint-cache golangci-lint run ./...` in `packages/terraform-provider-asuswrt`
  - `GOCACHE=/private/tmp/go-build-cache go test ./...` in `packages/terraform-provider-asuswrt`
  - `bun run lint` in `packages/scout-for-lol/packages/frontend`
  - `dagger call generate-and-lint --pkg-dir ./packages/scout-for-lol --pkg scout-for-lol --dep-names eslint-config --dep-dirs ./packages/eslint-config --tsconfig ./tsconfig.base.json`
  - `git diff --check`

### Remaining

- Push the fix commit to `fix/github-links` and monitor the new Buildkite run.

### Caveats

- Local non-Dagger `bun run lint` from `packages/scout-for-lol` needs generated Prisma artifacts first; the Dagger CI target runs generation and passed.
