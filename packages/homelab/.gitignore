!jest.config.js
node_modules
.idea
*.iml
dist/
*.tar
*.tsbuildinfo
.eslintcache

# MCP credentials cache
.cursor/cache/
.claude/settings.local.json

# Worktree initialization marker
.git/worktree-initialized

# Secrets
.env
# Tofu .env only contains op:// references, not secrets
!src/tofu/.env

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfplan

# cdk8s codegen output (regenerated by `bun run scripts/setup.ts`:
#   - src/cdk8s/scripts/update-imports.ts -> generated/imports
#   - src/cdk8s/generate-helm-types       -> generated/helm
# Sensitive (per CLAUDE.md auto-memory: HA-derived types must be CI-generated).
src/cdk8s/generated/
