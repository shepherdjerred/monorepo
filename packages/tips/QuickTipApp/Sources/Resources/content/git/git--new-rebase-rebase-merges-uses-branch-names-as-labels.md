---
app: Git
icon: arrow.triangle.branch
color: "#F05032"
website: https://git-scm.com
category: New in 2.48
---

- `git rebase --rebase-merges` now uses actual branch names as labels in the generated todo list when the branches still exist, making the rebase sequence much easier to read and edit than the old hash-based labels.
