{
  "name": "@shepherdjerred/tips",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cd QuickTipApp && swift build",
    "test": "cd QuickTipApp && swift test",
    "lint": "swiftlint --strict",
    "typecheck": "cd QuickTipApp && swift build"
  }
}
