---
import Badge from "./Badge.astro";

interface Props {
  title: string;
  subtitle?: string;
  badge?: string;
  centered?: boolean;
}

const { title, subtitle, badge, centered = true } = Astro.props;
---

<div class={`mx-auto max-w-2xl ${centered ? "lg:text-center" : ""}`}>
  {badge && <Badge text={badge} color="indigo" />}
  <h2
    class={`mt-2 text-pretty text-4xl font-semibold tracking-tight text-scout-ink sm:text-5xl ${centered ? "lg:text-balance" : ""}`}
  >
    {title}
  </h2>
  {subtitle && <p class="mt-6 text-lg/8 text-scout-subtle ">{subtitle}</p>}
</div>
