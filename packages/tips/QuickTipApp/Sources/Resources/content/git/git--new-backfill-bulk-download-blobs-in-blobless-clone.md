---
app: Git
icon: arrow.triangle.branch
color: "#F05032"
website: https://git-scm.com
category: New in 2.49
---

- `git backfill` — In a blobless partial clone, bulk-download blobs that were lazily omitted at clone time. Run it before going offline or doing operations that need full file contents, instead of fetching one blob at a time on demand.
