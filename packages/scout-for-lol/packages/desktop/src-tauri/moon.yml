language: rust

workspace:
  inheritedTasks:
    exclude: ["build", "generate", "typecheck", "test", "lint"]

tasks:
  fmt-check:
    command: cargo fmt --check
    toolchains: [system]
    inputs:
      - "src/**/*.rs"
      - "Cargo.toml"
