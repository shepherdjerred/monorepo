---
app: mise
icon: wrench.and.screwdriver.fill
color: "#6C5CE7"
website: https://mise.jdx.dev
category: Tasks
---

- `mise run build` — Execute the `build` task with all mise tools and env vars loaded; shorten to `mise build` if no command conflict exists.
