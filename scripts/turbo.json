{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "typecheck": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.buildkite/scripts/**/*.ts"]
    },
    "lint": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.buildkite/**"]
    },
    "test": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.buildkite/scripts/**"]
    },
    "test:ci": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.buildkite/scripts/**"]
    }
  }
}
