#!/bin/bash
DESIRED='{{
  dict
    "ui" (dict "theme" "GitHub Light")
    "mcpServers" (includeTemplate "claude-mcp-servers.json.tmpl" . | fromJson)
  | toPrettyJson
}}'
if [ -s /dev/stdin ]; then
  jq -s '.[0] * .[1]' /dev/stdin <(echo "$DESIRED")
else
  echo "$DESIRED" | jq .
fi
