---
app: Ghostty
icon: terminal.fill
color: "#1C1C1E"
website: https://ghostty.org
category: New in 1.3.0
---

- Use `tab-inherit-working-directory = true` to have new tabs open in the same directory as the current tab.
- Use `split-inherit-working-directory = true` to have new splits inherit the working directory independently.
- These two settings are independent, so you can mix behaviors for tabs versus splits.
