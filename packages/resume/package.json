{
  "name": "@shepherdjerred/resume",
  "version": "1.0.0",
  "private": true,
  "description": "LaTeX resume - built and deployed via Dagger + Buildkite",
  "scripts": {
    "build": "xelatex resume.tex",
    "lint": "true",
    "test": "true",
    "typecheck": "true"
  }
}
