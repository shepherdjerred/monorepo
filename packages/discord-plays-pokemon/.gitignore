dist
node_modules
coverage
.cache
config*.toml
!config.example.toml
!config.dev.toml
error.png
.tmp-earthly-out
*.gba
# The emulator wasm is built from source (scripts/build-wasm.sh locally, the
# Dagger image build in CI) and never committed.
*.wasm
logs
saves
.DS_Store
.claude/settings.local.json
