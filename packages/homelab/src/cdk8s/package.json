{
  "name": "@homelab/cdk8s",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/ && bun run src/app.ts && bun run scripts/patch.ts",
    "lint": "bunx eslint .",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test && bun run test:gpu-resources",
    "test:gpu-resources": "bun run scripts/test-gpu-resources.ts",
    "clear": "rm -rf dist/",
    "build-app": "bun run src/app.ts",
    "patch": "bun run scripts/patch.ts",
    "update-imports": "bun run scripts/update-imports.ts",
    "generate-helm-types": "bun run scripts/generate-helm-types.ts",
    "up": "bun run build && kubectl delete job golink-sync -n golink --ignore-not-found; kubectl delete job dagger-zfs-tuning docker-config-builder -n dagger --ignore-not-found; bun run scripts/helm-render.ts --apply",
    "render": "bun run build && bun run scripts/helm-render.ts --render",
    "diff": "bun run build && bun run scripts/helm-render.ts --diff"
  },
  "dependencies": {
    "cdk8s": "^2.69.60",
    "cdk8s-plus-31": "^2.2.4",
    "constructs": "^10.4.2",
    "cron-parser": "^5.4.0",
    "lodash": "4.18.1",
    "remeda": "^2.22.5",
    "ts-pattern": "^5.7.1",
    "zod": "^4.1.11",
    "@shepherdjerred/helm-types": "file:../helm-types",
    "@grafana/grafana-foundation-sdk": "11.6.0-cogv0.0.x.1769699379"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.17",
    "jiti": "^2.6.1",
    "type-fest": "^5.0.0",
    "yaml": "^2.8.1"
  }
}
