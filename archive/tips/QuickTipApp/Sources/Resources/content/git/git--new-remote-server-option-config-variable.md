---
app: Git
icon: arrow.triangle.branch
color: "#F05032"
website: https://git-scm.com
category: New in 2.48
---

- Set `remote.<name>.serverOption = <value>` in your config to persistently pass a server option to a specific remote, equivalent to giving `--serverOption=<value>` on every `git fetch` or `git push` to that remote.
