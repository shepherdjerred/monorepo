import { palette } from "./colors.ts";

export const clashPalette = {
  canvas: palette.grey[6],
  navy: palette.blue[6],
  frame: palette.gold[4],
  frameInner: palette.gold[5],
  title: palette.gold[1],
  subtitle: palette.gold[2],
} as const;
