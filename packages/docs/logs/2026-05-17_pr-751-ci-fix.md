# PR 751 CI Fix

## Status

In Progress

## Session Log — 2026-05-17

### Done

- Checked PR #751 status and identified `buildkite/monorepo/pr/cloud-tunnel-dns-coverage` as the failing CI context.
- Reproduced the failure with `bun scripts/check-tunnel-dns-coverage.ts`.
- Added Cloudflare DNS records for `jellyfin.sjer.red` and `seerr.sjer.red` in `packages/homelab/src/tofu/cloudflare/sjer-red.tf`.
- Rebased `media-stack-modernization` onto current `origin/main` and resolved the `packages/homelab/src/cdk8s/src/versions.ts` conflict by keeping current main's Bazarr digest and the PR's Seerr/Jellyfin/Kometa pins.

### Remaining

- Push the rebased PR branch and wait for Buildkite to re-run.

### Caveats

- `bun run --cwd packages/homelab lint` did not complete through the package wrapper locally because `bunx eslint` resolved to a global ESLint lacking `jiti`; direct local cdk8s ESLint invocation passed.
