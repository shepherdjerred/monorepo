# Castle Casters Web

Native browser client for Castle Casters.

```bash
bun run dev
bun run test
bun run build
```

The renderer requires WebGPU. Browsers without `navigator.gpu` show an unsupported message instead of a blank canvas.
