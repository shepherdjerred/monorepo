/**
 * The economy's pure rules: which transfers are allowed, what they do to balances, and how amounts
 * read. Uses the value types of the module's {@code app} ports and nothing outside the JDK.
 */
@NullMarked
package com.shepherdjerred.thestorm.economy.domain;

import org.jspecify.annotations.NullMarked;
