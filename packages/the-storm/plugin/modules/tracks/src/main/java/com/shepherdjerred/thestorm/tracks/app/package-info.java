/**
 * The tracks' public ports. A player's level in each track is also granted as permissions ({@link
 * Track#permission(int)}), so most modules gate features with a plain permission check.
 */
@NullMarked
package com.shepherdjerred.thestorm.tracks.app;

import org.jspecify.annotations.NullMarked;
