# Dotfiles and shell configuration (chezmoi-managed).

load("//tools/bazel:shellcheck.bzl", "shellcheck_test")

shellcheck_test(
    name = "shellcheck",
    srcs = glob(
        [
            "**/*.sh",
        ],
        exclude = [
            "node_modules/**",
        ],
    ),
)
