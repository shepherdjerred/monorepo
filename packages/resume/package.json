{
  "name": "@shepherdjerred/resume",
  "version": "1.0.0",
  "private": true,
  "description": "LaTeX resume - built with xelatex, deployed by CI from a build artifact",
  "scripts": {
    "build": "xelatex resume.tex",
    "deploy": "bun ../../scripts/deploy-site.ts resume"
  }
}
