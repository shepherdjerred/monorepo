{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    // build:db + build:search both write into the SQLite file.
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["data/leetcode.db"]
    }
  }
}
