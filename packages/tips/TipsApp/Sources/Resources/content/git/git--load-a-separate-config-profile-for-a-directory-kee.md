---
app: Git
icon: arrow.triangle.branch
color: "#F05032"
website: https://git-scm.com
category: Config & Workflow
---

- `git config --global includeIf "gitdir:~/work/" path=~/.gitconfig-work` — Load a separate config profile for a directory, keeping personal and work identities separate.
