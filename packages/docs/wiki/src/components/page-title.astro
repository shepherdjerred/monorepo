---
import DefaultPageTitle from "@astrojs/starlight/components/PageTitle.astro";

const { sourceKind } = Astro.locals.starlightRoute.entry.data;
---

{sourceKind === "working" ? null : <DefaultPageTitle />}
